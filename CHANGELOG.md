# Changelog

Todas as mudancas notaveis neste projeto serao documentadas neste arquivo.

O formato segue o [Keep a Changelog](https://keepachangelog.com/pt-BR/1.1.0/).

---

## [Unreleased]

### Revertido

- **As 4 correções "cirúrgicas" da costura do anel fechado (Meridiano de Greenwich) descritas anteriormente nesta seção foram TODAS revertidas** (`_tan` em `_jato_flat_overlay`, padding+compensação de DPI em `_jato_raster`, deslocamento de fase em `_jet_flow_sequence`, fechamento explícito do laço em `_draw_jet_flat`, e a variante equivalente em `_draw_jet_stream`/modo tela) — o usuário achou o resultado visual pior que antes e pediu pra desfazer tudo e resolver a causa raiz de outra forma (ver abaixo). Essas correções ficam só na história do git a partir de agora.

### Adicionado

- **Novo campo do globo (s38–s42): `wnd200_meridional_cores_psi200_contornos`** — irmão meridional do `wnd200_zonal_cores_psi200_contornos`: anomalia de VENTO MERIDIONAL em 200 hPa (shaded) + PSI200 (função de corrente) em contorno PRETO, ambos em pêntada móvel de 5 dias. Shaded usa a MESMA paleta/levels do vento zonal (`_PALETA_ANOM_HGT250`, 30 níveis, ±20 m/s via novo `GLOBO_3D_VMAX_WND200_MERIDIONAL_ANOM`). Generalizou o ramo especial de `_uv200_derived_anom_series` para aceitar `campo='v_meridional'` (v200 − climatologia de v, sem Poisson), além do `u_zonal` já existente + wrappers `_wnd200_meridional_reanalise_series`/`_forecast_series`. `globo_3d_anim.py` + `settings.toml`.
- **Novo campo do globo (s38–s42): `wnd200_zonal_cores_psi200_contornos`** — anomalia de VENTO ZONAL em 200 hPa (shaded) + PSI200 (função de corrente) em contorno PRETO. SÓ média móvel de 5 dias (`pentada_movel: 5`, como `olr_cores_psi200_contornos`). Shaded usa a MESMA paleta/levels do `wnd250_zonal_anom` (`_PALETA_ANOM_HGT250`, 30 níveis, ±20 m/s via `GLOBO_3D_VMAX_WND200_ZONAL_ANOM`). Reaproveita o pipeline de uv200 (mesmo dos psi/chi200): nova opção `campo='u_zonal'` em `_uv200_derived_anom_series` (u200 − climatologia de u, sem Poisson) + wrappers `_wnd200_zonal_reanalise_series`/`_forecast_series`. `globo_3d_anim.py` + `settings.toml`.
- **s42: fundo padrão = OCEANO cor sólida (`GLOBO_3D_COR_OCEANO`, default `#0b5292`) + CONTINENTE com a imagem de satélite blue marble.** `GLOBO_3D_BLUE_MARBLE` agora default `true` (só o s42 lê). O blue marble é RECORTADO na terra (via `_land_clip_path`, geometria vetorial 50m projetada) e a água mostra a cor sólida; a variável pedida entra por cima. Ajuste por campo segue o `requerimento_video.md`. `globo_3d_anim.py` + `settings.toml`.
- **Campo shaded plotado SÓ nos continentes, com litoral LISO (sem serrilhado).** Novo `GLOBO_3D_MASCARA_OCEANO_<VAR>=true` recorta o shaded na costa VETORIAL 50m (`_land_clip_path` cacheado por projeção, ~1 s) em vez de mascarar na grade grossa do modelo (que serrilhava a costa e deixava o fundo vazar). O oceano fica de fora do recorte → aparece o fundo. `globo_3d_anim.py`.
- **Transparência central do shaded por variável (`GLOBO_3D_TRANSP_ATE_<VAR>`): `|anom| < valor` vira transparente** (ex.: 4 → −4..+4 somem e aparece o fundo). `globo_3d_anim.py`.
- **Paleta `temp_anom_the_weather_channel` como padrão da anomalia de T850** (`GLOBO_3D_PALETA_TMP850_ANOM` no master): negativo do azul-claro ao roxo, positivo do amarelo-claro ao magenta, BRANCO no centro (zero). Escala reduzida para **±10 °C** (`GLOBO_3D_VMAX_TMP850_ANOM`, default 10, era 15). `settings.toml` + `globo_3d_anim.py`.
- **Cor das costas/divisas/estados por variável (`GLOBO_3D_COR_FRONTEIRAS_<VAR>`) — generalizado para qualquer campo** (antes só z250_abs/z500_abs). Ex.: `black` para realçar sobre o blue marble. `globo_3d_anim.py`.
- **4 novos ícones de alta pressão recoloridos para `#1787ad`** em `Entrada/icones_pressao/` (prefixo `azul_original_`): `ALTA_HN`, `ALTA_HS`, `HIGH_HN`, `HIGH_HS`. Gerados via `_recolor_icon_frames` (troca só o matiz do azul, preserva sombreado/contorno/transparência/animação de 36 frames).
- **s38/s39: além do MP4 (período animado), geram automaticamente uma 2ª saída PNG com a MÉDIA do mesmo período (`DATA_INICIAL..DATA_FINAL`) resumida num quadro só.** Antes só o s41/s42 produziam esse PNG-da-média (junto com um GIF). Agora `gerar_animacao` calcula a série média (`serie_m` e as auxiliares `_hgt_m`/`_mslp_m`/`_olr_m`/`_cont_m`) também para s38/s39 quando `GLOBO_3D_PNG_MEDIA` (nova setting, default `true`) e renderiza o PNG estático via `_render_clip(..., estatico=True, png_path=...)`, com a câmera fixa no ponto final do voo (vista de assentamento = `_camera_path(2, script_id)[-1]`). Arquivo em `<dir>/<script>_<var>_media.png`, ao lado do MP4. O GIF-da-média continua exclusivo do s41/s42 (no s38/s39 seria redundante com o MP4). O MP4 do s38/s39 segue animando a variável normalmente (`GLOBO_3D_MP4_MEDIA_FIXA` continua restrito a s41/s42). `globo_3d_anim.py`, `settings.toml`, `settings.local.example.toml`.
- **Cor das isolinhas de Z250 absoluto sobre campo estranho agora é configurável via `GLOBO_3D_ISOL_HGT250_COR` (default `#666666`).** Ex.: `"black"` para isolinhas pretas sobre `tmp850_anom`. Aplicada na branch nova de `_build_frame` (`globo_3d_anim.py`), via `ctx['hgt_z250_iso_cor']`; `settings.toml` + `settings.local.example.toml`.
- **Isolinhas de altura geopotencial ABSOLUTA em 250 hPa (`GLOBO_3D_ISOL_HGT250_ABS`) agora podem ser plotadas sobre QUALQUER campo do globo (s38/s39), não só nas fichas nativas de Z250 (`z250_anom`/`jet_stream`).** Ex.: `tmp850_anom` em shaded + isolinhas de Z250 absoluto por cima. Reaproveita a infraestrutura já existente da corrente de jato: quando a flag está ligada e a ficha NÃO é uma das nativas de Z250, `gerar_animacao` baixa o `z250_anom` dedicado (`_isol_abs_precisa`) e `_render_clip` reconstrói o Z250 absoluto (anom + `clim_hgt250_daily`) reamostrado para a grade do campo shaded — o mesmo bloco `hgt_z250_abs_cyc` que o jato usa, agora disparado por `(_jato_on or _isol_flag_on)`. As isolinhas são traçadas direto desse Z250 reconstruído (`hgt_z250_iso_levels`/`hgt_z250_abs_cyc`), em cinza escuro, sem envolver a variável de fundo. Intervalo controlado por `GLOBO_3D_ISOL_HGT250_INTERVALO` (default 60 mgp). Se o modelo não tiver Z250, avisa e segue sem as isolinhas (mesmo comportamento gracioso do jato). `globo_3d_anim.py`.
- **Corrente de jato: costura da grade cíclica deslocada para ~160°E quando a câmera enquadra o Meridiano de Greenwich — solução ARQUITETURAL (não mais um patch pontual no desenho), válida para s38 a s42.** Causa raiz real do "corte no Meridiano de Greenwich": `_jet_segments`/`_jato_raster` tratam o array de longitude como um retângulo comum (contourpy não sabe que a borda esquerda e a direita são o mesmo lugar físico no globo) — qualquer efeito de borda dessa reconstrução acontece exatamente onde o array COMEÇA/TERMINA, e isso é sempre em 0°/360° por padrão (`add_cyclic_point` sempre fecha ali). Em vez de caçar cada efeito colateral disso no desenho (o que gerava fixes frágeis e nunca 100% eliminava o resíduo), a correção agora atua na ORIGEM: gira a ORDEM de armazenamento da grade (não altera nenhum ponto físico, só a indexação) para que a borda do array fique em ~160°E (meio do Pacífico) sempre que a câmera (fixa ou em voo) chega perto de Greenwich em algum frame do clipe — ver `_lon_seam_alvo`/`_lon_seam_roll_index`/`_lon_seam_roll_lon`/`_lon_seam_roll` (novo, `globo_3d_anim.py`, logo após `_camera_path`). Aplicado uma vez em `_render_clip` (após a câmera do clipe inteiro já ser conhecida) a TODOS os campos que compartilham a mesma grade cíclica (`vals_cyc`, `hgt_anom_vals_cyc`, `clim_abs_cyc`, `hgt_abs_cyc`, `hgt_z250_abs_cyc`, `mslp_cyc`, e `contour_cyc`/`contour_lon` com sua própria grade) — automático, sem nenhuma nova setting, e sem custo quando a câmera nunca chega perto de Greenwich (roll é pulado). Como `_render_clip` é compartilhado por s38–s42, o fix vale para os 4 globos automaticamente. Detalhe de implementação: o ponto de fechamento duplicado que `add_cyclic_point` adiciona no fim do array (`lon[-1] == lon[0]+360`) precisa ser descartado antes de girar e RECRIADO no novo lugar — girar o array já fechado sem esse cuidado deixa o duplicado preso no MEIO do array, reintroduzindo o mesmo tipo de artefato só que deslocado.

- **Múltiplas caixas de texto livre no mesmo clipe (antes só era possível UMA, via `GLOBO_3D_CAIXA_LIVRE_*`).** Nova setting em lista `GLOBO_3D_CAIXAS_LIVRES_EXTRA` (mesmo padrão de `GLOBO_3D_ICONES_PRESSAO`): cada item é um dict independente (`lat`, `lon`, `texto`, `cor_box`, `cor_texto`, `contorno_cor`, `contorno_lw`, `fontsize`, `largura`, `pad`, `sombra`, timing de fade). `_draw_caixa_livre` (`globo_3d_anim.py`) virou um laço fino que delega pra `_draw_uma_caixa_livre` (lógica original de desenho, agora parametrizada por caixa em vez de ler `ctx['caixa_livre']` fixo); a caixa legada `GLOBO_3D_CAIXA_LIVRE_*` continua funcionando sem mudança nenhuma — vira o primeiro item da lista interna `ctx['caixas_livres']` quando ativa, seguido de todos os itens de `GLOBO_3D_CAIXAS_LIVRES_EXTRA`. Sem limite de quantidade. Usado no s42 pra sinalizar "vale no ocidente"/"vale no oriente" da corrente de jato junto com a caixa original de calor extremo.
- **Recolorização de ícone de pressão via troca de matiz em HSV, preservando saturação/luminosidade (sombra/gradiente do desenho original intactos).** Nova `_recolor_icon_frames(frames, cor_alvo)` (`globo_3d_anim.py`): converte cada frame RGBA pra HSV, substitui o MATIZ só dos pixels dentro da faixa azul (`hue` 0.45–0.72, saturação > 0.12) pelo matiz de `cor_alvo`, mantendo saturação/valor de cada pixel — preto/branco/cinza (contorno, fundo) ficam intocados. Setting `GLOBO_3D_ICONE_ALTA_COR` (default `""` = sem recolor) aplica automaticamente a qualquer ícone `HIGH_*`/`ALTA_*` carregado em `GLOBO_3D_ICONES_PRESSAO`.
- **Redesign definitivo dos ícones de alta pressão (letra "H" e letra "A"), aplicado diretamente nos arquivos de produção `Entrada/icones_pressao/{HIGH,ALTA}_{HN,HS}.gif`.** Ambas as letras recoloridas pra `#09519d` (mesma cor da corrente de jato/caixas de texto). Letra "H": engordada via `scipy.ndimage.binary_dilation` com elemento estruturante QUADRADO (`np.ones((3,3))`, não o padrão losango/cruz — evita quinas chanfradas), preenchida com a cor DOMINANTE do "H" original (não a cor de um pixel arbitrário da borda anti-aliased) pra ficar 100% flat/2D, sem nenhum resquício de relevo/gradiente. Letra "A": reposicionada (subiu) preservando o anti-aliasing original (copia os pixels RGBA diretamente, em vez de reconstruir a partir de uma máscara binária — evita serrilhado nas diagonais); composição por PESO contínuo (`peso = (255-media_rgb)/max`, 0=fundo/1=letra cheia) em vez de um recorte retangular sólido, pra não "morder" as pontas das setas girando quando a caixa delimitadora do "A" cruza a área varrida pelas setas; a posição ANTIGA da letra é apagada com branco sólido total (zona seguramente nunca visitada pelas setas), só a posição NOVA usa o blend ponderado — evita tanto o "relevo" quanto o contorno fantasma residual da posição anterior.
- **`GLOBO_3D_FONTE_TWC`/caixas "título" e "data" do overlay `GLOBO_3D_SO_CREDITO` (s42): fonte reduzida (21→19) e ambas deslocadas levemente à esquerda.** Ajuste fino de `_overlay_guillaume` (`globo_3d_anim.py`): a caixa cinza da data é ancorada à direita da caixa azul do título, então desloca junto automaticamente.
- **Data da caixa cinza do overlay `GLOBO_3D_SO_CREDITO` (s42) convertida de formato BRASILEIRO (`DD/MM`) para INGLÊS ABREVIADO (`Mon D`).** `_fmt_data_br` (`globo_3d_anim.py`, compartilhada por s42 e s43) trocou `strftime('%d/%m')` por `strftime('%b %-d')`; pêntada no mesmo mês agora sai como `"Jul 5–10"` em vez de `"05–10/07"` (cruzando mês/ano, mesmo padrão em inglês abreviado).

### Corrigido

- **PNG-média (s38–s42) de campos com PÊNTADA MÓVEL saía rotulado com UM dia só (ex.: "July 17") em vez do intervalo ("July 17–21") quando a janela tinha exatamente o tamanho da pêntada.** O `rotulo_dias` do PNG-média usava `len(_dts)` (nº de FRAMES da série), mas a pêntada móvel de 5 dias colapsa uma janela de 5 dias em 1 frame só → `rotulo_dias=1` → `_agg_estatico` removia o marcador `pentada_dias` → data vira dia único. Campos sem pêntada (ex.: `wnd250_zonal_anom`) não sofriam (série diária mantém N frames). Corrigido calculando `rotulo_dias` pelos DIAS cobertos: `(último − primeiro frame).days + 1 + (pentada_dias − 1)`. Verificado em todos os campos compostos (olr/chi/psi/wnd200-zonal/wnd200-meridional + psi200) e nos sem pêntada — todos rotulam o intervalo correto. `globo_3d_anim.py`.
- **OLR (shaded) saía em BRANCO no(s) primeiro(s) frame(s) quando a janela incluía dia observado (emenda ERA5/CPC + previsão).** O OLR observado vem do CPC Blended em grade **2,5° nativa** (mantida de propósito) e o previsto do GEFS em **0,5°**; o `xr.concat(join='outer')` da emenda fazia a UNIÃO das grades e enchia de NaN os pontos que só existiam numa delas → o dia observado ficava ~96% NaN na grade fina → shaded transparente (branco). Os contornos (PSI200) não sofriam porque observado e previsto compartilham a grade da climatologia. **Só se manifestava quando a janela cruzava a fronteira observado↔previsão** (ex.: `DATA_INICIAL` = ontem); em previsão pura nunca aparecia. Corrigido com novo `_alinhar_grade_partes` (`globo_3d_anim.py`): antes de cada `concat` da emenda, reamostra (interp linear) as partes para a grade de MAIOR resolução, fechando a longitude com ponto cíclico (lon0+360) para não reintroduzir faixa NaN no limbo leste. Sem efeito quando as partes já compartilham grade. Verificado: frame da emenda foi de 96% → 0% NaN e o render sai colorido. `globo_3d_anim.py`.
- **`wnd200_zonal_cores_psi200_contornos` quebrava no modo forecast com `KeyError: 'u_zonal'`.** Em `_uv200_forecast_series`, o nome do campo era resolvido por `_UV200_DERIVADO[campo]['nome']`, mas o `u_zonal` (vento zonal, ramo especial SEM Poisson) não está registrado nesse dict (só `psi`/`chi`) — a parte de reanálise passava (não usa o dict), mas a de previsão estourava. Corrigido com fallback: `nome = _UV200_DERIVADO[campo]['nome'] if campo in _UV200_DERIVADO else 'wnd200_zonal_anom'` (o `nome` ali é só para log; o campo real é resolvido dentro de `_uv200_derived_anom_series`, que já trata `u_zonal`). `globo_3d_anim.py`.
- **Todas as alterações do campo de anomalia de T850 feitas para o s42 (paleta TWC, escala ±10 °C, T850 só nos continentes e costas pretas) vazavam para o s38/s39.** Eram pedidas SÓ para o s42 mas usavam settings GLOBAIS/por-variável (sem escopo de script), então o `tmp850_anom` de todos os globos mudava junto. Corrigido com o MESMO padrão `_S42` já usado no pcolormesh/sigma — todas passaram a aceitar override POR SCRIPT (`GLOBO_3D_<X>_<VAR>_<SCRIPT>` > `GLOBO_3D_<X>_<VAR>` > ficha) em `_render_clip`: (1) **paleta** virou `GLOBO_3D_PALETA_TMP850_ANOM_S42`; (2) **escala** — o `vmax` da ficha voltou a ±15 °C (original do s38/s39) e criou-se `GLOBO_3D_VMAX_TMP850_ANOM_S42 = 10`; (3) **máscara de oceano** (`GLOBO_3D_MASCARA_OCEANO_<VAR>`) e (4) **cor das fronteiras** (`GLOBO_3D_COR_FRONTEIRAS_<VAR>`) ganharam variante `_<SCRIPT>`. Assim o s42 mantém TWC + ±10 °C + T850 só nos continentes + costas pretas, e o s38/s39 voltam ao ORIGINAL: paleta da ficha (`_PALETA_T850_ANOM`, magenta→carvão), ±15 °C, T850 desenhado no oceano também, costas na cor da ficha, campo CRU (sigma 0) em contourf de bandas. O `niveis=128` da ficha é o mesmo de antes (não era mudança do s42) e foi mantido. `globo_3d_anim.py` + `settings.toml` + `settings.local.toml`.
- **Suavização do shaded (pcolormesh contínuo + sigma) do s42 vazava para s38/s39.** `GLOBO_3D_PCOLORMESH` e `GLOBO_3D_SIGMA_<VAR>` eram GLOBAIS — com eles ligados no settings.local (para o s42), TODO globo (s38/s39/s40/s41) trocava o contourf de bandas pelo gouraud contínuo e suavizava o campo. Agora aceitam override POR SCRIPT: `GLOBO_3D_PCOLORMESH_<SCRIPT>` e `GLOBO_3D_SIGMA_<VAR>_<SCRIPT>` (ex.: `_S42`) têm precedência sobre a global. Assim o s42 fica liso (via `_S42`) e s38/s39 voltam ao contourf/campo cru de sempre (globais no default). `globo_3d_anim.py`.
- **Texto "JET STREAM" do jato (drape, `_jato_flat_overlay._word`): sempre em tamanho cheio, seguindo a DIREÇÃO DO FLUXO, sem sumir e sem empilhar letras nas curvas.** (1) Removido o guard `texto_max_curva` que pulava a palavra em curva fechada (vão) — nunca mais some; (2) removido o `flip` de legibilidade de tela que invertia a palavra inteira (ordem+180) quando a tangente apontava p/ esquerda (`|ang|>90`), deixando-a CONTRA o fluxo e "virada" no cusp — agora cada glifo usa a tangente local `+s` (igual às setas); (3) espaçamento por CORDA (distância reta entre letras) em vez de arco — em curva o arco entre letras > corda, então por arco os glifos ficavam mais próximos que a largura e se empilhavam no lado côncavo. Em curva ~180° muito apertada resta leve embaralho (limite geométrico: raio da curva < altura da letra, inevitável em qualquer ferramenta); decidido com o usuário manter o texto CHEIO com esse leve embaralho em vez de encolher/pular/inverter (todas testadas e rejeitadas). `globo_3d_anim.py`.
- **Caixa de texto livre agora pode ESMAECER no s42 por caixa (antes era sempre fixa) e sai cheia no PNG/GIF.** Novo campo por-caixa `fixa` (setting `GLOBO_3D_CAIXA_LIVRE_FIXA`, default = comportamento antigo por script: fixa no s42, esmaece nos demais) permite uma caixa específica esmaecer na linha do tempo do MP4 (ex.: aparecer no início da cauda, no último dia) mesmo no s42. Como os ícones, uma caixa que esmaece no MP4 sai em alpha CHEIO nas saídas estáticas (`ctx['saida_estatica']` = PNG/GIF) — senão o frame 0 do PNG a deixaria invisível. `globo_3d_anim.py` + `settings.toml`.
- **Ícone de pressão com `fade_in=true` sumia do PNG da média (e oscilava no GIF) — todas as saídas do s42 devem mostrar os ícones.** O fade-in do ícone usa `_prog = f/(total_frames-1)` (fração da linha do tempo do MP4); no PNG estático `total_frames=1` e `f=0` → `_prog=0`, então com `fade_inicio>0` o alpha zerava e o ícone ficava invisível; no GIF (loop do campo médio) o alpha oscilava a cada volta. Corrigido: novo flag `ctx['saida_estatica'] = estatico or gif` faz `_draw_icones_pressao` PULAR o fade nessas saídas (ícone em alpha cheio, ainda girando no GIF). O MP4 mantém o fade animado. `globo_3d_anim.py`.
- **Caixa de texto livre (`GLOBO_3D_CAIXA_LIVRE`) nunca aparecia nos frames de fundo CONGELADO (cauda do MP4 + todo o GIF) quando `GLOBO_3D_BG_CACHE` estava ligado (default).** A caixa só era desenhada no caminho "completo" de `_build_frame`; o fast-path do cache de fundo (`_render_overlay_rgba`, usado em todos os frames >= `_bg_from`) só compunha jato + ícones, nunca a caixa — self-inflicted pela otimização de cache adicionada antes. Corrigido fatorando o desenho da caixa em `_draw_caixa_livre` (nova função) e chamando também de dentro de `_render_overlay_rgba`; a construção do fundo cacheado (`ctx['_bg_arr']`, sempre com `skip_overlay=True`) passou a pular a caixa explicitamente (`if not skip_overlay:`), já que ela agora é sempre redesenhada por frame via o overlay — sem isso ela ficaria congelada no alpha do frame em que o fundo foi cacheado, ou desenhada em dobro.
- **Caixa de texto livre esmaecendo (fade-in) durante a animação do jato/ícones de pressão, mesmo quando isso não fazia sentido pro conteúdo (ex.: rótulo fixo tipo "Extreme heat in Western Europe").** Nova flag interna `caixa_fixa` (`script_id == 's42'`, qualquer saída — MP4, GIF ou PNG): quando ativa, a caixa usa sempre `alpha_max` (sem rampa), independente da fase do clipe. Sem essa flag, mantém o fade-in original (`GLOBO_3D_CAIXA_LIVRE_INICIO`/`FADE`). Diferente de `_fade_cauda_on` (que só vale no MP4 com cauda, pois jato/ícones não precisam de fade-in no GIF/PNG — já aparecem prontos), a caixa precisa ficar fixa em QUALQUER saída quando o master está ligado, já que ela não faz parte da animação do jato/ícones. Desacoplada de `GLOBO_3D_FADE_CAUDA` (antes exigia as duas condições): a caixa fica fixa incondicionalmente pro s42, independente do jato/ícones terem ou não fade-in configurado.

- **s43 (mapa 2D): corrente de jato cortada no meridiano de Greenwich quando a região cruza lon 0° (ex.: "europa", -30° a 60°).** `_build_frame_2d` desenhava o jato DIRETO nos eixos reais via `_draw_jet_flat` (que chama `ax.plot`/`ax.text` sem `transform=`) — a grade global vem em 0..360°, mas os eixos usam a janela -30..60°, então sem `transform` o matplotlib cortava tudo que caía fora dessa faixa numérica (mesmo estando visível fisicamente). Corrigido renderizando o jato num raster plano à parte (`_jato_raster`, mesmo mecanismo já usado pelo globo) e colando via `ax.imshow(..., transform=ccrs.PlateCarree(), extent=[...])`. Ver nota permanente em `requerimento_video.md` (seção "Cuidados conhecidos") pra não reintroduzir esse tipo de erro.

- **Caixa "The Weather Channel": data em intervalo do MESMO mês incluía o ano (`05–10/07/2026`), mas o docstring de `_fmt_data_br` já documentava o formato certo sem ano (`'20–24/07'`).** Corrigido `_fmt_data_br` (`app/src/uteis/globo_3d_anim.py`) para bater com o que já estava documentado — intervalo no mesmo mês/ano agora sai `DD–DD/MM`, sem o ano.

### Adicionado

- **`GLOBO_3D_MP4_MEDIA_FIXA` (s41/s42, default `false`): o MP4 passa a usar o CAMPO MÉDIO fixo + câmera parada (mesmo campo/câmera do GIF/PNG), em vez de animar a variável dia a dia/hora sinótica.** Vira uma espécie de "GIF em formato de vídeo": só a corrente de jato/ícones/caixa de texto livre se movem, sobre o campo parado. `gerar_animacao` agora calcula a MÉDIA do período (`serie_m`) ANTES da chamada do MP4 (antes só existia para PNG/GIF) e escolhe qual série passar pro MP4 conforme a flag. Corrigido também o branch de MP4 normal em `_render_clip`: antes ignorava totalmente o parâmetro `camera` (só os branches `estatico`/`gif` respeitavam); agora, se `camera` for passado, usa esse ponto fixo em vez de `_camera_path` (necessário pro `GLOBO_3D_MP4_MEDIA_FIXA` funcionar sem voo). Combine com `GLOBO_3D_FADE_CAUDA=false` pra jato/ícones/caixa aparecerem direto, sem esmaecer (não há "antes" da cauda quando o campo já é fixo desde o frame 0).

- **`GLOBO_3D_JATO_PNG` (default `true`): permite excluir a corrente de jato SÓ da figura PNG (média estática) do s38–s42, mantendo o jato animado no GIF e no MP4.** Antes `GLOBO_3D_JATO` era tudo-ou-nada nas 3 saídas. `_render_clip` passa `skip_jet=not GLOBO_3D_JATO_PNG` pro `_build_frame` já existente (mesmo parâmetro usado internamente pelo cache de fundo) só no branch estático — GIF/MP4 não mudam.

- **Novo script `s43_globo_the_weather_channel_2D.py`: versão em mapa 2D PLANO do s42 (sem globo, sem voo de câmera).** Motor próprio `app/src/uteis/mapa_2d_anim.py`, irmão do `globo_3d_anim.py`: reaproveita 100% a camada de dados/geometria agnóstica de projeção já existente (`VARIAVEIS`, `_build_var_series`/`_build_var_series_synoptic`, `_output_plan`, `expandir_variaveis`, a geometria da corrente de jato `_draw_jet_flat`/`_jet_segments`/`_offset_polyline`, os ícones de pressão `_draw_icones_pressao`/`_load_gif_frames`, o mecanismo de overlay `_overlay_guillaume` da caixa "The Weather Channel"/crédito, `_wrap_balanceado`, `_fmt_data_br`/`_fmt_data_pentada`) — só o pipeline de RENDER é novo, bem mais simples que o do globo por não precisar de câmera, halo de atmosfera/estrelas, vinheta circular nem reprojeção esférica (os eixos `ccrs.PlateCarree` já são a projeção final, então o shaded/jato/ícones são desenhados DIRETO neles, sem raster intermediário + `imshow` de regrid). Região fixa via nova setting `GLOBO_2D_AREA` (default `"europa"`, já cadastrada em `settings.json["areas_plotagem"]`); transição entre horas sinóticas por crossfade suave (`GLOBO_2D_FRAMES_POR_PASSO`), sem interpolação de câmera (não há). Mesmas settings globais `GLOBO_3D_JATO*`/`ICONES_PRESSAO`/`CAIXA_LIVRE*`/`CREDITO`/`TITULO_THE_WEATHER_CHANNEL` compartilhadas com s38-s42 (master único "para todos os globos", agora também o mapa 2D) — sem duplicar namespace. Saídas idênticas ao s42: MP4 do período + PNG da média + GIF da média com jato animado. **Limitação v1**: a corrente de jato só é plotada quando a variável já é uma altura geopotencial absoluta própria (`z250_abs`/`z500_abs` — reusa o próprio campo como guia); para outras variáveis, loga aviso e segue sem jato (reconstrução via Z250 anomalia+climatologia fica para uma iteração futura, sem uso confirmado ainda). Sem multiprocessing/cache de fundo — render de mapa plano é leve o bastante para um loop sequencial simples. Registrado em `app/cli/run_script.py` (`RUN_S43`) e `app/settings/settings.toml` (nova seção "5i").

### Alterado

- **Corrente de jato: reaproveitamento do campo shaded como guia deixa de ser exclusivo do Z250, agora vale pra qualquer altura geopotencial absoluta (`kind in ('hgt250', 'hgt500')`).** Antes, o atalho que evita baixar Z250 à parte só disparava com `ficha['spec']['kind'] == 'hgt250'` — rodar o jato sobre `z500_abs` com `GLOBO_3D_JET_STREAM_NIVEL` numa isolinha de 500 hPa (ex.: 5760 mgp / 576 dam) fazia o jato tentar contornar esse valor dentro do campo de Z250 (~9800–11200 mgp), onde a isolinha nunca existe — nenhum segmento era desenhado. Generalizado em 2 pontos de `globo_3d_anim.py` (reuso de `vals_cyc` como `hgt_z250_abs_cyc` + a flag `_e_z250_absoluto` que pula o download extra em `gerar_animacao`).

### Adicionado

- **Nova variável absoluta: `z500_abs` (altura geopotencial 500 hPa), disponível em `GLOBO_3D_VARIAVEIS_S42`.** Espelha exatamente a arquitetura do `z250_abs` (mesma paleta de cores, MP4 em hora sinótica, sem climatologia), mas aponta pro downloader dedicado de Z500 (`downloaders_hgt500_ERA5`/`downloaders_gdas_hgt500`/`downloaders_{gfs,gefs,ecmwf,aifs}_hgt500`, já existentes no projeto — usados por outros scripts como `plot_geop500.py`) via `_era5_z500`/`_gdas_z500` (novos wrappers) e novas entradas `('<modelo>', 'hgt500')` em `_fcst_downloader`. Escala calibrada pela carta de referência (GFS 500 hPa, 468–600 dam = 4680–6000 mgp): `GLOBO_3D_VMIN/VMAX_Z500_ABS`; demais overrides seguem o mesmo padrão do z250_abs (`NIVEIS/PALETA/ALPHA/COR_OCEANO/COR_FRONTEIRAS/LW_COAST/BORDER/STATES_Z500_ABS`, todos com os mesmos defaults). Suavização gaussiana do campo shaded já funciona automaticamente via o mecanismo genérico `GLOBO_3D_SIGMA_<VAR>` (novo `GLOBO_3D_SIGMA_Z500_ABS`, default 2.0, mesmo valor do z250_abs) — não precisou de código novo. Importante: `spec['kind'] = 'hgt500'` (não `'hgt250'`) — a corrente de jato continua usando o Z250 dedicado, baixado à parte, independente da variável shaded (`_e_z250_absoluto` só é `True` quando `kind == 'hgt250'`).

- **`requerimento_video.md` (raiz): catálogo de settings + registro de pedidos de vídeo do globo 3D (s38–s42), pra parar de acumular overrides soltos no `settings.local.toml`.** Fluxo: o usuário descreve o vídeo em linguagem natural, eu traduzo pros nomes reais de setting (catálogo com script/variável, projeção e câmera de voo — por script, já que s38/s39/s40 usam `GLOBO_3D_*` e s41/s42 usam `GLOBO_3D_GE_*` —, corrente(s) de jato, ícones de pressão, caixa de texto livre e aparência geral, incluindo o que é exclusivo do s42 como `GLOBO_3D_FADE_CAUDA`/blue marble/caixa "The Weather Channel"), registro o pedido resolvido numa entrada datada na seção "Pedidos", aplico no `settings.local.toml` só durante o render e devolvo o arquivo ao estado anterior depois — o histórico de pedidos fica só neste arquivo.

### Alterado

- **Caixa de texto LIVRE (`GLOBO_3D_CAIXA_LIVRE`): quinas quadradas em vez de arredondadas, sombra esfumaçada desligável, mesma fonte da caixa "The Weather Channel".** `boxstyle` trocado de `round,pad=0.55,rounding_size=0.45` para `square,pad=0.55`. Fonte agora usa `ctx['font_twc']` (novo, resolvido 1x em `_render_clip` — `GLOBO_3D_FONTE_TWC`, default herda `GLOBO_3D_FONTE_LEGENDA`) em vez de sempre `font_legenda`, deixando as duas caixas visualmente consistentes; `_overlay_guillaume` também passou a usar esse `ctx['font_twc']` pré-resolvido em vez de resolver a fonte a cada frame. A sombra (`GLOBO_3D_CAIXA_LIVRE_SOMBRA`) já era opcional — só desliguei no seu `settings.local.toml`.

### Corrigido

- **Corrente de jato: descontinuidade das faixas finas na PONTA de uma isolinha CIRCUMPOLAR (perto do meridiano de Greenwich).** O fix anterior (desenrolar `lo` em `_jet_segments`) resolveu o salto no MEIO do segmento, mas isolinhas que fecham dando a volta no globo inteiro (`closed`, span de longitude ~360°) ainda quebravam nas PONTAS do array: `np.gradient` usa diferença de uma ponta só nos extremos (não sabe que index 0 e index -1 são vizinhos no anel), e o contourpy começa/termina o traço bem na borda da grade cíclica — que coincide com Greenwich. `_offset_polyline` ganhou um parâmetro `closed` que, quando `True`, usa diferença CENTRADA PERIÓDICA nos extremos (o vizinho "antes do 1º ponto" é o ÚLTIMO ponto do anel, e vice-versa) — `_draw_jet_stream` (tela) e `_jato_flat_overlay`/loop de faixas (drape) agora calculam e passam `closed` pra cada segmento.

- **Ícones de pressão: ficavam OVAIS (esticados na vertical) fora do equador — ex. bem visível sobre a Europa (~50°N).** O extent do ícone usava o MESMO raio em graus pros eixos de longitude e latitude (`[lon-r, lon+r, lat-r, lat+r]`), mas 1° de longitude equivale a só `cos(lat)`° de latitude em distância física na esfera — a 50°N isso é ~36% menor, então um box "quadrado" em graus vira um retângulo estreito na direção EW, aparecendo esticado NS depois de reprojetado. Perto do equador `cos(lat)≈1` mascarava o problema. Corrigido calculando um raio de longitude compensado (`r_lon = r / cos(lat_c)`, com um piso pra não explodir perto do polo) — aplicado ao ícone e à sombra.

### Adicionado

- **s42: caixa "The Weather Channel" (título azul + data BR), fonte Liberation Sans (substituta livre da Arial).** Dentro do modo `GLOBO_3D_SO_CREDITO`, se `TITULO_THE_WEATHER_CHANNEL` estiver preenchido, desenha duas caixas de quinas quadradas lado a lado no canto superior esquerdo (referência: prints do The Weather Channel): título em caixa azul `#0077a7` com texto branco, e logo à direita uma caixa cinza `#eaebed` com a data/hora do frame em texto `#36566a` — mas em **formato brasileiro** (`DD/MM/AAAA`, com dia da semana abreviado em PT-BR e hora UTC quando sinótico, ex. `Qui 25/06/2026 00Z`), diferente do formato US (`June 25, 2026`) usado no resto do overlay. Nova função `_fmt_data_br` (espelha `_fmt_data_pentada`, mas com `%d/%m/%Y`) e novo array `ctx['dates_br']` ao lado de `dates_en`/`dates_full`/`dates_wapo`. Fonte resolvida via `GLOBO_3D_FONTE_TWC` (novo, default vazio = usa `GLOBO_3D_FONTE_LEGENDA` compartilhada) — `Liberation Sans` foi copiada de `/usr/share/fonts/` pra `Entrada/fonts/` (mesmo mecanismo de `_register_fonts` já usado por Playfair Display/Libre Franklin/Ubuntu Sans); é metricamente compatível com a Arial (Arial em si é proprietária, não redistribuível). Sem `TITULO_THE_WEATHER_CHANNEL` configurado, nada muda (só o crédito, como já era).

- **s42: fundo de satélite "blue marble" no globo (`GLOBO_3D_BLUE_MARBLE`, default `false`).** Reusa `Entrada/blue_marble.png` (mesmo arquivo do s23), carregado e redimensionado (máx. 4096px) **uma única vez** (`_blue_marble_rgba`, cache em módulo — mesmo padrão de `_vignette_rgba`) e desenhado via `ax.imshow(..., zorder=0)`, embaixo de tudo. Só fica visível onde o campo shaded acima estiver semi-transparente (`shaded_alpha`/`GLOBO_3D_ALPHA_<VAR>` < 1.0 — combina direto com o knob de opacidade já adicionado). `GLOBO_3D_BLUE_MARBLE_REGRID` (default 2048) controla a nitidez da reprojeção, mesmo mecanismo do jato/ícones. Por enquanto só liga no **s42** (`script_id == 's42'` explícito, não usa o namespace `GE_` compartilhado com o s41 — pra não ligar sem querer no s41 também).

- **z250_abs: oceano com cor sólida (`GLOBO_3D_COR_OCEANO_Z250_ABS`, default `#0e426d`).** Novo par com `cor_continente` (já existia, so pra terra): `ax.add_feature(cfeature.OCEAN, facecolor=..., zorder=1)`, mesmo zorder/padrão. Combinado com o blue marble: terra mostra a textura de satélite (sem `cor_continente` setado), oceano vira cor chapada — visual "TV" quando o `shaded_alpha` deixa o fundo aparecer.

- **z250_abs: costa/fronteiras/estados pretos e mais grossos (`GLOBO_3D_COR_FRONTEIRAS_Z250_ABS`/`GLOBO_3D_LW_COAST/BORDER/STATES_Z250_ABS`).** O estilo `guillaume` (s39/s41/s42) usa cinza `#444444` por padrão quando a ficha não define `cor_fronteiras` — sobrescrito pra preto, com linhas mais grossas (costa 1.0, países 0.8, estados 0.6 — default global era 0.5/0.35/0.2) pro contraste ficar melhor contra o blue marble. `lw_coast`/`lw_border`/`lw_states` do `ctx` agora checam a ficha ANTES do default global (`ficha.get('lw_coast', settings.get(...))`) — novo mecanismo genérico, qualquer ficha pode sobrescrever sem afetar as demais.

- **Ícones de pressão animados (`Entrada/icones_pressao/*.gif`).** 12 GIFs 300×300 com fundo transparente (36 frames, loop): quatro setas anulares girando em torno de um glifo central. Baixa pressão em vermelho (letras `B`/`LOW`) e alta em azul da corrente de jato `#1787ad` (letras `A`/`HIGH`), com o sentido de giro fisicamente coerente por hemisfério (baixa: HS horário / HN anti-horário; alta: o oposto). Inclui 4 variantes com `?` central cobrindo cor × sentido para sistemas indefinidos.

- **Globo: flag `GLOBO_3D_VARIANTES_AUTO` (default `true`) para desligar as variantes automáticas.** Com `false`, `z250_anom` gera **só a versão diária** (3 saídas), sem a média móvel de 5 dias (`z250_anom_5d`) — útil quando se quer exatamente 3 arquivos por variável no s41. Variantes pedidas **explicitamente** na lista `VARIAVEIS_GLOBO_3D` seguem valendo. Implementado em `expandir_variaveis`.

- **Globo MP4/GIF: CACHE DE FUNDO — ~2× mais rápido, sem qualquer perda de qualidade nem impacto na animação do jato (`GLOBO_3D_BG_CACHE`, default `true`).** Nos frames de **fundo congelado** (todos do GIF e a cauda do MP4, onde campo e câmera não mudam — só o jato desliza) o fundo pesado (sombreado + reprojeção + costa/fronteiras + isolinhas) é renderizado **uma vez** e reusado; por frame renderiza-se **apenas a corrente de jato** (transparente, mesma projeção/enquadramento) e faz-se *alpha-composite* sobre o fundo, com a caixa de texto por cima. Como o jato usa o **mesmo `_jato_raster`/regrid/fase por frame**, a animação e a qualidade do jato ficam **idênticas** — muda só o que fica atrás dele (constante). Implementado fatorando `_build_frame` em `_draw_jet_layer` + `_render_jet_only_rgba` + `_composite_overlay_box`, com fast-path para `f >= _bg_from` e o fundo pré-computado em `ctx['_bg_arr']` (herdado pelos workers via CoW). Autoteste opcional `GLOBO_3D_BG_CACHE_CHECK` renderiza um frame congelado do jeito antigo e pelo cache e loga `max|diff|` (0 = pixel-idêntico). Ganho maior em períodos curtos (a cauda+GIF dominam) e escala bem: cauda e GIF são custo fixo.

- **Corrente de jato: seleção por HEMISFÉRIO (`GLOBO_3D_JATO_HEMISFERIO_SUL` / `GLOBO_3D_JATO_HEMISFERIO_NORTE`, default ambos `true`).** Permite plotar o jato só no HS, só no HN, ou nos dois — ao olhar o Hemisfério Sul, desligar o HN evita desenhar o jato do outro hemisfério (que apareceria no limbo/topo do globo). Implementado como filtro no `_jet_segments` (novo parâmetro `hemisferios`): mantém os pontos da isolinha do(s) hemisfério(s) habilitado(s) (HS: `lat<0`, HN: `lat>0`) e descarta o segmento se <50% cai na região permitida — em cima do filtro de `|lat|` já existente (`JATO_LAT_MIN/MAX`). `_jato_on` agora exige ≥1 hemisfério ligado; `hemisferios` entra no estilo compartilhado dos dois jatos. Vale para os 4 globos. Backward-compatible (ambos `true` = comportamento anterior).

- **Globo MP4: "cauda" final com o campo congelado e a corrente de jato ainda fluindo (`GLOBO_3D_JATO_MP4_CAUDA_SEG`, default 7s).** No MP4, depois que a animação da variável termina, acrescenta N segundos de frames onde o **campo e a isolinha-guia ficam parados no último dia** (a interpolação temporal já clampa `pos` em `n_dias-1`) e a **câmera para na posição final**, mas a **fase do jato continua avançando** (é função do índice `f` do frame) → "JET STREAM"/setas seguem **fluindo W→E** sobre o campo congelado. Implementado estendendo `total_frames` no ramo MP4 de `_render_clip` (`base_frames` do voo + `_tail = round(cauda_seg*fps)`) e repetindo a última posição da câmera na cauda. Só entra quando o jato será desenhado (`_jato_on` + Z250 disponível); `0` desliga. Vale para os 4 globos (feature compartilhada do jato); a fase é contínua na emenda (sem salto). Assume que a variável já chegou ao último dia ao fim do voo (verdade p/ `GLOBO_3D_VELOCIDADE_VAR >= 1`). Knob em `settings.toml` + `settings.local.example.toml`.

### Adicionado

- **s42: campo ABSOLUTO (`z250_abs`) com MP4 em HORA SINÓTICA (00/06/12/18Z), reanálise E previsão.** Nova ficha `z250_abs` (altura geopotencial 250 hPa absoluta, sem subtração de climatologia) com escala/paleta/opacidade configuráveis (`GLOBO_3D_VMIN/VMAX/NIVEIS/PALETA/ALPHA_Z250_ABS`, default 4800–11200 mgp / 50 bandas / rampa purple→darkred). No **MP4**, cada passo de tempo passa a ser um horário sinótico real (não mais média diária) — nova função `synoptic_scalar_on_grid` (`forecast_daily.py`, irmã de `daily_scalar_on_grid` sem o `.resample('1D').mean()`) e o par `_absolute_reanalise_series_synoptic`/`_absolute_forecast_series_synoptic` + `_build_var_series_synoptic` (emenda observado+previsão na hora sinótica, mesmo padrão de `_build_var_series`). **GIF e PNG continuam exatamente como antes** (média do período inteiro) — `gerar_animacao` constrói duas séries por variável quando `ficha['sinotico_mp4']=True`: a diária de sempre (GIF/PNG) e a sinótica nova (só o MP4), evitando um bug de rótulo de data que apareceria se a mesma série sinótica alimentasse o `_agg_estatico` do período (o cálculo de `rotulo_dias` assume "1 índice = 1 dia"). O rótulo de data do MP4 ganha a hora UTC (`_fmt_data_pentada(..., mostrar_hora=True)`, ex. "June 5, 2026 12Z") pra diferenciar os 4 passos do mesmo dia. Quando a variável shaded já é Z250 absoluto, o campo-guia da corrente de jato reaproveita o próprio campo (`vals_cyc`) em vez de baixar `z250_anom` à parte e reconstruir anomalia+climatologia — a suavização gaussiana já existente (`GLOBO_3D_SIGMA_HGT_CONTORNOS`, default 1.5) segue sendo aplicada por passo de tempo, agora nos 4× mais passos sinóticos. Nova setting `GLOBO_3D_VARIAVEIS_S42` (lista própria do s42, que plota campo fisicamente diferente do s38-s41) com fallback pra `VARIAVEIS_GLOBO_3D` compartilhada. Novo knob genérico `shaded_alpha`/`GLOBO_3D_ALPHA_<VAR>` (opacidade do campo shaded por variável — não existia antes). Guarda de segurança: pêntada móvel configurada numa ficha `sinotico_mp4` é ignorada nesse caminho (loga aviso) — `rolling(time=dias)` assume 1 passo = 1 dia, que deixa de valer em hora sinótica. Escopo desta entrega: só `z250_abs`; outras absolutas (`tmp850_abs`, `olr_abs`, `mslp_abs`...) usam o mesmo mecanismo genérico e entram quando pedidas. `script_version` do s42 bumpado pra `1.1-z250-abs-sinotico`.

### Corrigido

- **`_absolute_reanalise_series`/`_absolute_forecast_series` eram hard-coded pra vento (usadas só por `jet_stream`) e quebravam com qualquer outra variável `absoluto: True`.** Testando o `z250_abs` novo, a série DIÁRIA (usada pelo GIF/PNG) caía nessas funções — que chamavam `_daily_wind_speed_on_grid` incondicionalmente, esperando variáveis u/v de vento; num arquivo de altura geopotencial (sem u/v) isso quebraria. Generalizado: as duas funções agora usam `_daily_scalar_on_grid` com `spec['var_candidates']` quando a ficha tem esse campo (ex.: `z250_abs`), e só caem no caminho de vento quando `var_candidates` está ausente (ex.: `jet_stream`, que deriva o campo de u/v) — sem regressão pro `jet_stream`.

- **Script s42 (`s42_globo_the_weather_channel.py`): cópia fiel do s41, ponto de partida para o estilo "The Weather Channel".** Mesmo motor (`globo_3d_anim.py`), mesmas variáveis, mesma saída (MP4 + PNG médio + GIF médio) e mesma projeção Google Earth do s41 — sem diferença visual própria ainda; será editado iterativamente. Como o motor tinha 5 pontos com `if script_id == 's41'` hard-coded (namespace de override `GLOBO_3D_GE_*`, estilo `guillaume`, projeção Google Earth, enquadramento paisagem, geração do PNG/GIF médio além do MP4), todos foram estendidos para `script_id in ('s41', 's42')` — sem isso o s42 rodaria com o visual/saída do s38 (sem PNG/GIF, projeção `nearside`), não uma cópia do s41. Registrado em `SCRIPTS` (`app/cli/run_script.py`) e `RUN_S42=true` em `settings.toml`. `script_version` próprio (`1.0-copia-s41`) para evoluir independente do s41.

- **Ícones de pressão: fade-in opcional (`fade_in`/`fade_inicio`/`fade_duracao` por ícone).** Mesmo mecanismo já usado na caixa de texto livre (`GLOBO_3D_CAIXA_LIVRE_INICIO/FADE`): com `fade_in=true`, o ícone sobe de alpha 0 até o `alpha` configurado entre `fade_inicio` e `fade_inicio+fade_duracao` (fração 0–1 do clipe); default `fade_in=false` mantém o comportamento anterior (aparece direto, opaco). Implementado dentro de `_draw_icones_pressao` (não em `_build_frame`/caixa_livre) — funciona tanto no render completo quanto no fast-path do cache de fundo (cauda do MP4 + todos os frames do GIF), diferente da caixa de texto livre que só aparece no render completo. Enquanto o ícone está totalmente invisível (alpha ≤ 0.3%), pula o `imshow`/regrid — economiza render nos primeiros frames antes do fade começar.

- **Ícones de pressão: zorder sempre ABAIXO da corrente de jato quando ela está ligada.** Antes os ícones (zorder 10/11) ficavam por cima do jato (zorder até 7 em tela / 9 em drape) — se a faixa do jato passasse sobre um ícone, ficava escondida atrás dele. Agora, com o jato ligado, os ícones usam `zorder = jato_zorder_max - {1, 2}` (dinâmico: 9 em modo drape, 7 em modo tela) — sempre 1-2 abaixo do topo do jato, garantindo que a faixa nunca fique tampada. Sem jato, os ícones mantêm o zorder alto de sempre (10/11), no topo do mapa.

### Corrigido

- **Ícones de pressão: nítidos (`GLOBO_3D_ICONE_PRESSAO_REGRID`, default `2048`) e giro sem "corte/reinício".** Dois bugs no `imshow` dos ícones (introduzido junto com a feature, ainda não lançado): **(1) serrilhado** — `regrid_shape` do `imshow` reprojetado do cartopy se aplica ao **extent inteiro do eixo** (todo o hemisfério visível), não ao extent do ícone; com `regrid_shape=256` hard-coded, o globo inteiro virava uma grade de 256px e o ícone (só ~8° de um campo de ~70°) sobrava com uns ~25px reais antes de escalar pro canvas final (1080px+) → serrilhado. Corrigido subindo p/ a mesma ordem do drape do jato (`GLOBO_3D_JATO_DRAPE_REGRID=2048`), agora configurável. **(2) giro não uniforme** — `gif_f = int(f*velocidade) % len(frames)` indexava os 36 frames do GIF usando o contador de frame de SAÍDA (`f`), mas `total_frames` (definido pelo fluxo do jato, ex. 48) não é múltiplo de 36 → ao fechar o loop do GIF, a fase do ícone não voltava exatamente a 0, dando um salto visível ("corta e reinicia"). Corrigido calculando um nº de voltas completas arredondado (`n_voltas = round(velocidade*total_frames/36)`) e indexando por fração de `f/total_frames` — o giro fecha sempre num nº inteiro de voltas, sem sobra. `ctx['total_frames']`/`ctx['icone_pressao_regrid']` adicionados.

- **Globo MP4: a velocidade do jato (`..._VELOCIDADE`) quase não alterava o vídeo — agora tem o MESMO efeito por frame que no GIF.** O MP4 usava `_phase_unit = 1/fps` (velocidade em "espaçamentos por SEGUNDO") enquanto o GIF usa `1/GIF_FRAMES` (por LOOP) — com `fps=8` o fluxo do MP4 ficava ~6× mais rápido, e mudar a velocidade quase não se percebia no vídeo. Unificado: MP4 e GIF passam a compartilhar a **base de fluxo** `_flow_base = GIF_FRAMES` (`_phase_unit = 1/base` nos dois), então a velocidade avança `v/base` espaçamentos por frame **de forma idêntica** nas duas saídas. O MP4 fica mais lento e suave (controlável pela velocidade, como o GIF); para acelerar, aumente a velocidade ou reduza `GLOBO_3D_GE_GIF_FRAMES`. PNG segue parado.

- **Globo GIF: corrente de jato "reiniciava no meio" do loop com velocidade FRACIONÁRIA (ex.: `VELOCIDADE=0.5`).** O loop do GIF só era sem emenda quando a velocidade era INTEIRA: a fase avançava exatamente `velocidade` espaçamentos por loop (`base = (frac*step) % step`), então `0.5` fechava o loop no meio de um espaçamento → salto visível (fluxo parecia recomeçar do meio, sem continuidade W→E). Corrigido estendendo o loop por um multiplicador inteiro `m = _seamless_loop_multiplier([velocidades])` (menor inteiro que torna `m·v` inteiro p/ todo jato, via `Fraction.limit_denominator(6)` + LCM): `GIF_FRAMES` vira a **base** (fase = `1/base` por frame, preserva velocidade/suavidade) e o loop passa a ter `base·m` frames, cobrindo `m·v` espaçamentos **inteiros** por jato → emenda perfeita. Ex.: `v=0.5` → `m=2` → 1 espaçamento inteiro em 2× os frames = metade da velocidade, contínuo. Funciona p/ os dois jatos com velocidades diferentes (ex.: `{1, 0.5}` → `m=2`, avanços `{2, 1}`). Velocidades inteiras dão `m=1` (sem regressão). Só o GIF (o MP4 é linear, sem emenda).

- **Globo (s40/s41 estáticos): rótulos (título/legenda/subtítulo) trocados entre variáveis no render de FIGURAS.** O cache do overlay "Guillaume" (`_OV_CACHE_KEY`/`_OV_CACHE_VAL` em `_build_frame`) era indexado **só por `idx_dia`**, partindo do princípio "1 variável por processo" — válido nos workers `fork` do MP4, mas **falso no render estático (PNG/GIF)**, que desenha várias variáveis no MESMO processo com `idx_dia=0`. Resultado: a 2ª variável (ex.: `jet_stream`) reaproveitava o overlay da 1ª (ex.: `psi200_anom`) → PNG do jet saía com título "STREAMFUNCTION ANOMALY" e legenda de anomalia, apesar do campo e do jato corretos. Corrigido incluindo a variável na chave (`(variavel_key, idx_dia)`); `variavel_key` foi adicionado ao `ctx`. Afeta também o s40 (figuras diárias/pêntadas de múltiplas variáveis).

### Alterado

- **Corrente de jato: MASTER ÚNICO para os quatro globos (s38, s39, s40 e s41), plotável em QUALQUER campo.** Antes o jato era controlado só pelo s41 via `GLOBO_3D_GE_JATO*` (bloco exclusivo do "Google Earth"); o motor `globo_3d_anim.py` já é compartilhado e sempre plotou o jato sobre qualquer campo, faltava apenas a configuração para os demais globos. Agora as flags do jato (`GLOBO_3D_JATO`, `GLOBO_3D_JET_STREAM*`, `GLOBO_3D_SUBTROPICAL_JET*` e todo o estilo `GLOBO_3D_JATO_*`) são **compartilhadas** e valem para os quatro scripts. Em `_script_setting` o override por-script do s41 (`GLOBO_3D_GE_<suffix>`) passou a **excluir** os sufixos do jato (`JATO*`, `JET_STREAM*`, `SUBTROPICAL_JET*`) — o s41 mantém o override GE só para câmera/zoom/projeção. Renomeado o bloco em `settings.toml` e `settings.local.example.toml` (`GLOBO_3D_GE_JATO*` → `GLOBO_3D_JATO*`, idem `JET_STREAM`/`SUBTROPICAL_JET`). Os dois jatos independentes (JET STREAM azul + SUBTROPICAL JET verde) seguem com nível/cor/texto/velocidade próprios. `cache_params['jato']` adicionado ao s38/s39/s40; `script_version` bumpado (s38 `2.58`, s39 `3.92-guillaume`, s40 `1.1`, s41 `1.3-ge-3saidas`). **Migração:** quem tinha `GLOBO_3D_GE_JATO*` em `settings.local.toml` deve renomear para `GLOBO_3D_JATO*`. **Doc:** corrigidos comentários/docstrings do s41 que ainda diziam o design antigo ("jato nunca no MP4 / só no GIF") — o comportamento atual (mantido) desenha o jato nas **três** saídas quando ligado: **fluindo no MP4** (fase 1/fps), **parado no PNG** (fase 0) e **animado no GIF** (fase 1/total_frames).
- **s41: seta da corrente de jato agora tem a forma "enviar/telegram" (ref. `Entrada/seta.png`), não mais um triângulo.** O marcador triangular simples `(3, 0, ang)` deu lugar a um polígono com **ponta + duas asas + entalhe côncavo na traseira** (`_ARROW_VERTS`). No modo drape é desenhado como polígono preenchido em graus (`ARROW_TAM_DEG`, escala equirectangular, gira pela tangente e é drapejado); no modo tela é um `MarkerStyle` de `Path` girado pela tangente de tela. Cantos retos (a referência é levemente arredondada — nicety opcional).

### Corrigido

- **s41: "piscar" de setas/"JET STREAM" no limbo LESTE (isolinha circumpolar não dava a volta).** A isolinha de Z250 do jato é um **anel** que fecha ao redor do globo (lon 0→360, mesmo meridiano nas duas pontas). Como esse meridiano de emenda cai no **limbo leste** (câmera na América do Sul), e a sequência tratava o arco como um segmento ABERTO `[0, L]` cortando nas pontas, os elementos apareciam/sumiam aleatoriamente ali (a oeste, sem emenda, ficava OK). Agora detecta-se a isolinha **circumpolar** (lon cobre ≥ 350°) e a sequência **dá a volta** (posições módulo `L`, número inteiro de palavras dividindo o anel; letras cruzam a emenda com `s_ch % L`) → o fluxo é **contínuo na costura**, sem piscar. Aplicado nos dois modos (`_jet_flow_sequence`, `_draw_text_on_path`, overlays tela e drape ganharam `closed`).

- **s41: fluidez/ordem/legibilidade da animação da corrente de jato (setas + "JET STREAM") — reescrita p/ bater com `Entrada/JETSTREAM.png`.** Três problemas corrigidos: **(1) padrão irregular** — antes texto e setas ficavam em **duas grades independentes** (setas *suprimidas* perto do texto) → em alguns trechos só setas, em outros só texto, com buracos. Novo `_jet_flow_sequence` gera a sequência **regular** `JET STREAM → → → JET STREAM …`: uma palavra a cada `TEXTO_ESPAC` e `SETAS_ENTRE` setas **igualmente espaçadas no VÃO entre as bordas** de palavras consecutivas (setas nunca invadem a palavra — some o "encoste" no "J"), deslizando uniforme e com **loop perfeito**. **(2) setas caóticas / apontando pra trás nas curvas** — a tangente vinha de uma diferença curtíssima (0,3°); agora é **centrada e suavizada** (`_screen_tangent` e o `_tan` do modo drape usam janela ±~1–2° de arco) → setas e letras seguem o fluxo geral sem jitter. **(3) texto sumia nas curvas** — o `TEXTO_MAX_CURVA` (55°) pulava a palavra; subiu p/ **110°** (só pula em *doubleback* extremo) → o texto aparece e curva ao longo das curvas. Novo knob `GLOBO_3D_GE_JATO_SETAS_ENTRE` (default **2**, como na referência: `→ → JET STREAM → → JET STREAM`); `ARROW_ESPAC`/`TEXTO_GUARDA` viraram legado.

### Adicionado

- **s41: DOIS jatos independentes (JET STREAM azul + SUBTROPICAL JET verde), em seção dedicada no settings.** Cada jato tem flag/nível/cor/texto próprios: `GLOBO_3D_GE_JET_STREAM(_NIVEL/_COR/_TEXTO)` (azul `#1787ad`, "JET STREAM") e `GLOBO_3D_GE_SUBTROPICAL_JET(_NIVEL/_COR/_TEXTO)` (verde `#2e8b57`, "SUBTROPICAL JET"). O **texto é uma string livre** — o que se escrever aparece animado no fluxo. O resto do estilo (faixa, faixas finas, seta, drape, GIF) é **compartilhado** (`GLOBO_3D_GE_JATO_*`). `ctx['jato']` (dict único) virou `ctx['jatos']` (lista); `_jato_raster` desenha todos os jatos num único raster e faz **uma** reprojeção; `_build_frame` itera a lista. **Espaçamento texto↔setas AUTOMÁTICO**: `esp_txt = 2·(meia-largura-do-texto) + (SETAS_ENTRE+1)·SETAS_PASSO` → qualquer string (curta ou longa) nunca sobrepõe as setas. **Loop do GIF por fração** (`velocidade = 1/total_frames`, fase ∈ [0,1)): cada jato avança exatamente um espaçamento de palavra no loop → emenda perfeita para os dois jatos simultâneos, com qualquer texto. Novo knob `GLOBO_3D_GE_JATO_SETAS_PASSO`; saíram `JATO_NIVEL/COR/TEXTO/VELOCIDADE/TEXTO_ESPAC/TEXTO_GUARDA/ARROW_ESPAC` (substituídos). Sincronizado em `settings.toml`, `settings.local.example.toml` e `settings.local.toml`.

- **s41: modo "corrente de jato DRAPEJADA na superfície" (`GLOBO_3D_GE_JATO_DRAPE`, opcional).** Resolve o embaralhamento do texto/setas no limbo do globo 3D. Em vez de desenhar o jato como adesivos no plano da TELA (tamanho fixo em pontos → não sofrem foreshortening → amontoam na borda), o jato inteiro (faixa + faixas finas + "JET STREAM" + setas) é renderizado num **raster plano equirectangular (lon×lat)** e **"colado" na esfera via `imshow`** (reprojeção de raster), **exatamente como o sombreado das variáveis**. Assim ele fica **rente à superfície** e ganha a perspectiva 3D correta: comprime graciosamente rumo ao limbo em vez de embaralhar, e a curvatura vem "de graça" da reprojeção. No modo drape os tamanhos são em **graus geográficos** (`LARGURA_DEG`, `STRIPE_LARGURA_DEG`, `TEXTO_TAM_DEG`, `ARROW_TAM_DEG`); o raster é limitado à banda de latitude do jato p/ maximizar a nitidez (`DRAPE_PX`, `DRAPE_REGRID`, `DRAPE_PAD`). Novos helpers `_jato_raster` (figura plana → RGBA + extent), `_draw_jet_flat` e `_jato_flat_overlay` (desenho em coords lon/lat com escala uniforme, tangente = `atan2(dlat,dlon)`, sem projeção). **Atrás de flag** (default `false` = mantém o modo adesivo, rápido); ligado em `settings.local.toml` p/ comparação. Ressalva honesta: no limbo exato o texto ainda não é *legível* (limite físico da projeção), mas fica *correto/comprimido* em vez de embaralhado.

### Alterado

- **s41: agora gera TRÊS saídas por variável (antes: só 1 MP4).** Por variável (e por modelo, no forecast): **(1) MP4** `s41_<var>.mp4` — vídeo do período (voo da câmera + evolução dia a dia); **(2) PNG** `s41_<var>_media.png` — figura da **média** do período (com a corrente de jato **parada**, se ligada); **(3) GIF** `s41_<var>_media.gif` — a **média** do período (**campo atmosférico fixo**) com **só a corrente de jato animando** (setas + "JET STREAM" deslizando W→E). **A corrente de jato (`GLOBO_3D_GE_JATO`) aparece no PNG (estática) e no GIF (animada), NUNCA no MP4** (no MP4 o campo evolui dia a dia e o jato atrapalharia). Novo modo `gif` em `_render_clip` (campo estático de 1 passo + câmera fixa + N frames em que só a fase do jato avança → `imageio.mimsave` com `loop=0`); o gating `_jato_on` ganhou `and (gif or estatico)`. Loop **perfeito**: em `GIF_FRAMES` frames o padrão avança exatamente um `TEXTO_ESPAC` (por isso o default passou de 28→**30**, múltiplo de `ARROW_ESPAC`=6), e `JATO_VELOCIDADE` é ignorado no GIF (velocidade de reprodução = `GIF_FPS`). Otimização: variáveis sem jato geram o GIF com **1 frame** (evita renderizar dezenas de frames idênticos). Só o `s41` produz PNG+GIF; `s38`/`s39` seguem só com MP4. Novos knobs `GLOBO_3D_GE_GIF_FRAMES` (48), `GLOBO_3D_GE_GIF_FPS` (12) em `settings.toml`/`settings.local.example.toml`. Bump `script_version` s41 → `1.2-ge-3saidas`; `output_files`/`cache_params` incluem os 3 caminhos + flags do jato/GIF.

- **s41: corrente de jato REDESENHADA no padrão `Entrada/JETSTREAM.png` (mais objetiva).** O desenho estilizado antigo (faixa azul com *glow* + núcleo claro + *chevrons* triangulares deslizando) deu lugar a: **faixa central OPACA** sobre a isolinha de Z250 absoluto (`JATO_NIVEL`, 10200 mgp) + **faixas finas TRANSLÚCIDAS paralelas** acima e abaixo (`STRIPE_N`=3 por lado, estáticas), e por cima a palavra **"JET STREAM" + setas deslizando W→E** — **só o overlay é animado** (fase = frame); as faixas ficam paradas. Texto e setas **acompanham a curva do globo**: cada um é girado pela **tangente local em coordenadas de tela** (`_screen_tangent`), e as setas são suprimidas perto do texto (`TEXTO_GUARDA`) para não sobrepor. As faixas paralelas usam offset perpendicular em espaço métrico (`_offset_polyline`, lon ponderado por cos(lat)). **Knobs trocados** (`GLOBO_3D_GE_JATO_*`): saíram `COR_GLOW`, `COR_NUCLEO`, `CHEVRON_ESPAC/TAM/COR`; entraram `STRIPE_LARGURA/ALPHA/N/GAP0/GAP`, `ARROW_ESPAC/TAM/COR`, `TEXTO/TEXTO_COR/TEXTO_TAM/TEXTO_ESPAC/TEXTO_GUARDA`; `COR` default `#2f80ff`→`#1787ad`, `LARGURA` `7.0`→`16.0`, `ALPHA` `0.95`→`1.0`. Sincronizado em `settings.toml` e `settings.local.example.toml`.

- **s41: "JET STREAM" agora curva LETRA A LETRA ao longo da corrente de jato (antes: bloco rígido).** A palavra deixou de ser desenhada como um único texto girado pela tangente do centro (que "cortava" as partes curvas) e passou a ser desenhada glifo a glifo: cada letra é posicionada num ponto do arco e girada pela tangente local em tela → a palavra acompanha a curvatura da faixa. Novos helpers `_char_advances_pt` (largura de avanço de cada glifo em pontos, via `TextPath`, cacheado) e `_draw_text_on_path` (converte a largura pt→graus de arco pela escala local px/grau da projeção, distribui as letras centradas no ponto de ancoragem, e inverte ordem+gira 180° quando o fluxo aponta E→W na tela para manter a leitura). **Anti-embaralhamento** perto do limbo do globo (onde a projeção 3D comprime o arco e a curvatura na tela dispara): (a) a palavra é PULADA se a tangente gira mais que `TEXTO_MAX_CURVA` (default 55°) entre seu início e fim — melhor um vão do que letras sobrepostas, as setas preenchem; (b) as setas agora são suprimidas sobre TODA a extensão da palavra (meia-extensão devolvida por `_draw_text_on_path` + `TEXTO_GUARDA` de margem), não só perto do centro — antes a seta caía em cima das letras das pontas. Novo knob `GLOBO_3D_GE_JATO_TEXTO_MAX_CURVA`.

- **s41: projeção "Google Earth" + quadro PAISAGEM (reproduz `Entrada/exemplo_google_earth.png`).** O s41 deixou de usar o quadro quadrado 8×8in do s38/s39 (globo flutuante centralizado) e passou a renderizar, no modo `google_earth`, um **quadro paisagem** (mais largo que alto) no estilo do print de referência: o globo é **GRANDE** (preenche a largura) com o **centro do disco empurrado para baixo**, de modo que se vê a **metade de cima do disco** — América do Sul ampliada + o arco do limbo no topo/cantos (Patagônia sai pela base). O eixo do globo é um quadrado (em polegadas) maior que o quadro, e a figura recorta o excesso; a caixa do nome (topo-esq) e o subtítulo (topo-dir) caem no preto dos cantos superiores. Knobs próprios: `GLOBO_3D_GE_ASPECT` (altura/largura do quadro, default `0.62`), `GLOBO_3D_GE_GLOBO_FRAC` (diâmetro do disco como fração da **largura**, default `1.02` = preenche), `GLOBO_3D_GE_GLOBO_CY` (posição vertical do centro do disco, default `0.29` = baixo → desce o globo) e `GLOBO_3D_GE_ALTURA` (altura da câmera; default `8.000.000` → `5.000.000` m = mais perto/curvatura). Implementado via `figsize`/`globe_rect` no `ctx` (usados no `_build_frame` e na figura de overlay); demais scripts seguem no 8×8 quadrado com disco flutuante. Câmera **fixa na América do Sul** por default (`GLOBO_3D_GE_LON/LAT_INICIAL == FINAL`, lon `-58`, lat `-17`). Bump `script_version` s41 → `1.1-ge-landscape`; knobs no `cache_params`. Sincronizado em `settings.toml`, `settings.local.toml` e exemplo. Para reativar o voo, divergir os valores inicial/final da câmera.

- **s38/s39: campo `wind250_abs` renomeado para `jet_stream`.** Nome mais claro para o mesmo campo (magnitude do vento absoluto em 250 hPa = corrente de jato). Renomeadas também as settings hardcoded: `GLOBO_3D_VMIN_WIND250_ABS`→`GLOBO_3D_VMIN_JET_STREAM`, `GLOBO_3D_VMAX_WIND250_ABS`→`GLOBO_3D_VMAX_JET_STREAM`, `GLOBO_3D_ISOL_HGT_WIND250`→`GLOBO_3D_ISOL_HGT_JET_STREAM` (as por-variável tipo `GLOBO_3D_PALETA_*` seguem o novo nome automaticamente). Atualizado em `settings.toml` (mãe), `settings.local.toml` e exemplo. **Ação necessária:** quem usava `"wind250_abs"` em `VARIAVEIS_GLOBO_3D` ou as settings antigas deve trocar para `jet_stream`.

- **s38/s39: sempre regeneram o MP4 por padrão (novo `GLOBO_3D_SEMPRE_REGERAR`, default `true`).** O globo é saída de mídia iterada visualmente, e as features de aparência (box do Niño, caixa de texto livre, câmera, cores, vinheta, atmosfera…) **não entram na chave de cache** — antes, mudar uma delas dava *cache-hit* e trazia o vídeo velho, exigindo apagar o MP4 na mão. Agora, por padrão, todo run refaz o render com os settings atuais. Os **downloads seguem em cache** (só o render é refeito, rápido). Para reativar o skip por cache, `GLOBO_3D_SEMPRE_REGERAR = false`.

### Alterado

- **s38/s39/s41: caixa de texto livre agora OPACA por padrão + sombra preta esfumaçada ao redor.** O default de `GLOBO_3D_CAIXA_LIVRE_ALPHA_MAX` passou de `0.92` (levemente translúcida, deixava o globo aparecer atrás) para `1.0` (opaca). Nova flag `GLOBO_3D_CAIXA_LIVRE_SOMBRA` (default `true`): desenha um halo preto **levemente esfumaçado** ao redor da caixa (três contornos concêntricos pretos, do mais largo/transparente ao mais estreito/opaco, atrás do preenchimento opaco e da borda branca) — realça a borda branca sem cobri-la. Ambos acompanham o fade-in da caixa. Aplicado em `_build_frame` (via `path_effects.Stroke` no `bbox_patch`).

### Adicionado

- **Novo script `s41` — Globo 3D animado padrão "Google Earth".** Cópia **fiel do s39** (mesmo motor, mesmas variáveis, mesmo estilo Guillaume, mesma saída MP4 para reanálise E previsão de todos os modelos habilitados); a **única diferença é a projeção 3D**: em vez do globo flutuante geoestacionário, usa `NearsidePerspective` com a câmera mais perto (`GLOBO_3D_GE_ALTURA`, default 8.000 km) → região **ampliada e com mais curvatura** (estilo Google Earth). No modo `google_earth`, atmosfera/estrelas/vinheta são **desligadas** (assumem o disco flutuante centralizado). **Motor:** `_make_projection` ganhou `mode`/`sat_height` (projeção resolvida por script e passada via `ctx` — o s38/s39 seguem lendo `GLOBO_3D_PROJECTION`); `estilo` guillaume passou a valer para `s39` e `s41`; projeção do s41 controlada por `GLOBO_3D_PROJECTION_S41` (default `google_earth`). Saída em `Saida/s41_GLOBO_GOOGLE_EARTH/{REANALISE|FORECAST/<MODELO>}/s41_<variável>.mp4`. Registrado no CLI (`RUN_S41`) e nos settings. **Knobs próprios do s41** (`GLOBO_3D_GE_*`, todos com fallback para os do s39 — regulam só o s41 sem afetar o s38/s39): **zoom** (`GLOBO_3D_GE_ALTURA`), **corte HS/HN** (`GLOBO_3D_GE_INCLINACAO`: >0 mais Norte, <0 mais Sul) e **animação** (voo `GLOBO_3D_GE_LON/LAT_INICIAL/FINAL`, `GLOBO_3D_GE_VOLTAS_EXTRA`, `GLOBO_3D_GE_EASING`; ritmo `GLOBO_3D_GE_FRAMES_POR_DIA`, `GLOBO_3D_GE_FPS`, `GLOBO_3D_GE_VELOCIDADE_VAR`). Implementado via `_script_setting` (precedência `GLOBO_3D_GE_<x>` > `GLOBO_3D_<x>` só para o s41), usado em `_camera_path` e `_render_clip`. Mantido o **disco redondo ampliado** (decisão do usuário) — enquadramento retangular full-frame fica como opção futura.

- **Novo script `s40` — Globo 3D ESTÁTICO (figuras PNG) estilo Guillaume.** Cópia real do s39 (mesmo motor, mesmas variáveis, mesmo estilo), mas em vez de 1 MP4 por variável produz **figuras estáticas** no padrão do s34, com quatro coleções por variável e modo (reanálise/forecast): `diario/` (uma figura por dia), `media_movel/` (média móvel de `MOV_AVG_DAYS` dias, janelas deslizantes), `pentadas_fixas/` (pêntadas fixas de 5 dias — `p1`, `p2`, … contíguas a partir de `DATA_INICIAL`) e `media_total/` (média do período inteiro). **`media_movel` e `pentadas_fixas` só saem com ≥ `GLOBO_3D_ESTATICO_MIN_DIAS` dias** (default 5); com poucos dias (1, 2, 3…) são puladas **sem erro** e as pastas nem são criadas — `diario` e `media_total` saem sempre. Saída em `Saida/s40_GLOBO_ESTATICO/{REANALISE|FORECAST/<MODELO>}/<variável>/<coleção>/*.png`, câmera **fixa** em `GLOBO_3D_ESTATICO_LON/LAT` (default = `LON/LAT_FINAL` do voo). **Motor:** `_render_clip` ganhou modo `estatico` (renderiza 1 frame → PNG, câmera fixa, sem writer/pool) reaproveitando toda a construção de contexto do globo animado (nada muda no caminho MP4 do s38/s39); `_build_var_series` ganhou `aplicar_pentada=False` para o s40 obter a série diária crua e fazer as próprias agregações; nova função `gerar_figuras_estaticas`. Registrado no CLI (`RUN_S40`) e nos settings.

- **s38/s39: PNMM prevista (GEFS) nas isolinhas do campo `tmp850_mslp` no modo forecast.** Antes as isolinhas de PNMM eram **ERA5 apenas** (`_build_mslp_series`), então em `MODE="forecast"` (janela futura, sem ERA5) o `tmp850_mslp` saía **sem nenhuma linha de pressão**. Agora `_build_mslp_series` é **consciente do modo pelas datas** (espelha `_build_var_series`): passado → ERA5, futuro → GEFS, janela que cruza hoje → **emenda** das duas partes num vetor temporal contínuo. Novo downloader `downloaders_gefs_mslp.py` (`ensure_gefs_mslp_fcst_for_period`) puxa `PRMSL`/mean sea level do **mesmo `pgrb2a` 0.5°** do GEFS (membro `geavg`, média do ensemble), reusando os helpers do `downloaders_gefs_fcst200`; a previsão é o **lagged ensemble** (`_mslp_forecast_series`), recortado ao horizonte do modelo. Cada parte é resiliente (se ERA5 ou GEFS faltar, avisa e segue com a outra). **PNMM prevista wired só para o GEFS** (PRMSL não está conectado p/ GFS/ECMWF/AIFS): com outro modelo, avisa e omite as linhas. Isolinhas agora **pretas e de 3 em 3 hPa** por padrão (`GLOBO_3D_MSLP_COR="black"`, `GLOBO_3D_MSLP_INTERVALO=3.0` — antes branco/4 hPa), valendo igual para passado (ERA5) e previsão (GEFS). Bump `script_version` s38/s39.

- **s38/s39: novo campo `tmp850_cores_psi200_contornos` (anomalia de T850 em cores + Função de Corrente em 200 hPa em linhas).** Shaded = anomalia de temperatura do ar em 850 hPa (mesma paleta/escala do `tmp850_anom`: magenta→branco→carvão, −15..+15 °C, 128 bandas), com isolinhas PRETAS de anomalia de PSI200 por cima. **Ambos em média móvel de 5 dias** (`pentada_movel: 5` no shaded; o contorno reusa a série `psi200_anom`, que já traz a pêntada própria), via os mesmos mecanismos de `olr_cores_psi200_contornos`/`chi200_cores_psi200_contornos`. Isolinhas a cada 5 (×10⁶ m² s⁻¹), sem contorno branco no shaded. A paleta de T850, antes duplicada em `tmp850_anom`/`tmp850_mslp`, foi extraída para a constante `_PALETA_T850_ANOM` (reusada pelos três campos). Registrado em `settings.toml` (mãe) e `settings.local.example.toml`. Bump `script_version` s38/s39.

- **s38/s39: `z250_anom` agora gera DOIS MP4s — anomalia diária + média móvel de 5 dias.** Nova ficha `z250_anom_5d` (cópia profunda de `z250_anom` com `pentada_movel: 5`), então cada frame é a média de uma janela deslizante de 5 dias (mesma metodologia de PSI/CHI200). A **caixa cinza do s39 é idêntica à da versão diária** ("Geopotential Height Anomaly") — a média móvel já fica clara pelo intervalo de datas mostrado abaixo da caixa; só o título/tarja e o rótulo do canto superior direito marcam "5-day mean". Novo mecanismo genérico de **variantes automáticas** (`VARIANTES_AUTO` + `expandir_variaveis`): pedir `z250_anom` em `VARIAVEIS_GLOBO_3D`/`VARIAVEL_GLOBO_3D` insere `z250_anom_5d` logo depois (sem duplicar se já pedida explicitamente), produzindo `s3N_z250_anom.mp4` e `s3N_z250_anom_5d.mp4`. Aplicado no `_get_variaveis` de s38 e s39.

- **s38/s39: caixa de texto LIVRE ancorada em lat/lon com fade-in.** Nova seção de settings `GLOBO_3D_CAIXA_LIVRE*`: anotação de texto sobre uma área do globo (ponto lat/lon = centro da caixa), que **aparece esmaecendo (fade-in) um pouco antes do vídeo terminar** e segue a rotação do globo (desenhada em coords geográficas, como o rótulo do Niño 3.4). Cantos levemente arredondados (`boxstyle='round'`), contorno opcional (cor + espessura), cor de preenchimento e cor do texto configuráveis; o texto é centralizado (`ha/va/ma='center'`) e a caixa se ajusta ao texto (sem sobras). Quebra de linha automática (`_LARGURA`) ou manual (`\n`). Timing por `_INICIO` (fração do clipe onde começa o fade), `_FADE` (duração) e `_ALPHA_MAX` (opacidade final). Config lida em `_render_clip` (`ctx['caixa_livre']`) e desenhada em `_build_frame`. Documentada em `settings.toml` (mãe) e `settings.local.toml`.

- **s38/s39: novo campo `tsm_abs` (Temperatura da Superfície do Mar ABSOLUTA).** OISST 0.25° (só reanálise, como `tsm_anom`), mas SEM subtrair climatologia — SST em °C. **Levels e cores idênticos ao s30** (`SST_MEAN_LEVELS` 0..32°C passo 0.5 e as 16 cores `white→blueviolet→…→magenta→white`, em `_SST_ABS_LEVELS`/`_SST_ABS_COLORS`). Escala não-simétrica via `levels` explícito (BoundaryNorm), legenda numérica 0..32°C de 4 em 4. Novo builder `_tsm_abs_reanalise_series` (usa `sst_obs_daily` direto). Registrado em `settings.toml` (mãe), `settings.local.toml` e exemplo.

- **s38/s39: box do Niño 3.4 com VALOR DIÁRIO dinâmico na TSM absoluta.** Nova flag de ficha `nino34_valor`: quando ativa (só `tsm_abs`), `_render_clip` calcula a média diária da área do box (170°W–120°W, 5°S–5°N, ponderada por cos(lat), ignorando continente) e `_build_frame` mostra `Niño 3.4 = xx.x°C` interpolando por frame — na anomalia (`tsm_anom`) o rótulo segue só `Niño 3.4`.

- **s38/s39: nível de pressão nos títulos das caixas normalizado para `250 hPa`.** A caixa do s39 é escrita toda em maiúscula, então `250-hPa` virava `250-HPA`; novo helper `_fmt_nivel_hpa` converte `NNN-hPa`/`NNN-HPA` (com/sem hífen, qualquer caixa) → `NNN hPa` nos títulos do s38 (tarja) e s39 (caixa).

- **s38/s39: caixa do Niño 3.4 vira flag de settings `GLOBO_3D_BOX_NINO34` (aplicável a qualquer variável).** Antes a caixa (170°W–120°W, 5°S–5°N + rótulo) era fixa na ficha `tsm_anom` (`box_nino34: True`); agora o `ctx['box_nino34']` é lido de settings com precedência `GLOBO_3D_BOX_NINO34_<VAR>` > `GLOBO_3D_BOX_NINO34` (global) > `ficha['box_nino34']`. Assim dá para ligar a caixa em CHI200/OLR/qualquer campo, sem tocar código. Retrocompatível: sem a setting, vale o default da ficha (só `tsm_anom` traz a caixa); `GLOBO_3D_BOX_NINO34 = false` desliga em todas. Documentada em `settings.toml` (mãe, comentada) e `settings.local.toml`. Bump `script_version` s38/s39.

- **s38/s39: dois novos campos `chi200_cores_z250_contornos` e `olr_cores_z250_contornos`.** Espelham os campos `*_psi200_contornos`, mas com a **anomalia de altura geopotencial em 250 hPa** (`z250_anom`) como isolinhas pretas em vez do PSI200: (1) CHI200 shaded (verde→marrom, ±100 ×10⁵ m²/s) + Z250 anom em linhas; (2) OLR shaded (BrBG_r) + Z250 anom em linhas. Sem contorno branco no shaded; isolinhas de Z250 a cada 40 mgp. **Shaded E contorno ambos em pêntada móvel de 5 dias.** Como a ficha `z250_anom` não tem pêntada própria (é usada também como campo diário standalone), o motor ganhou a chave de ficha `contorno_serie_pentada`: `gerar_animacao` aplica a pêntada à série de contorno seguindo a ficha-pai (só se ela ainda não veio pentada'da — evita duplicar quando o contorno já tem pêntada própria, como o `psi200_anom`). Refatoração: núcleo da pêntada extraído em `_pentada_movel_serie(serie, dias, nome)`, reusado por `_aplicar_pentada_movel`. Registrados em `settings.toml` (mãe) e `settings.local.example.toml`.

- **s38/s39: campo `olr_cores_psi200_contornos` documentado nos settings.** A ficha (OLR shaded BrBG_r + PSI200 em isolinhas pretas, ambos em pêntada móvel de 5 dias) já existia no motor mas não constava na lista de campos disponíveis. Adicionada ao comentário de `VARIAVEIS_GLOBO_3D` em `settings.toml` (mãe) e `settings.local.example.toml`.

### Corrigido

- **s38/s39: cores nomeadas de 9 letras (ex.: `limegreen`) não são mais truncadas.** A lógica que remove o alpha de hex `#RRGGBBAA`→`#RRGGBB` cortava qualquer string de 9 caracteres, transformando `limegreen`→`limegre` e quebrando o matplotlib (`Invalid RGBA argument`). Passou a exigir `startswith('#')` (afeta `paleta_opaca` e a cor de fundo central). Exposto pelo novo `tsm_abs`, que usa cores nomeadas do s30.

- **s38/s39: contorno auxiliar (2ª variável) agora usa a GRADE PRÓPRIA, não a do shaded.** O `olr_cores_psi200_contornos` quebrava com `Length of x (721) must match number of columns in z (145)`: o `ax.contour` do PSI200 usava o `lon_cyc`/`lat` do campo principal (OLR, grade fina ~0.5° → 721 lon com cyclic), mas a série do PSI200 vem em 2.5° (145 lon) — grades incompatíveis. O `chi200_cores_psi200_contornos` não sofria porque CHI200 e PSI200 partilham a mesma grade 2.5°. Correção em `_render_clip`: guarda o lon cíclico + lat da própria série auxiliar (`contour_lon`/`contour_lat` no `ctx`, antes descartados no `add_cyclic_point`); `_build_frame` desenha o contorno nessa grade (fallback para a do principal quando ausente). Bump de `script_version` do s38/s39.

- **s38/s39: fim do OOM/estouro de RAM em clipes longos (35 dias) com 2 variáveis (shaded + contornos).** Causa raiz: o `Pool` de render (`_render_clip` em `globo_3d_anim.py`) era criado **sem `maxtasksperchild`**, então cada worker vivia o clipe inteiro (~45 frames com 3 workers). Como cada frame aloca transientes grandes que matplotlib/cartopy **não devolvem 100% ao SO** — raster 3600² do `contourf` (`_contourf_raster`), reprojeção do `ax.imshow`, e as paths de contorno da **2ª variável** (ex.: PSI200 preto sobre CHI200 shaded) — o RSS crescia monotonicamente até o WSL matar o processo (observado por volta do frame 80/136). Correção: `Pool(workers, maxtasksperchild=GLOBO_3D_MAXTASKS)` (default **20**) recicla o worker a cada N frames, devolvendo a RAM acumulada ao SO. Complementarmente, para o pico por worker: `GLOBO_3D_WORKERS` (menos workers = pico menor), `GLOBO_3D_SHADE_PX` (o raster 3600² é o maior transiente) e `GLOBO_3D_SHADE_REGRID`. Motor compartilhado (s38 e s39).

- **s38/s39: formatação de datas em inglês US (mês-dia).** Embora todos os rótulos do globo estejam em inglês, as datas seguiam o padrão BR/europeu: a data grande saía como `30 JUNE 2026` (ordem dia-mês, com dia-da-semana `TUESDAY` no s39) e o rótulo de rodada como `29/06/2026` (DD/MM numérico, ambíguo/errado em inglês). Padronizado para inglês US: data grande do s38 e s39 agora `June 30, 2026` (sem dia-da-semana, `%B %-d, %Y`); rótulo de rodada `Jun 29, 2026 · run 00Z` (`%b %-d, %Y`, mês por extenso evita a ambiguidade DD/MM vs MM/DD). Motor compartilhado (`globo_3d_anim.py`).

- **s38/s39: fim do "washout" — sombreado do globo migrado de `contourf` para `pcolormesh` bandado.** Causa raiz dos frames isolados que apareciam "lavados" (anomalias negativas sumindo, globo near-white): bug do **cartopy 0.25 + matplotlib 3.10** em que a reprojeção de certas geometrias de `contourf` levanta `TypeError`; o workaround `_safe_transform_path` devolvia path **vazio** e **descartava polígonos inteiros** em frames específicos (determinístico, dependente da geometria do contorno daquele frame) → o fundo near-white do eixo aparecia. Diagnóstico: o `campo` plotado estava rico (≈40% negativo) nos frames lavados, mas o `contourf` não pintava. Correção: o sombreado em bandas (padrão) agora é renderizado por `_contourf_raster` — um `contourf` desenhado num **raster plano** (sem projeção) e composto no globo via `ax.imshow` (reprojeção de **raster**, que não passa pelo transform de geometrias bugado). Mantém as **bandas suaves do contourf** (estilo WaPo) sem o washout. Nitidez controlada por `GLOBO_3D_SHADE_PX` (raster do contourf, default 3600) e `GLOBO_3D_SHADE_REGRID` (resolução da reprojeção do `imshow`, default 2048 — o default do cartopy é 750, que deixava o sombreado mole). `GLOBO_3D_PCOLORMESH=true` continua dando o gradiente contínuo (gouraud). Verificado no s39 (GEFS, 7 dias): bandas nítidas, negativos ricos, zero frame lavado. Vale para s38 e s39 (motor compartilhado).

- **Forecast (todos os scripts): D-1 do GEFS calculado em horário LOCAL (Brasília), não UTC.** `resolve_forecast_lead_init` (`forecast_daily.py`, usada por s16/s34/s31/s32/s38/s39) calculava o "dia anterior" da rodada com `datetime.utcnow()`. À noite no Brasil (UTC-3, quando o UTC já virou o dia seguinte), o D-1 caía na **rodada de HOJE** em vez da de ontem — e a rodada de hoje 00Z ainda está publicando a extensão de 16→35 dias (só ~22 dias prontos), encurtando o horizonte. Trocado para `datetime.now()` (convenção local do projeto, `gotchas.md`): com `GEFS_FORECAST_LEAD_DAYS=35` agora pega sempre **D-1 00Z** (ex.: hoje 29/06 → init 28/06 → alcança 02/08); com `=15` pega **hoje 00Z**. Mesma correção para o D-1 do CFS.

- **s38/s39: guarda de robustez contra frames de anomalia degenerados (dado parcial do GEFS).** Um dia de previsão baixado de forma incompleta (rodada ainda publicando) podia gerar silenciosamente um frame com a anomalia quase toda de um sinal (ex.: globo "todo amarelo" no `wnd250_zonal_anom` de 08/07, sem valores negativos). Novo `_validar_dias_degenerados` em `globo_3d_anim.py` (chamado em `_build_var_series`, cobre reanálise + forecast + emenda): para variáveis de anomalia simétrica, detecta dias degenerados por **dois critérios** — ABSOLUTO (`min(frac_neg, frac_pos) < GLOBO_3D_DIA_DEGEN_FRAC`, default 0.02, pega o "~todo de um sinal") e RELATIVO (`frac_neg`/`frac_pos < GLOBO_3D_DIA_DEGEN_REL × mediana_da_série`, default 0.5, pega o caso PARCIAL onde um dia tem bem menos de um sinal que o típico). Avisa com as datas e, por padrão (`GLOBO_3D_SKIP_DIAS_DEGENERADOS=true`), pula esses dias na animação; se todos degenerarem, aborta com erro claro. Não afeta variáveis absolutas (ex.: `wind250_abs`).

### Adicionado

- **s38/s39: novo campo `chi200_cores_psi200_contornos` no globo 3D — CHI200 shaded + PSI200 em isolinhas pretas.** Combina os dois campos derivados num só clipe: **CHI200 em cores** (mesma paleta verde→marrom e escala ±100 ×10⁵ m²/s, passo 10, do `chi200_anom`; **sem** contorno branco no shaded) e **PSI200 em isolinhas pretas** por cima — ambos em **pêntada móvel de 5 dias**. Novo mecanismo genérico de **série auxiliar plotada como isolinhas** no motor: a ficha declara `contorno_serie_var` (aqui `psi200_anom`), `contorno_serie_cor`/`_intervalo`/`_lw`; `gerar_animacao` constrói essa série pelo mesmo `_build_var_series` (herda modo reanálise/forecast e a pêntada), `_render_clip` monta `contour_cyc`/`contour_levels` (intervalo fixo, suavização opcional) e `_build_frame` desenha o `ax.contour`. Ajustável por `GLOBO_3D_CONTORNO_SERIE_INTERVALO`/`_LW`/`_SIGMA`. Paleta do CHI extraída para a constante `_PALETA_CHI200` (compartilhada com `chi200_anom`). Para ativar, inclua `"chi200_cores_psi200_contornos"` em `VARIAVEIS_GLOBO_3D`.

- **s38/s39: todos os campos do globo garantidos em reanálise E forecast (exceto `tsm_anom`).** Auditados os 12 campos: `z250_anom`, `psi200_anom`, `chi200_anom`, `chi200_cores_psi200_contornos`, `wnd250_zonal_anom`, `wnd850_zonal_anom`, `wnd850_meridional_anom`, `tmp850_anom`, `tmp850_mslp`, `olr_anom` e `wind250_abs` funcionam nos dois modos (e na emenda observado+previsão). O único que não tem previsão é `tsm_anom` (OISST é observacional — não há downloader de SST prevista no projeto): passou a ter um `forecast_fn` que **falha com mensagem clara** (em vez do erro cru "modelo sem suporte a sst"), orientando a usar `tsm_anom` só com `DATA_FINAL` passada.

- **s38/s39: novo campo `chi200_anom` (anomalia do potencial de velocidade em 200 hPa) no globo 3D — reanálise + forecast, pêntadas móveis.** Irmão do `psi200_anom`: também derivado do vento u/v 200 hPa, mas invertendo a equação de Poisson da **divergência** (reusa `_compute_divergence`/`_solve_poisson_sphere` de `plot_chi200.py`) em vez da vorticidade — CHI é linear no vento, então a anomalia é o χ do vento de anomalia. Os builders de PSI/CHI foram **generalizados** em `_uv200_derived_anom_series`/`_uv200_reanalise_series`/`_uv200_forecast_series` (parâmetro `campo` ∈ {psi, chi}), com wrappers nomeados por campo nas fichas — todo o pipeline de download u/v200 (ERA5/GDAS + lagged ensemble GFS/GEFS/ECMWF/AIFS), remoção da média de gauge e **pêntadas móveis de 5 dias** (rótulo de data como intervalo) é compartilhado. **Paleta verde→bege→marrom idêntica ao s03** (`CHI200_COLORS`): verde = χ **negativo** (divergência em altos níveis/convecção), marrom = χ **positivo** (subsidência). Escala ±60 (×10⁵ m² s⁻¹, mesma do s03) em **24 bandas → passo de 5 ×10⁵ m² s⁻¹** (`GLOBO_3D_VMAX_CHI200_ANOM`), com **linhas brancas finas** contornando o shaded (`GLOBO_3D_CONTORNO_CHI200_ANOM = true`). Para ativar, inclua `"chi200_anom"` em `VARIAVEIS_GLOBO_3D`.

- **s38/s39: novo campo `psi200_anom` (anomalia da função de corrente em 200 hPa) no globo 3D — reanálise + forecast.** Campo **derivado** do vento u/v 200 hPa: como a função de corrente é linear no vento, a anomalia de PSI é a PSI do vento de anomalia (`u−clim_u`, `v−clim_v`), invertendo a equação de Poisson da vorticidade dia a dia — mesma metodologia do s04, reusando `_compute_vorticity`/`_solve_poisson_sphere` de `plot_psi200.py`. Calculada na grade 2.5° da LTM NCEP (PSI é um campo suave/grande-escala → inversão por dia barata). Reanálise via ERA5/GDAS (`ensure_era5_uv200_for_period`/`ensure_gdas_uv200_for_period`) e **forecast** via lagged ensemble GFS/GEFS/ECMWF/AIFS (`fcst200` no `_fcst_downloader`), com janela recortada ao horizonte de cada modelo — entra automaticamente no "modo previsão" do globo (1 MP4 por modelo). Suporte: `clim_uv200_daily` e `daily_uv200_on_grid` (já existentes), wrappers `_era5_uv200`/`_gdas_uv200`, e **novo hook `forecast_fn` no `spec`** (`_build_var_series`) para campos derivados que não passam pelo leitor escalar genérico. **Cores:** usa a paleta da anomalia de Z250 do s38/s39 **invertida** (`reversed(_PALETA_ANOM_HGT250)`) — tons **frios** (azul/roxo) nos **desvios positivos** de PSI e tons **quentes** (vermelho/laranja) nos **negativos**, conforme a convenção do Hemisfério Sul pedida. **Linhas brancas finas** contornando o shaded via o mecanismo existente `GLOBO_3D_CONTORNO_PSI200_ANOM = true` (traça cada limite de banda). Escala ±50 (×10⁶ m² s⁻¹) configurável por `GLOBO_3D_VMAX_PSI200_ANOM`. **PSI é gauge-dependente** (constante aditiva fixada pela BC ψ=0 nos polos) — removo a média ponderada por área de cada dia para centrar o campo em 0 (colormap simétrico coerente + evita falso "dia degenerado"). **Pêntadas móveis:** cada frame é a **média móvel de 5 dias** [d, d+4] deslizando 1 dia, rotulada pelo dia inicial — mesmas janelas/datas das figuras do s34 (ex.: 29/06→03/07, 30/06→04/07, …). Novo `_aplicar_pentada_movel` em `_build_var_series`, configurável por `GLOBO_3D_PENTADA_<VAR>` / `GLOBO_3D_PENTADA_DIAS` (global, default 0 = diário); a `psi200_anom` já vem com 5 na ficha (`pentada_movel`). **Rótulo de data:** quando há pêntada, a data exibida no globo vira o **intervalo da janela** (`June 30 – July 4, 2026`; cruza o mês quando preciso) em vez de um dia único — via `_fmt_data_pentada` + atributo `pentada_dias` propagado à `_render_clip`. Escala ±50 com **40 bandas → passo de 2,5 ×10⁶ m² s⁻¹** (`niveis`). Para ativar, inclua `"psi200_anom"` em `VARIAVEIS_GLOBO_3D`.

- **s38/s39: novo campo `tsm_anom` (anomalia de TSM) no globo 3D — só reanálise por enquanto.** Anomalia de temperatura da superfície do mar a partir do OISSTv2 0.25° observado menos a climatologia diária OISST (LTM 1991-2020, arquivo `FILE_CLIMATOLOGIA_SST`). Novo módulo `app/src/uteis/clim_diaria_sst.py` (`sst_obs_daily` baixa `sst.day.mean.{ano}` do PSL; `clim_sst_daily_for_anim` lê o LTM por dia-do-ano); builder `_tsm_reanalise_series` (espelha o do OLR via `reanalise_fn`, sem suporte a forecast). Grade subamostrada p/ ~0.5° (`GLOBO_3D_SST_COARSEN`, default 2) p/ controlar RAM. Escala fixa **±6.4 °C, 32 bandas de 0.4 °C**; paleta de 32 cores **amostradas pixel a pixel** da colorbar da referência sigmameteorologia (1 cor por banda; magenta→roxo→indigo→azul→verde no frio; amarelo→laranja→vermelho→vinho→marrom→bege→rosa no quente), com as **2 bandas centrais (−0.4 a +0.4 °C) em branco** como divisor neutro frio/quente (o branco era o "gap" do 0 na colorbar original). Como `len(cmap_colors)==niveis`, o motor passou a usar **`ListedColormap`+`BoundaryNorm`** (cada banda = cor exata, sem o blend do `from_list`); demais variáveis seguem com `LinearSegmentedColormap`. `GLOBO_3D_VMAX_TSM_ANOM` (default 6.4) ajusta a escala. **Legenda numérica** (flag `legenda_numerica` na ficha): no s38 e s39 a `tsm_anom` mostra valores em °C de 0,5 em 0,5 (sem os rótulos "above/below average") — s39 vira ticks numéricos sob a barra de gradiente; s38 troca os 4 swatches por uma barra contínua com ticks. Passo configurável via `GLOBO_3D_LEGENDA_NUM_STEP` (default 0.5). **Caixa do Niño 3.4** (flag `box_nino34` na ficha): desenha o retângulo 170°W–120°W / 5°S–5°N no globo (coords geográficas → segue a curvatura/perspectiva, só contorno preto) com o rótulo "Niño 3.4" em branco/negrito e contorno preto, **curvado** caractere a caractere ao longo da latitude (cada um rotacionado pelo tangente local da projeção). **Níveis explícitos**: o motor passou a aceitar `ficha['levels']` (grade não-uniforme + `BoundaryNorm`) — usado pela `tsm_anom` para herdar a escala do s11. Paleta da sigma preservada em standby na chave `cmap_colors_sigma`.

- **s16: novo campo `z250_anom` no modo forecast — anomalia de altura geopotencial 250 hPa (sombreado) + isolinhas da altura geopotencial média 250 hPa.** Somado aos campos existentes do s16, só no modo forecast e só em modelos com hgt250 (pulado em reanálise e no CFS). Shaded com a paleta de anomalia do projeto, escala ±200 m geopotenciais (`LEVELS_Z250_ANOM`); isolinhas da média em cinza (`#333333`), intervalo de 120 m (`Z250_MEAN_CONTOUR_INTERVAL`), com rótulos em placa branca. Reaproveita os campos já calculados em `_window_fields` (média `da_z250` e climatologia `zc25`), expondo a anomalia em `da_z250_anom` sem download nem cômputo extra. Gera figuras para janelas móveis E pentadas fixas (entra automaticamente no loop de tipos da Etapa 5/6). `script_version` 3.4 → 3.5.

- **s38/s39: três novos campos de anomalia de vento no globo 3D.** `wnd250_zonal_anom` (anomalia de vento zonal 250 hPa), `wnd850_zonal_anom` (zonal 850 hPa) e `wnd850_meridional_anom` (meridional 850 hPa) — cada um é uma variável separada que pode entrar em `VARIAVEIS_GLOBO_3D`. Todos usam a **mesma paleta da anomalia de hgt250** (extraída para a constante `_PALETA_ANOM_HGT250`, reutilizada por `z250_anom` e pelos três). Reanálise (ERA5/GDAS) + forecast (GFS/GEFS/ECMWF/AIFS). Suporte: `clim_u250_daily` (nova, 3-tupla, em `clim_diaria_uv200_ltm.py`), `clim_u850_daily`/`clim_v850_daily` (já existentes); wrappers `_era5_uv850`/`_gdas_uv850` e `kind='uv850'` no `_fcst_downloader`; constantes `U_VARS`/`V_VARS`. Escalas configuráveis `GLOBO_3D_VMAX_WND250_ZONAL_ANOM` (20), `GLOBO_3D_VMAX_WND850_ZONAL_ANOM` (10), `GLOBO_3D_VMAX_WND850_MERIDIONAL_ANOM` (8).

- **s16: novo campo `olr_psi200_div` (só no modo forecast).** Vento divergente 200 hPa anômalo **apenas onde χ<0** (regiões de divergência em altos níveis/convecção, mesma máscara dos modos full/mag) + **anomalia de OLR** (shaded `BrBG_r` ±40 W/m²) + **anomalia de PSI 200 hPa em linhas de corrente** (`streamplot` do vento rotacional anômalo a 200 hPa, novo `psi200` no dict de campos). Forecast-only + só modelos com OLR (igual ao `olr_wnd850`, via `_TIPOS_OLR_FORECAST`). Quiver divergente extraído para o helper `_draw_div_quiver` (reusado por full/pos/mag e olr_psi200_div). `script_version` do s16 → `3.4`.

- **s16: novo campo `olr_wnd850` (só no modo forecast).** Anomalia de OLR (shaded, paleta diverging `BrBG_r` ±40 W/m²: verde=convecção, marrom=inibição) + **anomalia do vento 850 hPa em linhas de corrente** (`streamplot`). Gerado apenas em `MODE="forecast"` e só em modelos com OLR (AIFS/AIGFS/AIGEFS ficam sem). `script_version` do s16 → `3.3`. `_render_window` passou a receber a lista `tipos` já filtrada (em vez de iterar `_TIPOS`), garantindo que `olr_wnd850` não seja gerado na reanálise e que `waf`/`wind_zonal_psi_waf` sejam pulados sem Z250.

- **s16: modo previsão (forecast) + médias móveis + pêntadas, espelhando o s34.** O `s16` (anomalia de vento zonal 250 hPa + vento divergente 200 hPa + magnitude do jato + WAF de Rossby 250 hPa) agora roda em `MODE="forecast"` para CADA modelo habilitado pelas flags `RUN_GFS`/`RUN_GEFS`/`RUN_ECMWF`/`RUN_ECMWF_ENS`/`RUN_AIFS`/`RUN_AIFS_ENS`/`RUN_AIGFS`/`RUN_AIGEFS`/`RUN_CFS`. Em ambos os modos os 5 mapas (full/pos/nodiv/mag/waf) são calculados por **janela móvel** (`MOV_AVG_DAYS`, default 5d); a reanálise adiciona o mapa de **média do período total** (`media_total`) e o forecast adiciona **pêntadas fixas** (5d a partir do dia seguinte à rodada). Anomalias passam a usar **climatologia diária NCEP** (`clim_uv250_daily`/`clim_uv200_daily`/`clim_hgt250_daily` + CPC OLR) em vez da climatologia PSL MM-DD dos helpers `plot_*` (que faziam um período único). Saída reorganizada por **modelo** e **tipo de plotagem**: `Saida/s16_WND250_ZONAL_ANOM/{REANALISE,FORECAST/<CONTINENTE>/<MODELO>}/<tipo>/<area>/`. Cache por modelo (chave `s16_<modelo>`). `script_version` do s16 → `3.0`. CFS (sem Z250) pula os contornos do `mag` e o modo `waf`. Reaproveita os downloaders de forecast e a infraestrutura de janelas/clima já criados para o s34.

- **`app/src/uteis/forecast_models.py` (novo): peças compartilhadas do modo forecast.** Extraídas do s34 (flags `MODEL_FLAGS`/`MODEL_CONTINENT`, `enabled_models`, janelas móveis `windows`, pêntadas `pentad_windows`, `get_data_sources`, constantes `DEFAULT_SYNOPTIC_HOURS`/`ERA5_LATENCY_DAYS`/`N_PENTADAS_FIXAS`/`PENTADA_DIAS`/`MIN_DIAS_PENTADA`) para reúso pelo s16. O s34 passa a importar dessas definições (sem mudança de comportamento). Cada script mantém seu próprio dicionário de downloaders (o s34 baixa 8 campos; o s16, 4).

- **`plot_rossby_waf.waf_from_means(...)` (novo helper reutilizável).** Calcula o WAF (Takaya-Nakamura 2001) a partir de médias já calculadas, parametrizado pelo nível (`pressure`, default 250 hPa). Usado pelo s16 no cálculo por janela.

- **s16: novo campo `wind_zonal_psi_waf` + vento divergente ainda mais denso.** (1) Tipo novo `wind_zonal_psi_waf`: anomalia de vento zonal 250 hPa (shaded) + **streamlines da anomalia do vento rotacional/PSI 250 hPa** (`streamplot`, via `_compute_vorticity`/`_solve_poisson_sphere`/`_compute_rotational_wind` do plot_psi200) + **vetores WAF de Rossby 250 hPa** + **vetores do vento anômalo 850 hPa na faixa tropical** (config `WND850_TROPICO_QUIVER`). Depende de Z250 (pulado no CFS, como o `waf`). Total agora 8 tipos/área/janela. **Ajustes (script_version 3.1):** paleta de anomalia do vento zonal 250 hPa agora vai de **−40 a 40 m/s** (`LEVELS`/`TICKS`); título com a **1ª linha quebrada em duas** em todas as figuras (não estoura a largura); vetores tropicais de 850 hPa passaram para **|lat| ≤ 5°**, **verde escuro** (`darkgreen`) e mais finos (`width` 0.0028→0.0016), com latitude não subamostrada para preservar as poucas linhas da faixa fina. (2) Densidade do vento divergente 200 hPa aumentada: como o s16 opera na grade LTM 2.5° (não 0.25°), `QUIVER_POR_AREA`/`QUIVER_DEFAULTS` foram para `step=1` (1 vetor/2,5°, densidade máxima) em todas as áreas, exceto globo/PSA/hemisfério_sul/globo_3d em `step=2` (a cada 5°) para não saturar.

- **s16: áreas reduzidas no forecast, vetores divergentes mais densos e 2 campos novos de vento 850 hPa.** (1) No modo previsão o s16 plota apenas `enso`, `mjo`, `pacifico_leste_america_sul`, `hemisferio_sul`, `globo`, `psa` (configurável via `LST_AREAS_S16_FORECAST`); a reanálise mantém `LST_AREAS_S16`. (2) `QUIVER_POR_AREA`/`QUIVER_DEFAULTS` tiveram o `step` reduzido (~metade) em todas as áreas → vetores do vento divergente 200 hPa mais densos em todas as plotagens. (3) Dois novos tipos de mapa (shaded, paleta de anomalia ±10 m/s): `u850_anom` (anomalia de vento zonal 850 hPa) e `v850_anom` (anomalia de vento meridional 850 hPa), em reanálise e forecast — adiciona u/v 850 ao download (`ensure_*_uv850*`/ERA5/GDAS) e clima diária (`clim_u850_daily`/`clim_v850_daily`). Total agora 7 tipos/área/janela.

- **s38/s39: espessura das linhas de fronteiras configurável e reduzida.** `COASTLINE` 1.4→0.8, `BORDERS` 1.0→0.5, estados 0.7→0.3 (em pontos do pré-render 3600×1800 a 225 dpi). Configurável via `GLOBO_3D_COASTLINE_LW`, `GLOBO_3D_BORDERS_LW`, `GLOBO_3D_STATES_LW`.

- **s38/s39: `GLOBO_3D_PCOLORMESH` — alternativa `pcolormesh(shading='gouraud')` ao `contourf`.** Quando `true`, substitui `ax.contourf` por `ax.pcolormesh(shading='gouraud', norm=norm_fn)` em `_build_frame`. Elimina o marching-squares (50 passagens sobre 261k pontos) e projeta apenas os vértices da grade; estimativa 3-8x mais rápido para o campo meteorológico. Visual: gradiente contínuo dentro de cada célula 0,5° vs 50 bandas finas do `contourf`. Default `false` (comportamento atual preservado).

- **s38/s39: cap de workers elevado de 4 → 8 no auto-detect.** O limite de 4 foi posto para evitar OOM causado pelo `initargs=(ctx,)` que pickleava 150 MB por worker via IPC pipe. Com a migração para `_FRAME_CTX` global herdado via fork+CoW, cada worker aloca apenas ~20 MB de estado local por frame → 8 workers ≈ 160 MB adicionais, seguro no WSL. Usuários com mais de 8 núcleos podem forçar via `GLOBO_3D_WORKERS = N`.

- **s38/s39: pré-aquece shapefiles cartopy, vinheta e atmosfera no pai — workers herdam via CoW.** Em `_render_clip`, antes do fork: itera `cfeature.COASTLINE/BORDERS.with_scale('50m').geometries()` para popular o cache interno do `cartopy.io.shapereader`; chama `_vignette_rgba()`, `_atmosphere_glow_rgba(px, px, ...)` e `_starfield_rgba(px, px, ...)` com os parâmetros definitivos do clipe. Workers herdam `_VIGNETTE_CACHE`, `_ATMOSPHERE_CACHE` e `_STARFIELD_CACHE` já populados via CoW e não re-computam no 1º frame.

- **s38/s39: divisas estaduais pré-carregadas no pai — elimina 4 cargas redundantes nos workers.** `_state_line_geoms()` chamado em `_render_clip` antes do fork; workers herdam `_STATE_GEOMS_CACHE` via CoW. Antes: cada worker carregava 189 feições independentemente (~2s cada = ~8s desperdiçados por clipe). Agora: 1 carga única no pai.

- **s38/s39: revertido `imshow` → `contourf` e fronteiras vetoriais — qualidade original restaurada.** A otimização `contourf` → `ax.imshow` (RGBA pré-colorizado) e fronteiras pré-renderizadas como raster 3600×1800 introduziram pixelação visível nos campos meteorológicos e perda de resolução nas linhas de costa/países. Revertido: `ax.contourf` com marching squares (curvas suaves) e `ax.add_feature(COASTLINE/BORDERS)` + `add_geometries(estados)` vetoriais por frame. Mantida a correção de RAM (cache de overlay com globals `_OV_CACHE_KEY`/`_OV_CACHE_VAL`) e a leitura de linewidths via settings (`GLOBO_3D_COASTLINE_LW=0.5`, `GLOBO_3D_BORDERS_LW=0.35`, `GLOBO_3D_STATES_LW=0.2`).

- **s38/s39: elimina risco vertical na linha de data (antimeridiano 180°).** O `_ax_eq` (PlateCarree usado para pré-renderizar fronteiras) desenhava um contorno retangular em ±180°/±90° (spine do matplotlib). Esse contorno entrava no `borders_eq` e reaparecia como risco vertical no globo ao ser reprojetado via `ax.imshow`. Corrigido com `_ax_eq.spines['geo'].set_linewidth(0)` — mesmo padrão já usado no globo principal (`ax.spines['geo'].set_linewidth(0)` para estilo Guillaume).

- **s38/s39: isolinhas Z250 suavizadas — elimina serrilhado nas linhas de geopotencial.** `ax.contour` sobre grade 0,5° produzia degraus visíveis (staircase/aliasing) nas isolinhas brancas e coloridas (jet stream 10680/10200/10080 mgp). Correção: `gaussian_filter` aplicado em `hgt_abs_cyc` e `hgt_z250_abs_cyc` em `_render_clip` antes de armazenar no ctx — mesmo padrão de `GLOBO_3D_MSLP_SIGMA = 2.0`. Configurável via `GLOBO_3D_SIGMA_HGT_CONTORNOS` (default `1.5`; `0` desliga).

- **s38/s39: fix OOM — cache de overlay Guillaume ilimitado substituído por cache de última entrada.** O cache introduzido no commit anterior usava `ctx.setdefault('_overlay_cache_g', {})`, acumulando até N_dias overlays (18,7 MB cada, float32 1080×1080×4) dentro de cada worker via CoW. Com 20 dias e 4 workers: 20 × 18,7 MB × 4 = ~1,5 GB só de cache, causando crash no WSL. Substituído por dois globals de módulo `_OV_CACHE_KEY`/`_OV_CACHE_VAL` (padrão do projeto, igual a `_VIGNETTE_CACHE`), limitando o cache a **1 overlay por worker** (~19 MB total vs ~1,5 GB antes).

- **s38/s39: fronteiras pré-renderizadas como raster equiretangular (COASTLINE + BORDERS + estados).** `ax.add_feature(COASTLINE)`, `ax.add_feature(BORDERS)` e `ax.add_geometries(estados)` reprojetavam geometrias vetoriais (~24k vértices) para NearsidePerspective em **todo frame**. Substituído por: renderizar os três layers 1× como RGBA 3600×1800 em PlateCarree e armazenar em `ctx['borders_eq']`; em `_build_frame`, um único `ax.imshow(borders_eq, transform=PlateCarree, origin='upper')` faz apenas o warp raster (bilinear, O(pixels)). Workers herdam via CoW sem cópia extra. Speedup proporcional ao número de frames. s38 `2.38`, s39 `3.72-guillaume`.

- **s38/s39: `contourf` → `imshow` com RGBA pré-colorizada + cache overlay Guillaume por dia.** `ax.contourf()` executava o algoritmo marching-squares (O(niveis × pontos) = 13M ops) E reprojetava 261K pontos para NearsidePerspective todo frame. Substituído por `ax.imshow(cmap(norm(campo)))`: cartopy faz apenas o warp raster (PlateCarree → NearsidePerspective), ~3-5× mais rápido. Colormap/norm movidos para `ctx` (construídos 1× no pai, herdados por todos os workers via CoW). Overlay Guillaume (segunda figura matplotlib) cacheado por `idx_dia` dentro de cada worker: com `frames_por_dia=10`, 9 em cada 10 frames reutilizam o overlay renderizado. Resultado visual: gradiente contínuo (vs bandas discretas do contourf) — imperceptível em 0.5°/50 níveis. s38 `2.37`, s39 `3.71-guillaume`.

- **s38/s39: otimização de RAM — fork herda `_FRAME_CTX` via CoW, sem pickle dos arrays.** Causa raiz do OOM no WSL: o Pool passava `ctx` (~150 MB de arrays float32) via `initargs=(ctx,)`, que são pickle'd e enviados via pipe para cada worker — N workers × 150 MB = GBs de cópias desnecessárias. Solução: `_FRAME_CTX = ctx` setado no pai **antes** do `get_context('fork').Pool(workers)`, sem `initializer`/`initargs`. Filhos herdam via Copy-on-Write sem duplicar memória. Cap padrão de workers alterado de `os.cpu_count()` para `min(os.cpu_count(), 4)` (configurável via `GLOBO_3D_WORKERS`).

- **s39: `olr_anom` — omite período de climatologia no canto superior direito.** Flag `sem_clim_ref: True` adicionada à ficha; `_render_clip` define `clim_ref = ''` quando a flag está presente. Aplica-se a ambos s38 e s39 (OLR usa fonte CPC/PSL, não ERA5). s38 `2.36`, s39 `3.70-guillaume`.

- **s38/s39: `olr_anom` — escala ajustada para ±60 W/m².** `GLOBO_3D_VMAX_OLR_ANOM` 70.0 → 60.0. s38 `2.35`, s39 `3.69-guillaume`.

- **s38/s39: `olr_anom` — escala ajustada para ±70 W/m².** `GLOBO_3D_VMAX_OLR_ANOM` 80.0 → 70.0. s38 `2.34`, s39 `3.68-guillaume`.

- **s38/s39: `olr_anom` — suavização gaussiana configurável (`GLOBO_3D_SIGMA_OLR_ANOM`).** Mecanismo genérico de sigma por variável adicionado em `_render_clip`: lê `GLOBO_3D_SIGMA_<VAR>` e aplica `gaussian_filter(mode=['reflect','wrap'])` antes do `add_cyclic_point`, evitando seam. Default `1.5` para `olr_anom` (grade 2.5°, ~3.75° de suavização efetiva). Qualquer variável pode usar o mesmo mecanismo. s38 `2.33`, s39 `3.67-guillaume`.

- **s38/s39: `olr_anom` — escala ampliada para ±80 W/m².** `GLOBO_3D_VMAX_OLR_ANOM` alterado de 70.0 → 80.0 em `settings.toml` e `settings.local.toml`. s38 `2.32`, s39 `3.66-guillaume`.

- **s38/s39: `olr_anom` — paleta BrBG_r (igual ao s05) e grade nativa 2.5°.** `cmap_colors` da ficha `olr_anom` substituída por 21 cores do `BrBG_r` matplotlib (azul-esverdeado → branco → marrom, como no s05_olr_anom). `niveis` reduzido de 50 para 20, compatível com `np.arange(-40, 44, 4)` do s05. `_olr_reanalise_series` reescrita para usar grade nativa 2.5° (`_open_olr()` diretamente) em vez de interpolar para 0.5° — elimina a suavização excessiva causada pelo bilinear 2.5→0.5 e resolve o seam em Greenwich (faixa branca em 357.5–360°) sem precisar de cyclic point extra no código: `_render_clip` já faz `add_cyclic_point` que fecha o ciclo. s38 `2.30`, s39 `3.64-guillaume`.

- **s38/s39: nova variável `olr_anom` — Anomalia de OLR.** Campo de Radiação de Onda Longa Emergente (CPC Blended OLR/PSL) para reanalise; GFS, GEFS e ECMWF para previsão. Reanalise usa `olr_obs_daily` diretamente (sem ERA5/GDAS); climatologia via `clim_olr_daily_for_anim` (nova função em `clim_diaria_olr.py` com assinatura `(dates) -> (arr, lat, lon)` compatível com `_anom_from_clim`). `_build_var_series` suporta chave `reanalise_fn` no spec para variáveis sem ERA5. `_fcst_downloader` expandido com `('gfs','olr')`, `('gefs','olr')`, `('ecmwf','olr')`. Paleta divergente azul→branco→marrom (±40 W/m², configurável via `GLOBO_3D_VMAX_OLR_ANOM`). s38 `2.29`, s39 `3.63-guillaume`.

- **fix: MSLP não plotava no `tmp850_mslp` — downloader regional substituído por global.** O `ensure_era5_mslp_u100_v100_for_period` usa `area=[10,-80,-40,15]` (bounding box Brasil/SA), causando "coordinate must be equally spaced" no `add_cyclic_point` ao tentar criar MSLP global. Solução: novo `download_era5_mslp_global_hourly` + `ensure_era5_mslp_global_for_period` em `downloaders_wind100m_ERA5.py` (grade 1°, sem area, só `msl`); `_era5_mslp` em `globo_3d_anim.py` atualizado para usar o novo endpoint. Arquivos em `dados/ERA5_VENTO_PRESSAO/mslp_global_hourly/`. s38 `2.28`, s39 `3.62-guillaume`.

- **fix: `add_cyclic_point` falhava com "coordinate must be equally spaced" nos arquivos MSLP ERA5.** `lon % 360` introduz imprecisões de ponto flutuante. Corrigido em `daily_mslp_on_grid` e `daily_wind_speed_on_grid` (`forecast_daily.py`) arredondando a coordenada lon para 6 casas decimais antes do `_add_cyclic_point`. s38 `2.27`, s39 `3.61-guillaume`.

- **s38/s39: nova variável `tmp850_mslp` — T850 anomalia + PNMM isolinhas.** Quarto MP4 gerado por ambos os scripts. Mesma paleta e ficha de `tmp850_anom` (incluindo isoterma 0°C), com isolinhas de pressão ao nível médio do mar sobrepostas. `daily_mslp_on_grid` adicionada a `forecast_daily.py`; `_era5_mslp` e `_build_mslp_series` em `globo_3d_anim.py`. `_render_clip` agora aceita `mslp_serie` opcional: alinhat com a série de anomalia, ponto cíclico, pré-calcula níveis com `GLOBO_3D_MSLP_INTERVALO` (default 4 hPa); `_build_frame` desenha o contour interpolando i0/i1/w. Settings: `GLOBO_3D_MSLP_INTERVALO`, `GLOBO_3D_MSLP_COR`, `GLOBO_3D_MSLP_LW` em settings.toml e settings.local.toml. `VARIAVEIS_GLOBO_3D` atualizado. s38 `2.26`, s39 `3.60-guillaume`.

- **s38/s39: wind250_abs — correções de exibição.** Fix seam no meridiano de Greenwich (add_cyclic_point antes do interp em `daily_wind_speed_on_grid`). Continentes mais claros (`#888888→#c0c0c0`). Título s38: `"250-hPa wind speed (m/s) on..."` (unidade no título); swatches sem unidade: `['35','50','65','80+']`. s39: unidade "ms⁻¹" exibida à direita da barra de gradiente. Flag `GLOBO_3D_ISOL_HGT_WIND250` (settings) para ativar isolinhas de Z250 climatológico sobrepostas ao jet stream (independente de `GLOBO_3D_ISOL_HGT250_ABS` do z250_anom). Em `_build_frame`, campo absoluto usa apenas a clim Z250 diretamente nas isolinhas (não soma `campo + clim`). s38 `2.14`, s39 `3.48-guillaume`.

- **s38/s39: nova variável `wind250_abs` — Jet Stream (vento absoluto 250 hPa).** Terceiro plot disponível para ambos os scripts. Globo com fundo preto, continentes em cinza claro (`#888888`) revelados onde o vento < 30 ms⁻¹ (transparente via `extend='max'`), divisas em cinza escuro (`#444444`). Paleta ciano→azul→indigo→magenta (30–90 ms⁻¹). Pipeline sem subtração de climatologia: nova função `_absolute_reanalise_series`/`_absolute_forecast_series` usam `_daily_wind_speed_on_grid` (sqrt(u²+v²)) a partir dos downloaders `downloaders_wind250.py` (ERA5) e `downloaders_gdas_uv250.py` (GDAS); forecast via `gfs/gefs/ecmwf/aifs_uv250`. Ficha com `'absoluto': True` ramifica em `_build_var_series`. Título WaPo: sem a palavra "anomalies". Swatches: `['35 ms⁻¹', '50', '65', '80+']`; gradiente s39: `['30', '45', '60', '75', '90+']`. Settings `GLOBO_3D_VMIN_WIND250_ABS` (default 30) e `GLOBO_3D_VMAX_WIND250_ABS` (default 90). `daily_wind_speed_on_grid` adicionada a `forecast_daily.py`. s38 `2.13`, s39 `3.47-guillaume`.

- **s38: globo maior, atmosfera mais próxima das bordas (rect descido para dentro da tarja).** Rect WaPo alterado de `[0.01, 0.17, 0.98, 0.76]` para `[0.01, 0.10, 0.98, 0.83]`: disco agora tem r=0.415 (era 0.38, +9%), cy=0.515, disc_top=0.930. O disco desce até y=0.10 (abaixo da tarja em 0.17), mas um `Rectangle` sólido preto (zorder=15, acima do globo e da vinheta, abaixo do texto) mascara essa parte, mantendo a tarja limpa. O `bar_backup` elimina estrelas/halo da tarja. O halo atmosférico lateral agora chega a ~2.3% de cada borda (era ~6.3%). s38 `2.12`.

- **s38/s39: isolinhas de geopotencial absoluto 250 hPa mais claras e finas.** `colors='#333333'→'#666666'`, `linewidths=0.5→0.35`. s39 `3.46-guillaume`.

- **s38/s39: labels da isoterma 0°C totalmente opacos (pixel backup pós-atmosfera).** Os objetos `Text` do `ax.clabel()` são salvos em `_iso_txts`; após `fig.canvas.draw()` e antes de `plt.close()`, o bbox de pixel de cada label é calculado via `get_window_extent()` e o recorte de `arr` é armazenado em `_iso_label_backups`. Após aplicar atmosfera+estrelas, esses recortes são restaurados, garantindo que o halo azul e as estrelas não apareçam sobre as caixinhas. `alpha=1.0` na `bbox`. s38 `2.10`, s39 `3.44-guillaume`.

- **s38: título da tarja levemente subido e menor** — `y=0.125→0.136`, `fontsize=18→15` — para melhor enquadramento com os swatches do cbar.

- **s38/s39: isolinhas de geopotencial absoluto 250 hPa em cinza escuro (`GLOBO_3D_ISOL_HGT250_ABS`).** Flag `isolinha_hgt_abs: True` adicionada à ficha de `z250_anom`. Em `_render_clip`, se a flag e o setting estiverem ativos, a climatologia diária de Z250 é recalculada via `clim_hgt250_daily`, projetada na mesma grade da anomalia e armazenada como `hgt_abs_cyc` no ctx; os níveis absolutos são pré-calculados com intervalo `GLOBO_3D_ISOL_HGT250_INTERVALO` (default 60 mgp). Em `_build_frame`, `ax.contour` desenha as isolinhas em `#333333` (linewidth=0.5, zorder=3) com interpolação temporal i0/i1/w. Default: `false` (desativado). s38 `2.11`, s39 `3.45-guillaume`.

- **settings: `GLOBO_3D_ISOTERMA_0C` controla a isoterma de 0°C em s38/s39.** Flag booleana adicionada em `settings.toml` (default `true`), `settings.local.example.toml` (comentada) e `settings.local.toml` (ativa). Em `_render_clip`, a climatologia absoluta só é calculada se `ficha['isolinha_abs_0']` E `GLOBO_3D_ISOTERMA_0C = true`.

- **s38/s39: isolinha branca de temperatura absoluta 0°C em `tmp850_anom`.** Quando `ficha['isolinha_abs_0'] = True`, `_render_clip` recalcula a climatologia diária (chamando `spec['clim_fn']` novamente) e a armazena como `clim_abs_cyc` no ctx. Em `_build_frame`, o campo absoluto `campo + clim_abs[dia]` é computado por interpolação temporal (mesma ponderação i0/i1/w do shaded) e um `ax.contour` de `levels=[0.0]` branco (linewidth=1.5, zorder=6) é adicionado acima das isolinhas e costlines, com `ax.clabel(fmt='0°C')` para rotular. A isolinha fica visível em ambos s38 e s39 para `tmp850_anom`. s38 `2.8`, s39 `3.43-guillaume`.

- **s38: título da tarja em Libre Franklin bold com data dia/mês/ano.** Fonte do título mudou de `font_titulo` (Playfair Display) para `font_legenda` (Libre Franklin), mantendo `weight='bold'`. Formato da data mudou de `'%B %-d'` ("May 1") para `'%-d %B %Y'` ("1 May 2025") via chave `dates_wapo` no ctx. Texto descido de `y=0.150` para `y=0.125` para melhor distribuição na tarja. s38 `2.9`.

- **s38: tarja preta inferior estilo Ben Noll/WaPo.** O disco do globo WaPo foi ajustado para rect `[0.01, 0.17, 0.98, 0.76]` (r=0.38, cy=0.55), fazendo a borda inferior do disco coincidir exatamente com o topo da tarja (y=0.17). A técnica `bar_backup` salva os pixels da tarja ANTES da aplicação de atmosfera/estrelas e os restaura depois, garantindo uma faixa de texto limpa (sem estrelas ou halo vazando). O background preto da figura já serve como tarja; o restore preserva título, swatches e créditos baked no fig.canvas.draw().

### Corrigido

- **s38: z250_anom usa agora a mesma paleta/níveis/vmax do s39 (Guillaume).** `cmap_colors` voltou a ser a paleta Guillaume de 17 cores (roxa/cinza/rosa), `niveis=50`, `vmax=200 mgp` fixo. A lógica `_guillaume`/`_wapo` em `_render_clip` foi removida — não é mais necessária. s38 `2.6`, s39 `3.42-guillaume`.

- **s38: z250_anom restaurado aos valores originais (`LST_ANOM_CORRETA`, vmax dinâmico, 16 níveis).** Confirmado via `git diff 873c812 b71f1f9`: em todos os commits do s38, z250_anom usou `list(settings.LST_ANOM_CORRETA)` com `vmax=None` e `niveis=None` (global 16). A ficha voltou a ter `cmap_colors = list(settings.LST_ANOM_CORRETA)`, `vmax=None`, `niveis=None` como valores principais (caminho padrão usado pelo s38). A paleta Guillaume (17 cores, vmax=200, niveis=50) foi movida para chaves `_guillaume` (`cmap_colors_guillaume`, `vmax_guillaume`, `niveis_guillaume`) e `_render_clip` lê essas variantes apenas quando `script_id == 's39'`. `script_version` s38 → `2.5`.

- **s38: globo subido para mais perto do topo do frame (topo do disco fixo em y=0.93).** Rect WaPo `[0.01, 0.09, 0.98, 0.84]` → `[0.01, 0.14, 0.98, 0.79]`: bottom sobe 5% (0.09→0.14), height reduzida para manter o topo em 0.93 e não cortar o halo atmosférico (glow_w≈64px < 76px disponíveis). Centro do disco: cy 0.51→0.535; raio: 0.420→0.395. `script_version` s38 → `2.7`.

- **s38: globo maior — menos preto nas laterais e embaixo, legenda pode sobrepor a borda inferior.** Rect WaPo alterado de `[0.03, 0.22, 0.94, 0.72]` para `[0.01, 0.09, 0.98, 0.84]`: laterais passam de 3% para 1% de margem; base do disco desce de `y=0.22` para `y=0.09`, de modo que o título (y=0.15) fica dentro do disco e os swatches (y=0.08) ficam logo abaixo. Parâmetros do halo atualizados: `cy=0.51, r=0.420`. `script_version` s38 → `2.3`.

- **s38: halo da atmosfera cortado no topo do frame (rect WaPo com altura excessiva).** O rect WaPo `[0.03, 0.22, 0.94, 0.76]` posicionava o topo do disco em `y=0.98`, deixando apenas 22 px de espaço acima do globo — menos do que os ~58 px necessários para o halo atmosférico (`glow_w = 15% × R`). Reduzida a altura do rect para `0.72` (`[0.03, 0.22, 0.94, 0.72]`), criando 65 px de espaço no topo; cy e r_frac do halo atualizados para `cy=0.58, r=0.360`. `script_version` s38 → `2.2`.

- **s38: atmosfera e estrelas desalinhadas com o globo (cy/r_frac errados para o rect WaPo).** O motor `globo_3d_anim.py` calculava o centro e o raio do disco usando os defaults de Guillaume (`cy=0.49, r_frac=0.433`, para `rect [0.07, 0.06, 0.86, 0.86]`), mas o s38 usa o rect WaPo `[0.03, 0.22, 0.94, 0.76]`, cujo centro y é `0.60` e raio `0.380` — diferença de 119 px no eixo y e 13% no raio, causando o halo e as estrelas fora do disco. Corrigido: `_render_clip` calcula `_atm_cx/cy/r` a partir do rect de cada estilo e os armazena no `ctx`; `_build_frame` passa esses valores para `_atmosphere_glow_rgba`, `_starfield_rgba` e para o blur na borda; cache keys das duas funções agora incluem `cx`, `cy` (e `r_disc` na starfield) para evitar reutilização incorreta entre estilos. `script_version` bumped: s38 → `2.1`, s39 → `3.41-guillaume`.

- **s27: arquivo SST de ano passado corrompido/truncado não era detectado.** O download de OISST (`_download_sst_anos`) pulava o arquivo de anos anteriores checando apenas `Path.exists()`, então um download HTTP interrompido (ex.: `sst.day.anom.2023.nc` truncado em 5,5 MB vs ~560 MB) era reutilizado e quebrava na abertura com `NetCDF: HDF error (-101)`. Removido o atalho por `.exists()`; agora **todos** os anos passam por `arquivo_cobre_periodo`, que abre o `.nc` e valida a dim `time` — arquivo corrompido retorna `False` e força o re-download (auto-cura). Removida a variável `current_year` agora não utilizada.

### Adicionado

- **s39: layout "estilo Guillaume Jauseau" do globo animado.** O `s39_globo_midia_guillaume` usa um overlay próprio (selecionado pelo `script_id` no motor, sem afetar o s38), reproduzindo o visual do globo de anomalia do Guillaume Jauseau:
  - **Globo** grande preenchendo o quadro (sem vinheta/esfumaçado), com **contornos** de costa/países/estados em cinza-escuro (`#444444`) e **sem grade** de lat/lon.
  - **Topo-esquerdo** (enquadrado na quina, caixa cinza-clara justa ao texto via *extent*): nome da variável em inglês (branco, maiúsculo) → **data do frame** em inglês (`%A %-d %B %Y`, ex.: "THURSDAY 25 JUNE 2026"; histórica na reanálise, prevista no forecast) → **modelo + data/hora da rodada** (ex.: "ERA5 + GFS · 26/06/2026 run 00Z", via `run_init` propagado do forecast) ou a palavra **"REANALYSIS"** quando o dado é passado.
  - **Topo-direito** (alinhado à quina): subtítulo com variável + nível (ex.: "Air temperature at 850 hPa") e **período de climatologia** ("Relative to the 1991-2020 normal").
  - **Legenda**: barra de gradiente **contínua e fina** numa caixa translúcida escura arredondada no canto inferior do globo, com **5 rótulos** em inglês (`GLOBO_3D_LEGENDA5`). **Rodapé** apenas com o crédito à direita (`GLOBO_3D_CREDITO`).
  - **Paleta de `tmp850_anom`** reescrita a partir de amostragem **pixel a pixel** da barra de referência (magenta no frio extremo → azul → branco no "conforme" → vermelho → vinho → cinza-carvão no quente extremo), com **escala fixa −15 a +15 °C** e shaded suave de **128 níveis**.
  - Novos settings (só leitura, defaults sensatos): `GLOBO_3D_ROTULO_<VAR>`, `GLOBO_3D_SUBTITULO_<VAR>`, `GLOBO_3D_LEGENDA5`, `GLOBO_3D_CLIM_REF`. Log permanente da escala do shaded (`vmin..vmax` + nº de bandas) em todo render. `script_version='3.13-guillaume'`. O s38 (Ben Noll/WaPo) permanece inalterado.

- **s38/s39: renomeação e duplicação do globo 3D animado por "estilo".** O script `s38_globo_midia` foi renomeado para **`s38_globo_midia_ben_noll`** e duplicado em **`s39_globo_midia_guillaume`** (cópia idêntica, ponto de partida para divergir o estilo de cada apresentador). O motor compartilhado `globo_3d_anim.py` agora aceita `script_id` em `gerar_animacao`/`_render_clip` (default `'s38'`), de forma que o MP4 é prefixado pelo id do script (`s38_<var>.mp4` / `s39_<var>.mp4`) e as saídas vão para `Saida/s38_GLOBO_MIDIA/` e `Saida/s39_GLOBO_MIDIA/` sem colisão. Nova flag `RUN_S39` (default `true`); ambos compartilham a mesma config `GLOBO_3D_*`/`VARIAVEIS_GLOBO_3D`. Registrados no dict `SCRIPTS` e no help do CLI.

- **s31: variante dos mapas de pêntada (5 dias) do forecast com anomalia de Z + WAF de Rossby.** Além dos mapas de CHI200 intrasazonal já existentes, o s31 forecast agora gera, para cada janela de **5 dias**, um mapa adicional `chi200_hgt{nível}_waf{nível}_5day_<ini>_a_<fim>.png` em `<MODELO>/5_DAY/`: CHI200 intrasazonal (shaded) + **anomalia de altura geopotencial em isolinhas vermelhas** (tracejado=negativo) + **Fluxo de Atividade de Onda de Rossby em setas pretas** (Takaya-Nakamura 2001, via `tnflux`). Cada modelo no seu nível de jato disponível: **GEFS → 250 hPa** (tem z250); **CFS → 200 hPa** (não tem z250, mas tem z200). A anomalia de Z e o WAF são **sem filtro** (média da pêntada − climatologia diária NCEP 1991-2020), enquanto o CHI200 segue intrasazonal (filtrado). Nova climatologia `clim_uv250_daily` (escoamento básico do WAF 250). O overlay é add-on: se o z do modelo faltar, os mapas de CHI200 saem normalmente. `script_version='2.13'`.

- **s38: modo automático pelas datas (observado + previsão emendados).** Removida a chave `GLOBO_3D_MODO`. O s38 decide pelo intervalo `DATA_INICIAL`/`DATA_FINAL`: datas passadas → reanálise (ERA5/GDAS); datas futuras → previsão (1 MP4 por modelo habilitado); janela que **cruza o dia de hoje** → **emenda observado + previsão** num único vídeo contínuo (rótulo "ERA5 + <MODELO>"). Funções de série por janela (`_reanalise_series`/`_forecast_series`) concatenadas em `_build_var_series`; plano de saída em `_output_plan`. A parte de previsão inclui o dia do init (campos instantâneos), sem buraco na emenda.

- **s38: multi-variável.** O s38 agora renderiza **uma lista de variáveis** (`VARIAVEIS_GLOBO_3D`, default `["z250_anom","tmp850_anom"]`; `[]` = todas registradas) — **1 MP4 por variável** na reanálise e **1 MP4 por variável × modelo** no forecast. Builders genéricos (`_build_reanalise_var`/`_build_forecast_var`) e registro `VARIAVEIS` extensível: adicionar variável = adicionar ficha. Nova variável **`tmp850_anom`** (anomalia de temperatura do ar em 850 hPa, °C) em reanálise e forecast, com normalização K→°C automática e climatologia diária `air850.day.ltm` (NCEP 1991-2020).

- **s38: Globo 3D animado (mídia / vídeo MP4).** Novo script `s38_globo_midia` + motor genérico `app/src/uteis/globo_3d_anim.py` que renderiza uma variável meteorológica sobre um globo flutuante (projeção `NearsidePerspective`/`Orthographic`) combinando **dois movimentos** no mesmo vídeo: (1) **voo da câmera** — o ponto central viaja de uma posição inicial a uma final pelo menor arco em longitude, com `GLOBO_3D_VOLTAS_EXTRA` giros completos opcionais (ex.: do meio do Pacífico à costa do Brasil), com perfil de velocidade configurável via `GLOBO_3D_EASING` (`linear`/`ease_out`/`ease_in`/`ease_in_out` — controla a desaceleração na chegada); (2) **evolução temporal** — a série diária de anomalia avança, com interpolação entre dias (`GLOBO_3D_FRAMES_POR_DIA`). O motor é **genérico**: cada variável é uma ficha no registro `VARIAVEIS` (título, paleta e *builders*), escolhida via `VARIAVEL_GLOBO_3D`; adicionar variável = adicionar ficha, sem mexer no motor. Primeira variável: **`z250_anom`** (anomalia de altura geopotencial em 250 hPa). Shaded em **bandas discretas** (`contourf`, `GLOBO_3D_NIVEIS` níveis) calculado numa **grade fina** (`GLOBO_3D_GRID_DEG`, default 0.5°) com a climatologia NCEP 2.5° interpolada para cima (preserva a textura do dado; ponto cíclico evita faixa NaN em Greenwich). **Paleta por variável** (na ficha; override via `GLOBO_3D_PALETA_<VAR>`): `z250_anom` usa `LST_ANOM_CORRETA`; `tmp850_anom` usa um diverging azul-branco-vermelho próprio. **Fonte da legenda** configurável (`GLOBO_3D_FONTE_LEGENDA`, ex.: "Aptos Display" — `.ttf` em `Entrada/fonts/`; fallback automático se ausente). Créditos do rodapé em **maiúsculas** (fonte à esquerda em inglês: "REANALYSIS" ou modelo; autor à direita "BRUNO CAPUCIN"). **Fontes estilo WaPo** embutidas em `Entrada/fonts/` (clones OFL: **Playfair Display** ≈ Postoni no título serifado; **Libre Franklin** ≈ Franklin Gothic nos rótulos). Controles extras: **inclinação fixa do globo** (`GLOBO_3D_INCLINACAO` — >0 mostra mais o HN, <0 mais o HS) e **velocidade da variável animada** independente do voo (`GLOBO_3D_VELOCIDADE_VAR`). **Render paralelo** em N núcleos (`GLOBO_3D_WORKERS`, 0=todos — ~5× mais rápido), **resolução de saída configurável** (`GLOBO_3D_TAMANHO_PX`, default 1080 "retina") e **fonte do título** configurável (`GLOBO_3D_FONTE_TITULO`). Cada ficha tem **dois modos**: `reanalise` (ERA5/CDS + GDAS/NOMADS, por `DATA_INICIAL`/`DATA_FINAL`) e `forecast` (1 MP4 por modelo habilitado — `RUN_GFS`/`RUN_GEFS`/`RUN_ECMWF`/`RUN_AIFS` — via *lagged ensemble*, reusando `forecast_daily` e os downloaders de Z250 do s34). Anomalia **diária** em ambos os modos (climatologia diária LTM 1991-2020 `hgt250.day.ltm`). Mapa com contornos pretos: linha de costa + divisas de **todos os países** (`cfeature.BORDERS` 50m) e divisas **estaduais apenas de EUA/Brasil/Austrália** (Natural Earth admin_1 filtrado por `adm0_a3`). Layout estilo WaPo: globo no terço superior e **tarja inferior preta** com **título em inglês** (`titulo_en` + "anomalies on <Month Day>", serif bold) e **legenda categórica** de 4 swatches ("Well below/Below/Above/Well above", cores amostradas da paleta). **Vinheta** radial (`GLOBO_3D_VINHETA` — cantos escuros, centro claro). Cantos do rodapé: **fonte à esquerda** (`Reanálise` ou nome do modelo no forecast) e **crédito do autor à direita** (`GLOBO_3D_CREDITO`, default "Bruno Capucin"). Encode via `imageio` + `imageio-ffmpeg` (libx264). Saída em `Saida/s38_GLOBO_MIDIA/{REANALISE,FORECAST/<MODELO>}/`. `script_version='1.0'`. Dependências novas: `imageio`, `imageio-ffmpeg`.

- **s37: Frequência de frentes frias por localidade (heatmap, ERA5 superfície).** Novo script `s37_cold_front_track` que baixa ERA5 single-levels 6-horário (`u10/v10/t2m/msl` via `downloaders_era5_superficie`), detecta passagens de frente fria por cidade pelo método de mudança de vento (Simmonds et al., HS — giro de `v` p/ sul com `|Δv|>DV_MIN`) + guarda térmica (queda da média diária de T2m), reusando `deteccao_frente_fria`, e plota um mapa onde cada localidade é colorida pelo número de passagens no período. Também salva CSV com a contagem e as datas dos eventos. Limiares calibráveis via settings (`FRENTE_DV_MIN`/`FRENTE_DTEMP_MIN`/`FRENTE_REFRACTORY_H`). Modo único de reanálise (previsão fica como evolução futura). `script_version='1.0'`.

- **Claude Code: hook `SessionStart` no `.claude/settings.json`** — exibe mensagem de confirmação do ambiente virtual ativo (Python version + path) no início de cada sessão.



- **s16: novo modo de plotagem `_waf` — anomalia de vento zonal 250 + anomalia de Z250 + WAF de Rossby 250 hPa.** Adiciona o mapa `wnd250_zonal_anom_{area}_waf.png`: shaded = anomalia de vento zonal em 250 hPa (mesma paleta do modo `full`), contornos pretos = anomalia de altura geopotencial em 250 hPa, vetores = Fluxo de Atividade de Onda de Rossby (WAF, Takaya-Nakamura 2001) em 250 hPa. **Reaproveita 100% do que já existe**: o `rossby_waf.nc` (via `plot_rossby_waf`, já usado no s10/s04) traz tanto `hgt_anom_mean` (anomalia Z250) quanto `waf_x`/`waf_y` (WAF) — nenhum download/climatologia novo foi necessário (tudo já em 250 hPa). Quiver do WAF normalizado pelo máximo com vetores fracos mascarados (espelha o s10); o quiver de vento divergente segue nos modos `full`/`pos`/`mag`. Validado end-to-end (hemisfério sul). `script_version='2.1'`

- **s34: novas áreas de plotagem `atlantico_norte` e `europa`.**
  - `atlantico_norte` (setor Hemisfério Norte centrado no Atlântico Norte: América do Norte + Atlântico + Europa + Norte da África/Ásia ocidental). Box: lat 10°N–80°N, lon 150°W–100°E; gridlines 40°/20°.
  - `europa` (Europa + Mediterrâneo + Norte da África + Oriente Médio/Cáspio). Box: lat 20°N–72°N, lon 30°W–60°E; gridlines 20°/10°.
  - Ambas com `central_longitude_mapa=0` (não cruzam o antimeridiano), definidas em `app/settings/settings.json`, adicionadas a `LST_AREAS_S34` no `settings.toml` e com gridlines em `_configure_gridlines`. Úteis para teleconexões/bloqueios no HN.

- **s35 (AAO): heatmap de desempenho dos modelos (Inc. 3).** Novo módulo `app/src/uteis/aao_heatmap.py`: lê o arquivo de verificação e gera `aao_skill_heatmap_YYYYMMDD.png` — matriz **modelo × data-alvo** colorida pelo **MSSS (Mean Squared Skill Score) vs climatologia**: `SS = 1 − MSE_modelo / VAR_clim`, onde `MSE_modelo` é a média dos `(fcst−obs)²` numa **faixa de lead fixa** (default 5–10 d, que dá a amostra de cada célula) e `VAR_clim` é a **variância climatológica CONSTANTE** do índice (= MSE de prever sempre a climatologia=0 = `média(obs²)` no arquivo). A referência é constante (uma só para todo o painel), **não** o `obs²` do dia — usar `obs²` por dia explodia em AAO neutra (`|obs|~0`), com um erro pequeno virando SS=−1 espúrio. Colormap divergente `RdYlGn` centrado em 0 (verde = bate a climatologia, vermelho = pior que ela), SS clipado em [−1,1]. Linhas de modelos **sem célula verificável são ocultadas** (um modelo só com o init de hoje prevê datas futuras sem observado → fica de fora até acumular histórico); células sem dado em cinza. Acionado pela flag `S35_HEATMAP` (default true); é gerado **antes do `check_cache_valid`**, pois reflete o arquivo de verificação (que cresce sozinho) e deve refrescar todo dia mesmo quando o gráfico de linha está em cache. O seed (`S35_BACKFILL`) passou a **semear também o observado** numa janela longa (≈`S35_BACKFILL_DAYS`+lead), para o heatmap encher à esquerda (o arquivo guarda mais dias de obs do que os `AAO_HIST_DAYS` plotados na linha). Settings: `S35_HEATMAP`, `S35_HEATMAP_WINDOW_DAYS` (60), `S35_HEATMAP_LEAD_LO` (5), `S35_HEATMAP_LEAD_HI` (10). Validado em 60 dias: vermelho marca misses reais (swing de 09–10/05; subestimação dos picos +2.7/+3.4 de 30/05–01/06 e 19–21/06) e GEFS (média do ensemble) com skill ≥ GFS na maioria dos dias. `script_version='3.0'`

- **s35 (AAO): backfill do CFS via AWS (`noaa-cfs-pds`).** O CFS também é backfillável — o bucket `noaa-cfs-pds` tem estrutura idêntica ao NOMADS prod e retém 120+ dias. As funções do `downloaders_cfs_ensemble.py` ganharam um parâmetro `base_url` opcional (default NOMADS), threadado até `_member_urls`; com `CFS_BASE_AWS` o mesmo pseudo-ensemble (16 membros) é baixado de inits passados. `_backfill_archive` no s35 passou a um dispatch por modelo (GFS/GEFS via S3 byte-range; CFS via o downloader de ensemble apontado ao AWS). Resultado: CFS vira uma 3ª linha do heatmap com histórico completo. Validado por-init (lead 0–16).

- **s35 (AAO): backfill do histórico de previsões via AWS S3 (Inc. 2 do heatmap).** Novo módulo `app/src/uteis/backfill_hgt700_aws.py`: como o NOMADS só retém ~10 dias de rodadas, o passado (~60 dias) do `fcst_archive.csv` é semeado dos buckets **AWS Open Data** da NOAA (`noaa-gfs-bdp-pds` 0.25° e `noaa-gefs-pds` 0.5° média do ensemble `geavg`), que retêm meses. Reaproveita a técnica de **byte-range via `.idx`** do downloader CFS (baixa só o campo HGT@700) e **toda** a maquinaria dos downloaders NOMADS (`_steps_for_day`, parser GRIB, `save_netcdf`, paralelismo) — só a chamada de fetch muda; os NetCDFs de saída têm o mesmo nome/dir do NOMADS (intercambiáveis e cacheados). Orquestração no s35 (`_backfill_archive`): para cada init 00Z dos últimos `S35_BACKFILL_DAYS` ainda ausentes, baixa Z700 até `S35_BACKFILL_LEAD_DAYS`, calcula o índice AAO por-init e anexa (helper `existing_init_dates` torna o processo **idempotente**). Acionado pela flag **`S35_BACKFILL`** (default false; one-off de manutenção, roda antes do cache do gráfico). Settings novos: `S35_BACKFILL`, `S35_BACKFILL_DAYS` (60), `S35_BACKFILL_LEAD_DAYS` (16). Validado end-to-end (GFS/GEFS, idempotência confirmada). `script_version='3.0'`

- **s35 (AAO): arquivo de verificação persistente (fundação do heatmap de desempenho + correção viés/MOS).** Novo módulo `app/src/uteis/aao_verif_archive.py` que acumula, a cada rodada do s35, os escalares necessários para verificar e corrigir as previsões: `dados/s35_verif/obs_archive.csv` (índice AAO observado por data — ERA5 sobrescreve o GDAS best-effort numa re-rodada, mantendo a última ocorrência) e `dados/s35_verif/fcst_archive.csv` (índice previsto por `(modelo, init, data_alvo)`, com `lead_days = data_alvo − init`, dedup por `(modelo, init, alvo)` mantendo o registro mais recente). O `_forecast_index` passou a retornar também a lista **por-init** (`per_init`) — antes do blend do lagged ensemble, onde o lead seria ambíguo — usada pelo `append_fcst`. São só escalares (KB); o heatmap e a correção (próximos incrementos) leem deste arquivo, sem re-baixar o passado. Estratégia híbrida: o arquivo começa a acumular já e completa a janela de ~60 dias com o tempo (backfill GFS/GEFS virá no Inc. 2). `script_version='3.0'`

- **s06: novo campo OLR + PSI200.** Além dos mapas existentes de OLR + vento anômalo em 250 e 850 hPa, o script agora gera `olr_psi200_anom_{area}.png`: OLR (contourf BrBG_r) com linhas de corrente do vento rotacional (`upsi_anom_mean`/`vpsi_anom_mean` de `psi200.nc`) na mesma formatação do s04 (densidade por área, `dimgray`, `linewidth` e `arrowsize` iguais). Reutiliza `plot_psi200_data()` como Etapa 1c e carrega o NC já produzido. `script_version='2.1'`

- **s27 (ENSO): novo Hovmöller de anomalia de altura geopotencial em 250 hPa (20°S–20°N).** Mesma formatação dos demais (eixo lon 160°E–80°W, tempo invertido, logo no superior-direito), porém **sem isolinhas de vento** (só a anomalia shaded). Dado: ERA5 (`z`, geopotencial) + GDAS (`hgt`) na ponta recente, convertido para altura (gpm) por slice de tempo; anomalia vs. nova climatologia diária NCEP `clim_hgt250_daily` (1991-2020). `_plot_hov_panel` passou a aceitar `hov_contour=None` (vento opcional) e `_hov_domain_mean` ganhou faixa de latitude parametrizável. A costura do antimeridiano (~180°) é preenchida (`interpolate_na` na longitude), evitando a coluna branca da grade ERA5/GDAS. `script_version='1.15'`

- **s36 — Índice da MJO: diagrama de fase RMM1×RMM2 (Wheeler & Hendon 2004)** (`scripts/s36_mjo_index.py`). EOF combinado **próprio** (módulo novo `app/src/uteis/mjo_rmm.py`): OLR (**CPC Blended**, satélite) + **U850** + **U200** (**NCEP Reanalysis**, via OPeNDAP `ncep.reanalysis.dailyavgs`), média meridional 15°S–15°N, anomalia do dia-do-ano − média dos 120 dias anteriores, normalizada por variável e projetada nos 2 primeiros EOFs combinados → RMM1, RMM2. Fonte CPC/NCEP escolhida por consistência com as climatologias do projeto e por ser a combinação original do WH04 (OLR de satélite + ventos NCEP). Gera **um diagrama de fase** (estilo CPC/BoM): trajetória preta do **observado** (últimos `MJO_HIST_DAYS`, default 40), uma trajetória colorida por **modelo de previsão** habilitado (só os 5 físicos com OLR — GFS/GEFS/ECMWF-HRES/ECMWF-ENS/CFS; os de IA não têm OLR → pulados), cada um até seu horizonte (GEFS 35d/CFS 45d/demais ~16d), e **linha tracejada de ensemble** (média dos modelos onde há ≥2). Reusa `forecast_daily.py` (lagged ensemble, regrade diária, resolução de inits — incl. a regra **GEFS init D-1**) e os downloaders de OLR/U850/U200 (`*_olr`/`*_uv850`/`*_fcst200`) já existentes. EOF cacheado 1x em `Entrada/arquivos_nc/mjo_rmm_eof_cpc_ncep_1991_2020.nc`. Flags: `RUN_S36`, `MJO_HIST_DAYS`. Validado: variância explicada (13,3%/12,0%) e fatores de normalização (15,9/1,81/4,91) batem com o Wheeler-Hendon 2004, e a propagação observada é anti-horária (para leste). Detalhes de implementação: o **observado recente usa ERA5** (latência ~7 d) e não a reanálise NCEP R1 (atrasa ~3 meses; serve só p/ treinar o EOF); só o **vento zonal** é lido (o RMM não usa `v`, o que também viabiliza o CFS, cujo `fcst200` só traz `u`); e o **dia do init (lead 0) é descartado** (a OLR em f000 é degenerada — acumulação começa do zero — e gerava um ponto RMM espúrio). `script_version='1.2'`

- **s35 — Índice AAO (Antarctic Oscillation): observado + previsões multi-modelo** (`scripts/s35_aao_index.py`). Replica a metodologia do CPC com **EOF própria**: EOF1 das anomalias mensais de altura geopotencial em **700 hPa no HS (poleward de 20°S), ERA5 1991-2020**, com peso de área `√(cos lat)` e covariância via SVD (módulo novo `app/src/uteis/aao_eof.py`, padrão cacheado em `Entrada/arquivos_nc/aao_eof_era5_1991_2020.nc`). O índice diário projeta as anomalias diárias de Z700 no EOF1 e normaliza pelo desvio do índice mensal do período base. Gera um **gráfico de linha** (estilo CPC): observado preto (últimos `AAO_HIST_DAYS`, default 120) + **uma linha colorida por modelo habilitado** no settings (só a média do ensemble; sem membros), com o eixo x indo até o horizonte do modelo mais longo (CFS ~45 dias) e os demais terminando no seu alcance. Modelo sem 700 hPa é simplesmente pulado. Reaproveita a infra do s34 (lagged ensemble, regrade diária, resolução de inits incl. CFS) via o novo módulo `app/src/uteis/forecast_daily.py`. Novos: `clim_hgt700_daily`, downloader ERA5 mensal (`downloaders_era5_hgt700_monthly.py`) e downloaders Z700 diários (ERA5/GDAS + GFS/GEFS/ECMWF/ECMWF-ENS/AIFS/AIFS-ENS/AIGFS/AIGEFS/CFS). Flags: `RUN_S35`, `AAO_HIST_DAYS` (default 30 dias). O gráfico tem **eixo y fixo em −4..4** e **faixas de fase** ao fundo (vermelho claro acima de +1, azul claro abaixo de −1); o observado é estendido até hoje com **GDAS best-effort** (para no último dia publicado) e cada previsão é **ancorada no último observado**, garantindo que nunca haja lacuna observado↔previsto (a previsão cobre o vão até o init). A conversão de altura geopotencial é feita **por dia (slice)** para suportar a série mista ERA5 (m²/s²) + GDAS (gpm). Gráfico: título preto, fontes maiores nos eixos, **rótulos do eixo Y de 1 em 1**, **eixo Y adaptativo** (−4..4 por padrão, expande simétrico se |índice| > 4), todas as previsões (inclusive o **CFS**, cujo init é de ontem) **ancoradas ao último observado** por uma regra uniforme, e um **ensemble multi-modelo** (média dos modelos) como **linha tracejada preta**, plotado só enquanto há ≥ 2 modelos (no trecho de lead longo só-CFS o ensemble é cortado). Título **"Índice AAO: Observado & Previsões"** (preto, fonte maior) e **legenda abaixo do eixo X** (fora da área do gráfico). `script_version='2.0'`
- **Refatoração `app/src/uteis/forecast_daily.py`**: extraídos do s34 (sem duplicação) os helpers genéricos `daily_scalar_on_grid`, `daily_uv200_on_grid`, `resolve_run_inits`, `lagged_ensemble_mean` (+ utilitários de grade), agora compartilhados por s34 e s35.

### Alterado

- **s10: range da anomalia de temperatura em 850 hPa ampliado de ±5 °C para ±8 °C.** `TMP850_LEVELS` (−8 a 8, passo 0,1) e `TMP850_TICKS` (−8 a 8, passo 1); `vmin`/`vmax` e os ticks da colorbar derivam dessas constantes. `script_version='1.1'`

- **s27 (ENSO): nos mapas espaciais de OLR, o rótulo de cada caixa Niño mostra só o NOME** (sem o valor em W/m²), centralizado no x do box. Os mapas de TSM (°C) seguem com o valor. Via novo parâmetro `show_box_value` em `_plot_enso_map` (False nas chamadas de OLR). `script_version='1.13'`
- **Horizonte (lead) e init de previsão agora são por MODELO** (s31, s32, s34, s35, s36), via o helper novo `resolve_forecast_lead_init` em `app/src/uteis/forecast_daily.py`. **CFS**: sempre 45 dias (pseudo-ensemble, init D-1). **GEFS**: lead = `GEFS_FORECAST_LEAD_DAYS` (limitado a 35); init segue a publicação — **D-1 00Z** se lead > 16 dias (ciclo estendido, publica tarde) ou a **rodada de hoje 00Z** se ≤ 16. **Demais modelos (GFS/ECMWF/AIFS/AIGFS...)**: alcance próprio FIXO (~16 dias), **desvinculados** de qualquer setting. A variável `FORECAST_LEAD_DAYS` foi **renomeada para `GEFS_FORECAST_LEAD_DAYS`** (agora só afeta o GEFS), com fallback ao nome antigo. No s34 as **pêntadas cobrem o horizonte do próprio modelo** (CFS 45d→9, GEFS 35d→7, 15d→3); `N_PENTADAS_FIXAS` só vale em reanálise. `RODADA`/`FORECAST_INIT` continuam controlando hora do ciclo e data de TODOS os modelos (a hora flui para o init do GEFS, incl. o D-1). Aviso novo no log quando `RODADA≠"00"` e `GEFS_FORECAST_LEAD_DAYS>16` — o GEFS só estende além de 16 dias no ciclo 00Z. `s35 script_version='2.9'`, `s36 script_version='1.6'`
- **s35 (AAO): eixo X de 15 em 15 dias e eixo Y responsivo assimétrico.** Eixo X agora marca o dia 1 e 15 de cada mês (`DayLocator(bymonthday=[1, 15])`). Eixo Y: default −4..4, mas estende em passos inteiros (`ceil`/`floor`) **só o lado que ultrapassar**, positivo e negativo independentes (ex.: pico +5,2 → topo 6, base segue −4). `script_version='2.8'`

### Corrigido

- **s16: OLR indisponível não aborta mais o script + título reflete o que é plotado.** (1) O OLR (CPC Blended) tem ~5 dias de latência; quando `DATA_FINAL` é mais recente que o último dia do arquivo, o `validar_cobertura_temporal` levantava erro e **derrubava o script inteiro** (perdendo também os mapas que não usam OLR). Agora a cobertura é checada sem levantar exceção: se o período não estiver coberto, loga um aviso e segue com `olr_cyc=None`; a camada de OLR (usada só no mapa `_mag`) é omitida e os demais mapas são gerados normalmente. Import de `validar_cobertura_temporal` removido. (2) **Título corrigido**: o mapa `_nodiv` (sem o quiver de vento divergente) não mostra mais "+ Vento Divergente 200hPa" no título; e o mapa `_mag` só inclui "+ Anom OLR<0" quando a camada de OLR foi de fato plotada. `script_version='2.0'`

- **s32 (forecast): linha "Previsão" do Hovmöller vai no último dia OBSERVADO real, não no init.** O OLR observado (CPC Blended) tem ~5 dias de latência, então a série tinha um vão (último obs → init) preenchido por `interpolate_na(linear)` usando a **previsão** como extremo — injetando informação de previsão (look-ahead) nos dias **antes** da linha, que ficava no init. Visualmente, a faixa suavizada da previsão aparecia acima da linha. Correção (opção mais correta cientificamente): a linha passa a marcar a **fronteira real observado↔projeção** (`eff_end` = último dia com dado observado), e o rótulo "Previsão" foi para **abaixo** da linha. Assim o trecho interpolado fica corretamente do lado da previsão e o look-ahead deixa de violar a região observada. O `init` continua fatiando as janelas espaciais de previsão (inalterado). `_mark_forecast_start`/Hovmöllers: param `init_date`→`boundary_date`. `script_version='3.1'`

- **s34 (reanálise): corrige `list indices must be integers or slices, not tuple` na Etapa 5 (janela móvel).** A variável `t2m_files` (dicionário de caminhos de PNG de saída, chaveado por `(wi, area)`) era sobrescrita no bloco de download da reanálise por uma **lista** de arquivos `.nc` baixados, fazendo a Etapa 5 indexar a lista com a tupla `(wi, a)`. A lista de dados foi renomeada para `t2m_data_files`, preservando o dict de paths. O modo forecast não era afetado (usa `t2m_files_k`).

- **s34 (forecast): descarta o dia do init (passo f000), que saturava o 1º mapa.** A OLR é um campo acumulado e no lead 0 sai degenerada (~2000 W/m², máx. 12578, vs ~230 reais), saturando a anomalia da 1ª janela (mapa inteiro marrom). Agora o período de forecast começa em **init+1** (as pêntadas já começavam), descartando o f000. Mantém os 35 dias de previsão (dias 1–35); campos instantâneos (vento/Z) perdem só o 1º dia. Mesmo tratamento que o s36 já fazia.
- **CFS: leitura de GRIB não descarta mais a variável `v` (afetava o s34).** Em `open_grib_bytes` (`downloaders_ecmwf_fcst200.py`), quando um membro do pseudo-ensemble CFS tinha `u` e `v` com `step` de tamanhos diferentes (ex.: `u` até 1080 h e `v` até 840 h), o `cfgrib` montava um único hipercubo e **descartava o `v`** (`skipping variable v` → `DatasetBuildError`), deixando o `cfs_fcst200_uvz`/`cfs_wnd200`/`cfs_uv850` só com `['u', ...]`. O s34, que lê u **e** v em 200 hPa, falhava com `"u/v nao encontrados. Disponiveis: ['u', 'hgt']"`. Agora usa `cfgrib.open_datasets` (plural) e mescla os sub-datasets pela **interseção das coordenadas** (`join='inner'`), preservando todas as variáveis nos passos comuns. Arquivos CFS já cacheados sem `v` foram regenerados.

### Alterado

- **Logo com TAMANHO PROPORCIONAL à largura dos eixos (não mais fixo) em todos os scripts.** O zoom fixo (0.65) deixava a caixa "Bruno Capucin" enorme em mapas pequenos e a logo da Ampère minúscula em mapas grandes. Agora `app/common/logo_helper.py::proportional_logo_zoom(ax, img_w_px)` calcula o zoom para o logo ocupar uma fração fixa da largura dos eixos (setting `LOGO_WIDTH_FRAC`, default 0.16) — mesmo tamanho relativo em todas as áreas de plotagem. Aplicado aos ~34 scripts (mapas, Hovmöllers, s35, s36) e ao s34 (que também ganhou o canto `upper-right` no seu Hovmöller). `s35 script_version='2.7'`, `s36 script_version='1.5'`
- **Novo logo `LOGO_CAPUCIN` (caixa "Bruno Capucin") em TODOS os scripts; chave `SEM_LOGO` removida.** Nova flag `LOGO_CAPUCIN` (imagem `Entrada/logo_capucin.png` — caixa branca de quinas arredondadas com "Bruno Capucin" em preto negrito), com prioridade `LOGO_CAPUCIN > LOGO_GREC > LOGO_AMPERE` (todas false = sem logo). A seleção foi centralizada num helper único `app/common/logo_helper.py` (`resolve_logo_path`), eliminando a lógica duplicada em ~36 scripts (que agora o importam). Posicionamento: **mapas espaciais** no canto inferior-esquerdo; **Hovmöllers** (s27/s28/s33 passam a usar) no canto superior-direito (s32 já usava), via um parâmetro `corner` no `_add_logo_to_map`; **AAO (s35)** no inferior-esquerdo; **MJO (s36)** FORA da área do diagrama (inferior-esquerdo da figura, abaixo das informações). `s35 script_version='2.6'`, `s36 script_version='1.4'`
- **s35 (AAO) e s36 (MJO): logo movido para o canto inferior ESQUERDO** (antes era o inferior direito) — `fig.figimage(xo=10, yo=10)`. A lógica de seleção do logo via settings (`SEM_LOGO > LOGO_GREC > LOGO_AMPERE`) já funcionava. `s35 script_version='2.5'`, `s36 script_version='1.3'`
- **s35 (AAO): acento corrigido na legenda do ensemble** ("Ensemble (média dos modelos)"). `script_version='2.4'`
- **s35 (AAO): legenda do observado simplificada para "Observado"** (sem o sufixo "(ERA5/GDAS)"). `script_version='2.3'`
- **s35 (AAO): eixo Y travado de volta em −4..4** (removida a expansão simétrica adaptativa que crescia quando algum valor passava de |4|). Valores fora da faixa agora são recortados. `script_version='2.2'`
- **s35 (AAO): o GEFS passa a usar sempre o init de ONTEM (D-1) quando `FORECAST_INIT='latest'`.** O GEFS de 35 dias (ciclo 00Z) só fica completo horas após a rodada, então o `latest` (`now-6h`) podia pegar o init de hoje ainda parcial. Agora, só para o GEFS, em modo `latest` o init0 recua para D-1 (na hora da `RODADA`), garantindo a rodada inteira publicada — espelhando o tratamento que o CFS já tem. Data explícita em `FORECAST_INIT` continua sendo respeitada, e os demais modelos seguem no init mais recente. A lógica de âncora do gráfico (previsão sai do último observado) já lidava com init de ontem. `script_version='2.1'`
- **s34 (forecast): modelos sem uma variável não criam mais diretório de saída vazio.** Campos opcionais ausentes em certos modelos (ex.: CFS sem T850/Z250, modelos de IA sem OLR) agora têm a disponibilidade **estrutural** detectada no início do `_run_once` (`has_tmp`/`has_hgt500`/`has_t2m`, junto do já existente `has_olr`, a partir dos downloaders `*_fn is None`). Essa disponibilidade passa a controlar **(1)** a criação de `<tipo>/<area>/` — só se cria a pasta de campos que terão figura (núcleo Ks/RWS/WAF-Z200/psi_jato/chi_jato sempre; opcionais só se disponíveis) e **(2)** o manifesto de cache (`output_files`) — campos ausentes ficam de fora, então o cache não espera arquivos que nunca existirão e valida corretamente. `script_version='4.16'`

- **s34 `psi_jato`: a corrente de jato (magnitude do vento 200 hPa) só é plotada a partir de 35 m/s** (antes 25). `WIND_MAG_LEVELS = arange(35, 90, 5)`, ticks `arange(40, 90, 10)`; abaixo de 35 m/s fica transparente (deixa o PSI da base aparecer). `script_version='4.6'`

### Alterado

- **s34 `t2m_wnd850`: isolinhas pretas de Z500 mais grossas** (linewidth 0.6 → 1.6) sobre a anomalia de T2m. `script_version='4.11'`

### Adicionado

- **s34: isolinhas de Z250 (jato) também no `waf_z200`** — as mesmas do `psi_jato`/`chi_jato` (azul 9960/10200, vermelho 10440/10680, rótulo + placa branca, zorder 400 linhas / 450 rótulos, abaixo do logo), via o helper `_draw_z250_isolines`. Vale para reanálise e forecast, em todas as janelas/pêntadas/média. `script_version='4.13'`
- **s34: nova modalidade `t2m_wnd850`** — anomalia de **temperatura do ar a 2 m** (shaded, paleta de anomalia do projeto, ±10 °C) + **linhas de corrente do vento 850 hPa ANÔMALO** + **isolinhas pretas da Z500 média do período** (sem rótulo, reusa `Z500_MEAN_LEVELS` do hgt500). Sem WAF. Gerada por janela móvel / pêntada / média, na reanálise e em todos os modelos de previsão. Exigiu: **climatologia diária de T2m** (NCEP `air.2m.day.ltm`, grade gaussiana **regridada p/ 2.5°** — nova `clim_t2m_daily`) e `clim_v850_daily`; **downloaders de T2m** (superfície 2 m): ERA5 reusa `ensure_era5_superficie_for_period`, GDAS novo (`downloaders_gdas_t2m.py`) e 8 de previsão (`downloaders_{gfs,gefs,ecmwf,aifs}_t2m.py` + funções nos módulos ENS/AI); e **downloaders de vento 850 de previsão** para os modelos que faltavam (`downloaders_{gfs,ecmwf,aifs}_uv850.py` + ENS/AI; GEFS já existia). T2m e u/v850 ficam **fora da interseção de datas** (degradam só este campo). `script_version='4.10'`
- **s34: isolinhas de Z250 (jato) também no `chi_jato`** — as mesmas do `psi_jato` (azul 9960/10200, vermelho 10440/10680, rótulo + placa branca, zorder 400 linhas / 450 rótulos, abaixo do logo). Refatorado num helper `_draw_z250_isolines` usado pelos dois mapas. `script_version='4.9'`
- **s34: nova modalidade `chi_jato`** — anomalia de **CHI200** (potencial de velocidade, shaded, paleta verde-marrom do s03, níveis ±60 escala 10⁵) + **vento divergente em 200 hPa anômalo** (vetores, estilo do mapa de RWS) + **magnitude média do jato em 250 hPa** (shaded, mesma paleta/limiar do `psi_jato`: azul-rosa do s16, transparente <35 m/s). **Sem WAF.** Gerada por janela móvel / pêntada / média do período, na reanálise e em todos os modelos de previsão. CHI200 e vento divergente reusam o solver do projeto (`chi_from_wind`/`div_wind_from_chi`) a partir do u/v200 que o s34 já baixa; o jato 250 exigiu **novos downloaders de u/v 250 hPa de previsão** (`downloaders_{gfs,gefs,ecmwf,aifs}_uv250.py` + funções `ensure_*_uv250_fcst_for_period` em `downloaders_ecmwf_ens.py`/`downloaders_aifs_ens.py`/`downloaders_ai_nomads.py`; reanálise usa ERA5/GDAS uv250 já existentes). u/v250 fica **fora da interseção de datas** (degrada só o jato, nunca os demais campos). `script_version='4.8'`
- **s34: isolinhas de Z250 (jato) sobre o `psi_jato`** — replicando a configuração do s16: altura geopotencial média em 250 hPa, **azul nos níveis 9960/10200 (lw 2.0)** e **vermelho 10440/10680 (lw 2.2)**, com rótulos e "placa branca", desenhadas acima do PSI/magnitude/streamlines e **abaixo do WAF** (que segue por cima). Como não havia downloader de Z250 de previsão, foram criados por modelo (espelham os `*_hgt500`, nível 250): `downloaders_{gfs,gefs,ecmwf,aifs}_hgt250.py` + funções `ensure_*_hgt250_fcst_for_period` em `downloaders_ecmwf_ens.py`/`downloaders_aifs_ens.py`/`downloaders_ai_nomads.py`; reanálise usa ERA5/GDAS Z250 já existentes. Z250 também fica **fora da interseção de datas** (degrada só as isolinhas, nunca os demais campos). `script_version='4.5'`
- **s34: duas novas modalidades de campos** (`s34_wave_guide_rossby_wave.py`), geradas por janela móvel / pêntada / média, para **reanálise e todos os modelos de previsão** (GFS/GEFS/ECMWF/ECMWF-ENS/AIFS/AIFS-ENS/AIGFS/AIGEFS), nas mesmas áreas do s34: (1) **`hgt500_anom/`** — anomalia de altura geopotencial em 500 hPa (sombreada) + **isolinhas da Z500 média do período em preto, sem rótulo** (sem WAF); (2) **`psi_jato/`** — anomalia de **PSI200** (sombreada, paleta vermelho-branco-azul do s04) na base + **magnitude média do vento em 200 hPa** sobreposta (paleta azul-rosa do s16, **transparente abaixo de 25 m/s**, deixando o PSI aparecer) + **linhas de corrente** do vento rotacional + **WAF por cima**. Reaproveita o solver de streamfunction do `plot_psi200` (`_helmholtz_psi` → `_compute_vorticity`/`_solve_poisson_sphere`/`_compute_rotational_wind`), a climatologia diária NCEP (nova `clim_hgt500_daily`) e os helpers de projeção/cyclic/WAF do próprio s34. **Downloaders de Z500 de previsão** novos por modelo (espelham os `*_tmp850`, variável HGT/`gh`/`z` em 500 hPa): `downloaders_{gfs,gefs,ecmwf,aifs}_hgt500.py` e funções `ensure_*_hgt500_fcst_for_period` adicionadas em `downloaders_ecmwf_ens.py`, `downloaders_aifs_ens.py` e `downloaders_ai_nomads.py`; reanálise usa ERA5/GDAS Z500 já existentes. O **Z500 fica FORA da interseção de datas** (mesma lição da correção de OLR): um dia de Z500 faltante degrada só o mapa `hgt500` daquela janela, nunca z200/Ks/RWS/T850/PSI. `script_version='4.4'`

### Alterado

- **s34: download NOMADS (GFS/GEFS) agrupado — 1 requisição por passo em vez de ~8** (`downloaders_nomads_combo.py`). Todas as variáveis que o s34 usa (u/v/HGT @ 200/250/500/850, TMP @ 850, T2m, ULWRF/OLR) estão no **mesmo arquivo GRIB**; a pré-busca combinada faz **uma requisição** ao grib filter por passo (pedindo todos os níveis/variáveis) e **fatia** o GRIB localmente nos mesmos NetCDFs por variável que os downloaders por-variável produzem. Roda na Etapa 1 **antes** dos downloaders por-variável, que viram **fallback/cache-hit** (best-effort: se algo faltar, baixam individualmente — pior caso = comportamento anterior). Reduz ~8× as requisições ao NOMADS → praticamente elimina o throttle (302) e acelera bastante. Vale para GFS e GEFS (grib filter); ECMWF/AIFS (open data) e AIGFS/AIGEFS (byte-range) usam seus próprios mecanismos. Validado: 1 GRIB combinado (~13,5 MB) extrai u/v/hgt@200, uv250, hgt250, hgt500, uv850, tmp850, t2m e olr com valores corretos.

### Corrigido

- **Variável ausente num modelo de IA não aborta mais o s34** (`downloaders_ai_nomads._byte_range` e `downloaders_ecmwf_fcst200.match_record`). Modelos de IA (AIGFS/AIGEFS no NOMADS; AIFS/AIFS-ENS no open data) podem **não publicar todas as variáveis/níveis** que o s34 pede (ex.: AIGFS **sem TMP@2m**). Antes, o registro ausente no `.idx`/index levantava **`KeyError`** (não capturado) → **abortava a rodada** (`'TMP:2 m above ground nao encontrado no .idx'`). Agora levanta **`StepNotAvailable`** (que todos os `_download_day` já tratam) → o passo é **pulado**, a variável simplesmente não sai e o s34 **pula só aquele campo** (desacoplamento), sem derrubar os demais.
- **s34: linhas de corrente agora são plotadas também no `globo_3d` (ortográfica)** nos campos `psi_jato` (vento rotacional) e `t2m_wnd850` (vento 850 anômalo). Eu havia **pulado o `streamplot` na projeção ortográfica** (achando que não era confiável), então o globo_3d saía sem vento. Confirmado que funciona; removido o bloqueio (com `try/except` por segurança). Vale para todos os modelos (forecast) e a reanálise. `script_version='4.14'`
- **GFS: throttling do NOMADS (302/403/503) não aborta mais o s34** (`downloaders_gfs_fcst200._download_grb2`, compartilhado por GFS 200/OLR/T850/HGT500/HGT250/UV250/UV850/T2M). O `_download_grb2` do GFS era a versão antiga (3 tentativas **sem backoff**; qualquer erro ≠404 virava `RuntimeError` **fatal**) — com 8 variáveis × muitos passos, o NOMADS respondia **302 Moved Temporarily** (throttle) e um passo qualquer (ex.: `gfs_hgt250_..._f036`) **abortava a rodada inteira**. Agora é igual ao GEFS: **backoff exponencial + jitter** (8 tentativas, 1→60 s) que atravessa a janela de throttle; se esgotar, levanta **`StepNotAvailable`** (passo pulado, não-fatal). Os chamadores GFS já tratam `StepNotAvailable`.
- **s34: logo dos mapas respeita as 3 opções do settings** (`SEM_LOGO`/`LOGO_GREC`/`LOGO_AMPERE`), em **todos** os tipos de mapa e nas duas modalidades (forecast de qualquer modelo + reanálise). Antes a seleção do logo estava **duplicada em 6 lugares** e só tratava `SEM_LOGO`/`LOGO_GREC` (o `LOGO_AMPERE` do settings era ignorado — caía no `else` implícito = `novo_logo.png`). Agora há um helper único `_resolve_logo_path` com a precedência do projeto (SEM_LOGO tem prioridade → nenhum; senão LOGO_GREC → `logo_grec.png`; senão LOGO_AMPERE → `novo_logo.png`; se nenhum estiver habilitado, não plota logo), usado por todas as funções de plotagem do s34. `script_version='4.12'`
- **s34 reanálise: crash `TypeError: list indices must be integers or slices, not tuple`** (`s34_wave_guide_rossby_wave.py`). O dict de paths do hgt500 que adicionei foi nomeado `hgt_files`, **colidindo** com a variável local `hgt_files` do ramo de reanálise (lista dos NetCDFs de hgt200 ERA5/GDAS). Em reanálise a lista sobrescrevia o dict → `hgt_files[(wi, area)]` quebrava. No forecast o ramo não roda, por isso passava. Correção: a lista da reanálise foi renomeada para `hgt200_files`.
- **s34: rótulos das isolinhas de Z250 sobem para zorder alto** (linhas 400, rótulos 450) — ficam acima de tudo (shaded/streamlines/WAF), mas **abaixo do logo** (z=500). Vale para reanálise e forecast.
- **s34 reanálise: isolinhas de Z250 (e a anomalia de Z500) saíam erradas por unidades MISTAS ERA5+GDAS** (`s34_wave_guide_rossby_wave.py`). Na reanálise a série diária combina **ERA5** (geopotencial `z`, ~102000 m²/s² em 250 hPa) com **GDAS** (altura geopotencial `hgt`, ~10400 m) no mesmo array. O `_to_geop_height` decidia o ÷g pela **média global** do array inteiro → como a maioria dos dias era ERA5, dividia TUDO por g, jogando os dias GDAS para ~1061 m; a média da janela caía para ~7000 m, **abaixo** dos níveis fixos 9960–10680 → **nenhuma isolinha** no `psi_jato` (e Z500 corrompido de forma silenciosa). No forecast não aparecia (fonte única, unidade uniforme). Correção: `_to_geop_height` passa a normalizar **por slice de tempo** (cada dia decide ÷g conforme sua própria escala), corrigindo a série mista. `script_version='4.7'`
- **s34: crash do Tk (core dump) ao rodar com ECMWF-ENS** (`s34_wave_guide_rossby_wave.py`). O s34 era o **único** script de plotagem que **não** forçava `matplotlib.use('Agg')` antes de importar o `pyplot` (todos os s11–s32 já fazem). Sem isso, o matplotlib escolhe o backend interativo **TkAgg**; como o ECMWF-ENS baixa os 50 membros em **paralelo** (`ThreadPoolExecutor` no `_ens_mean_2d`), o garbage collector destrói objetos Tk **fora da main thread** → enxurrada de `RuntimeError: main thread is not in main loop` e, no encerramento, `Tcl_AsyncDelete: async handler deleted by the wrong thread` → **core dump** (`IOT instruction`). GFS/GEFS não disparavam tão facilmente (menos threading) e os testes não pegaram porque rodavam com `MPLBACKEND=Agg` no ambiente. Correção: `import matplotlib; matplotlib.use('Agg')` **antes** do `import matplotlib.pyplot` no s34 (pipeline é headless, só salva PNG). Agora o backend é Agg mesmo sem a env var.
- **s34 forecast: lacuna de janelas móveis de z200 quando o OLR de um dia falha no download** (`s34_wave_guide_rossby_wave.py` + `downloaders_gefs_fcst200.py`). Observado no GEFS (init 19/06): as janelas móveis 26-30, 27-01, 28-02, 29-03 sumiram, enquanto a pêntada3 (30-04) saiu. **Causa raiz dupla**: (1) os 4 passos de OLR do dia 30/06 (`f264..f282`) bateram em **403 Forbidden / 302** (throttling do NOMADS, **não** 404) repetidas vezes; o backoff curto (6 tentativas, 1→15 s ≈ 30 s) não atravessava a janela de throttle (que dura **minutos**), então o dia inteiro de OLR foi pulado; (2) o eixo de datas do s34 era a **interseção de TODAS as variáveis, incluindo OLR** — logo o dia 30/06 sumia de **todos** os produtos, e como a janela móvel só plota se completa, toda janela contendo 30/06 era pulada (a pêntada sobreviveu por tolerar 4/5 dias). **Correções**: (a) `_download_grb2` (compartilhado por GEFS 200/OLR/T850) com **backoff mais longo + jitter** (8 tentativas, 1→60 s ≈ 123 s; jitter dessincroniza as threads paralelas), atravessando o throttle e recuperando o passo; (b) o eixo de datas passa a ser a interseção **apenas dos campos dinâmicos** (u/v/hgt/T850); o **OLR é reindexado com NaN** nos dias faltantes e a média da janela usa **`nanmean`** — uma falha de OLR num dia degrada **só** o mapa de OLR daquela janela (ou o pula, se a janela inteira ficar sem OLR), **nunca** z200/Ks/RWS/T850. `script_version='4.3'`
- **s29: cópia do HTML para a Área de Trabalho funciona em qualquer PC** (`s29_walker_cell_mundo.py`). O caminho do Desktop estava **fixo em `/mnt/c/Users/Pichau/Desktop`**, então na máquina de trabalho (usuário `Bruno Capucin`) o `desktop.exists()` era falso e o HTML nunca era copiado. Novo helper `_windows_desktop()` **auto-detecta** o usuário real do Windows no WSL (varre `/mnt/c/Users`, pula pastas de sistema, tenta `Desktop` e `Área de Trabalho`), com override opcional via setting `DESKTOP_DIR`. Funciona tanto na Pichau quanto no PC de trabalho. (O HTML sempre foi salvo também em `Saida/s29_WALKER_CELL_MUNDO/`.)
- **ERA5 HGT200: cache parcial de mês não era reusado corretamente** (`downloaders_hgt200_ERA5.py`). O arquivo mensal `era5_hgt200_AAAAMM.nc` não codifica o intervalo de dias; o cache só checava **existência+tamanho**. Se uma rodada anterior baixou só parte do mês (ex.: dias 1-15 de out/2009) e uma nova rodada pedia outro trecho (ex.: 16-31), o arquivo parcial era reusado → `da.sel(time=16..31)` vazio → erro `Nenhum dado valido no periodo para ('hgt','z','gh','geopotential')` no s34. Correção: novo `_file_covers_until` valida que o NetCDF cobre até o último dia pedido; se não cobrir, **re-baixa o mês**. (Os downloaders de u/v200 e t850 já validavam cobertura; os de hgt250/500/850 têm o mesmo padrão latente — corrigir se necessário.)
- **GDAS (u/v 200 e 850): ciclo não publicado (HTTP 404 ou 403) não aborta mais a rodada**. Em runs que incluem o **dia de hoje** (s31 forecast, e qualquer reanálise terminando hoje), o GDAS baixa o dia inteiro (00/06/12/18Z), mas os ciclos mais recentes de hoje ainda não saíram. O NOMADS sinaliza isso de **duas formas**: **404** quando a pasta do ciclo existe mas o arquivo não (ex.: 12Z em publicação) e **403 Forbidden** quando a pasta do ciclo futuro nem existe (ex.: 18Z). Ambos eram tratados como **fatais** (`RuntimeError` após 3 tentativas) e abortavam tudo. Correção em `downloaders_gdas_uv200.py` e `downloaders_gdas_uv850.py`: **403 e 404** viram **`StepNotAvailable`** → o ciclo é **pulado** (`download_..._for_date` segue com as horas publicadas; se o dia inteiro estiver indisponível, sinaliza e `ensure_..._for_period` pula o dia). As demais variáveis GDAS têm o mesmo padrão latente (corrigir conforme necessário).
- **GEFS: throttling do NOMADS (HTTP 403) não aborta mais a rodada** (mesmo problema que já fora corrigido no AIGFS/AIGEFS, mas o GEFS ficara de fora). O ciclo **00Z a 35 dias** (840 h = muitos passos estendidos) martela o NOMADS, que responde **403 Forbidden** nos passos longos. O `_download_grb2` (compartilhado por GEFS 200/OLR/T850) só tratava **404** como pulável; um **403** era retentado 3× **sem pausa**, re-batia no rate-limit e virava `RuntimeError` fatal — abortando o s34 inteiro (visto hoje em `gefs_olr_2026061700_f654`). Correção em `downloaders_gefs_fcst200._download_grb2`: **backoff exponencial** (6 tentativas, 1→15 s) que recupera o passo throttled e, se ainda assim esgotar num não-404, levanta **`StepNotAvailable`** (sinal de "pula o passo") em vez de `RuntimeError`. Como os três chamadores GEFS já capturam `StepNotAvailable` e seguem, **um único edit** torna o 403 não-fatal nas três variáveis → as figuras saem até onde há dado. GFS não precisa (vai só a 384 h = 16 dias, poucos passos estendidos, baixo risco de 403).
- **AIGFS/AIGEFS: throttling do NOMADS não aborta mais a rodada**. O NOMADS limita acesso com muitas requisições rápidas (retorna 403/503 transitório, não 404), e o byte-range fazia o s34 falhar de vez. Correção em `downloaders_ai_nomads.py`: **backoff exponencial** (6 tentativas, 1→15 s) que recupera o passo throttled; **teto de 4 conexões** paralelas no NOMADS (independente de `DOWNLOAD_WORKERS`); e, se ainda assim falhar, **pula o passo e segue** (a rodada termina com o que há).
- **Passo de previsão ainda não publicado (HTTP 404) não aborta mais o s34** — gera figuras até onde há dado. Antes, ao usar o ciclo **00Z de hoje** (cujo trecho estendido leva horas para subir no NOMADS/ECMWF), um 404 num passo (ex.: GEFS `f816`) era tratado como erro fatal e parava tudo. Agora um 404 vira `StepNotAvailable` (nova exceção em `app/common/forecast_download.py`): o downloader **pula o passo/dia faltante e segue**. Aplicado a todos os modelos (GFS/GEFS/ECMWF/AIFS e os ENS, no 200/OLR/T850). Combinado com a regra "janela móvel só completa", os mapas vão exatamente até o último dia disponível.
- `app/common/forecast_download.py` + downloaders de previsão: **corrupção silenciosa de NetCDF no download paralelo**. ecCodes (cfgrib) e HDF5 (netCDF4) **não são thread-safe**; o download paralelo de dias fazia leituras/escritas concorrentes que corrompiam arquivos de forma intermitente (coords faltando → erro "Nem 'time' nem 'valid_time'"). Correção: **lock global `GRIB_NETCDF_LOCK`** serializando as chamadas dessas libs (abertura GRIB via `open_grib_bytes`/`_open_gfs_*` e escrita via novo `save_netcdf`); a parte cara (download de rede) segue paralela. Validado: AIFS 16 dias baixou sem corrupção.
- `scripts/s34_wave_guide_rossby_wave.py`: **mapa de RWS estava com o disco todo verde na projeção `globo_3d` (ortográfica)** em algumas janelas/pêntadas. Causa: a RWS (operadores esféricos ~`1/cos φ`) é **singular nos polos** e gerava spikes de até ~`1e12` em `|lat|≥87,5°`; como a `globo_3d` é centrada perto do polo sul (`ORTHO_CENTRAL_LATITUDE≈-84`), esses valores saturavam o `contourf` e preenchiam todo o disco com a cor de extremo (verde quando o polo estourava positivo). O código só mascarava a singularidade equatorial (`|lat|<10°`). Correção: **adicionada máscara polar `|lat|>75°`** (nova constante `RWS_MASK_LAT_POLE=75.0`, mesmo critério do WAF/`POLAR_MASK_LAT`); campo passou de ±`7e12` para faixa física (~±80). `script_version='3.8'`

### Alterado

- **s27: range da anomalia de OLR ampliado de ±40 para ±60 W/m²** (`OLR_LEVELS = arange(-60, 66, 6)` — passo 6, 21 níveis, mesma densidade de antes mas com ticks redondos a cada 12 incluindo o 0). Vale para os dois Hovmöllers de OLR e o mapa OLR. `script_version='1.10'`.
- **s34: saída reorganizada por MODO aninhado** (igual ao s31) — `s34_TELECONEXAO/FORECAST/<CONTINENTE>/<MODELO>/<tipo>/<area>/` e `s34_TELECONEXAO/REANALISE/<tipo>/<area>/`, em vez do antigo sufixo `s34_TELECONEXAO_FORECAST`/`s34_TELECONEXAO_REANALISE`. A base `s34_TELECONEXAO/` vira contêiner com `FORECAST/` e `REANALISE/` dentro; o restante (continente/modelo/tipo/área) inalterado. `script_version='4.1'`
- **s31: saída reorganizada por MODO aninhado** — `s31_CHI200_INTRASAZONAL/REANALISE/` (reanálise) e `s31_CHI200_INTRASAZONAL/FORECAST/<MODELO>/` (previsão), em vez do antigo `s31_CHI200_INTRASAZONAL/` (flat) + `s31_CHI200_INTRASAZONAL_FORECAST/<MODELO>/`. A base vira um contêiner com `REANALISE/` e `FORECAST/` dentro (simetria; novo helper `_base_output_dir`).
- **ECMWF-ENS/AIFS-ENS muito mais rápido**: o download dos 50 membros é *bandwidth-bound*, mas o ECMWF open data (S3/CDN) **escala com conexões** — medido **~6× mais rápido** com 64 workers vs 8 (de ~2,3 para ~13 MB/s). O default de `ECMWF_ENS_WORKERS` subiu de **8 → 32** (pode-se chegar a ~64; só vale p/ ECMWF, NOMADS bane). Combinado com reduzir `ECMWF_ENS_MEMBERS` (ex.: 20), leva o processamento de ~1 h para poucos minutos. (Multipart byte-range foi testado e descartado: ~0,6× — um stream único é mais lento que N paralelos.)
- `scripts/s34_wave_guide_rossby_wave.py`: **saída reorganizada por MODO → CONTINENTE → MODELO → tipo → área**. Forecast: `Saida/s34_TELECONEXAO_FORECAST/<MODELOS_AMERICANOS|MODELOS_EUROPEUS>/<MODELO>/<tipo>/<area>/` (ex.: `.../MODELOS_AMERICANOS/GFS/fontes_rws/psa/`). Reanálise: `Saida/s34_TELECONEXAO_REANALISE/<tipo>/<area>/`. Americanos (NCEP) = GFS/GEFS/AIGFS/AIGEFS; europeus (ECMWF) = ECMWF/ECMWF-ENS/AIFS/AIFS-ENS (mapa `_MODEL_CONTINENT`). Substitui a antiga `Saida/<MODELO>/s34_WAVEGUIDE_ROSSBY/`. A subpasta por **área** dentro de cada tipo permanece (ex.: `fontes_rws/psa/`); `waf_olr` só é criada para modelos com OLR.
- `scripts/s34_wave_guide_rossby_wave.py`: **mapas não rotulam datas além do alcance do modelo**. A **janela móvel** só é plotada se **completa** (todos os dias presentes) → a média móvel encerra exatamente no último dia do modelo (ex.: modelo de 15 d encerra em `01/jul`; GFS de 16 d em `02/jul`, não `03`). A **pêntada** (janela fixa) só é plotada com **≥ 4 dos 5 dias** (`MIN_DIAS_PENTADA=4`; 4 ok, <4 pula). Antes, janelas/pêntadas com 1 dia eram plotadas com o rótulo da faixa inteira, sugerindo previsão inexistente.
- `scripts/s34_wave_guide_rossby_wave.py`: **contorno fino preto (linewidth 0.5) ao redor da área de plotagem** em todas as áreas, exceto `globo_3d` (ortográfica, que mantém a borda circular própria). Aplicado nos 5 mapas (Ks/RWS/WAF-Z200/OLR/T850) via novo helper `_add_map_border` (spine `geo` com **`zorder=1000`** — sem isso, o `contourf`/feições desenhavam por cima e "comiam" a borda em alguns lados, ex.: esquerda). `script_version='4.0'`
- `scripts/s34_wave_guide_rossby_wave.py`: range do mapa de anomalia de T850 ampliado de `-5..5` para `-8..8` °C (`TMP850_LEVELS = arange(-8, 8.5, 0.5)`, `TMP850_TICKS = arange(-8, 9, 1)`); `script_version='3.7'`
- `scripts/s27_hovmoller_enso.py`: isolinha de anomalia de vento zonal positivo alterada de `red` para `darkred`; mapa ENSO recebe labels com nome e anomalia média de TSM de cada box Niño (igual ao s12); Niño 4 em magenta, Niño 1+2 em limegreen (overrides locais via `_BOX_COLOR_OVERRIDE`); zorder dos boxes elevado para 300 (acima do quiver=200) para bordas não serem cortadas pelos vetores; `script_version='1.4'`
- `scripts/s28_hovmoller_iod.py`: isolinha de anomalia de vento zonal positivo alterada de `red` para `darkred`
- `scripts/s29_walker_cell_mundo.py`: `WIND_VEC_LAT_MAX` reduzido de 60° para 30°, alinhando a cobertura latitudinal do vento 850 hPa com o vento divergente 200 hPa; `script_version='5.45'`
- `scripts/s29_walker_cell_mundo.py`: contorno azul em regiões polares (borda do gelo/NaN do OISST) eliminado — causa: aresta WebGL entre célula whitesmoke (LAND_SENTINEL) e célula de TSM azul-fria adjacente renderizava com cor azul; correção: `binary_dilation` aplicado à máscara NaN completa (`terra + NaN oceano`) para garantir 1 célula whitesmoke de buffer em TODAS as fronteiras NaN; `script_version='5.50'`

### Adicionado

- **s27 — 3 Hovmöllers SEM média móvel (dado diário cru): OLR+U850, CHI200+U850 e TSM+U925** (`s27_hovmoller_enso.py`). Complementam os Hovmöllers em pêntada com a versão **crua** (sem suavização): **(5)** Anomalia de OLR + U850, **(6)** Anomalia de CHI200 + U850 (ambos reusam `hov_u` = U850 anômalo do Hovmöller original) e **(7)** Anomalia de TSM + U925 (reusa `hov_sst` cru + novo `hov_u925_raw`, o U925 anômalo antes da pêntada). Reusam `_hov_domain_mean(...)` direto (sem `_pentad_smooth`). Úteis para enxergar a estrutura diária/propagação fina que a pêntada (5d) borra. `script_version='1.11'`.
- **s27 — mapa da área ENSO com OLR + vento ANÔMALO em 850 hPa** (`olr_vento850_…png`). Mesma plotagem do mapa OLR+925, mas com os vetores em **850 hPa** (reusa `u_cyc_wind/v_cyc_wind` do mapa SST+850 já carregados de `wind850_anom.nc`; nenhum download extra). `script_version='1.12'`.
- **s27 — 2 novos MAPAS da área ENSO com vento ANÔMALO em 925 hPa: SST+925 e OLR+925** (`s27_hovmoller_enso.py`). Além do mapa existente de **Anomalia TSM + Vento Anômalo 850 hPa** (mantido, com os boxes Niño 1+2/3/3.4/4 e os rótulos por box), o s27 passa a gerar a **mesma plotagem** em mais duas versões: **(A)** **Anomalia de OLR** (CPC Blended PSL, `BrBG_r`, ±40 W/m²) + vetores de vento anômalo em **925 hPa**; **(B)** **Anomalia de TSM** + vetores de vento anômalo em **925 hPa** (igual ao original, mas no nível 925 padrão NOAA para os alísios). Os rótulos por box mostram a média da área no campo sombreado (TSM em °C, OLR em W/m²). O vento 925 anômalo (u+v, média do período) vem do novo `plot_olr_wind925_anom.py` (`wind925_anom.nc`), espelho do de 850. Plot do mapa fatorado em `_plot_enso_map`. `script_version='1.7'`.
- **Climatologia PSL de vento vetorial (u+v) em 925 hPa** — `app/src/uteis/clim_PSL_wnd925.py` (espelho de `clim_PSL_wnd850.py`, `select_option('925mb')`; `get_clim_wnd925_paths`). Necessária para os vetores de vento anômalo 925 dos novos mapas ENSO do s27 (a `clim_PSL_wnd_zonal_925.py` existente dá só a componente zonal u).
- **s27 — 3 novos Hovmöllers de ENSO (SST+U925, CHI200+U925, OLR+U925), com média móvel de pêntada** (`s27_hovmoller_enso.py`). Além do Hovmöller existente (SST + vento zonal **850** hPa, mantido), o s27 passa a gerar: **(2)** SST + vento zonal **925** hPa, **(3)** **anomalia de CHI200** (potencial de velocidade 200 hPa) + U925 e **(4)** **anomalia de OLR** (CPC Blended PSL) + U925. Seguem a convenção NOAA/CPC para a célula de Walker: faixa **5°S–5°N**, vento em **925 hPa** (alísios), **média móvel de 5 dias** (`HOVMOLLER_SUAVIZACAO_DIAS`, default 5; 0 = sem). O CHI200 é a **anomalia pura** (u/v200 → anomalia vs LTM NCEP → Poisson via `chi_from_wind`), **sem** filtro intrasazonal — preserva o sinal lento do ENSO (o passa-banda MJO o removeria); paleta verde/terra igual ao s31, range ±10 ×10⁶ m²/s. O OLR reusa o arquivo CPC Blended do s32 (BrBG_r). Plot fatorado em `_plot_hov_panel`. `script_version='1.6'`. Validado (15/05–15/06/2026, El Niño em desenvolvimento): CHI200 com ascensão standing e OLR com convecção, ambos no Pacífico central-leste.
- **Downloaders de vento u/v em 925 hPa (ERA5 + GDAS)** — `downloaders_wind925.py` e `downloaders_gdas_uv925.py`, espelhando os de 850 hPa (nível padrão NOAA/CPC para os alísios no monitoramento Walker/ENSO). API idêntica: `ensure_era5_uv925_for_period` / `ensure_gdas_uv925_for_period`. Para os novos Hovmöllers de ENSO do s27 (vento zonal 925). Validado (ERA5: `pressure_level=[925.]`, u/v ok).
- **s32 — modo previsão (OLR intrasazonal via GEFS + CFSv2), espelhando o s31**. Novo `MODE='forecast'` no s32: `main()` virou laço `_run_once(mode, fcst_model)` com **GEFS (35d) e CFS (45d) SEMPRE rodando**. A série de OLR no forecast emenda **anomalia CPC Blended (PSL) `[start_dl, init]` + previsão**; como a previsão é OLR BRUTO, a anomalia é **"igual com igual"**: `olr_fcst − clim_olr_daily` (mesma climatologia CPC do `clim_diaria_olr` usada pelo s34 — sem degrau no init), regridada para a grade 2.5° do PSL. Forecast usa só o sinal **causal** (sem Lanczos), à la NCICS. Painel u850 (OLR + vento) usa GEFS/CFS u850. **CFS OLR**: novo `ensure_cfs_olr_for_period` (`ulwtoa`/ULWRF no topo → var `olr`; downloader CFS generalizado p/ variáveis arbitrárias). Saída idêntica ao s31: `s32_OLR_INTRASAZONAL/FORECAST/<GEFS|CFS>/{1_DAY…10_DAY, HOVMOLLER, MEDIA_PERIODO_TOTAL}/` e `/REANALISE/`; logo no canto superior direito nos Hovmöllers; "Previsão" marcando o init. Reanálise inalterada. `script_version='3.0'`. Validado end-to-end (GEFS 16d truncado hoje, CFS 45d completos; árvore correta).
- **s34 reanálise — mapa da MÉDIA DO PERÍODO TOTAL** `[DATA_INICIAL, DATA_FINAL]` para cada tipo (Ks/RWS/WAF-Z200/WAF-OLR/WAF-T850) e área, salvo na **mesma pasta `<tipo>/<área>/`** com o sufixo **`media_total`** no nome (como as pêntadas usam `pentadaX`). É gerado **primeiro** (Etapa 4b, logo após os Hovmöllers, antes das janelas móveis). **Só na reanálise** (no forecast a média de todo o horizonte misturaria semanas distantes). Além disso, a reanálise **deixou de gerar pêntadas** (elas cairiam para frente, além de `DATA_FINAL`; pêntadas seguem só no forecast). Reusa `_render_spatial_window`. `script_version='4.2'`
- **s31 forecast — mapas espaciais em múltiplas janelas (1/2/3/5/7/10 dias) + Hovmöllers em subpasta** (paridade com o "Select Days" do NCICS). Antes só pêntadas (5d); agora, em forecast, os mapas de CHI200 são médias de **1, 2, 3, 5, 7 e 10 dias** em **janelas consecutivas a partir de `init`** (novo helper `_forecast_windows`, ancorado p/ frente — não recua na reanálise mesmo se a previsão truncar, fechando a pendência das pêntadas do GEFS). Cada tamanho vai numa subpasta própria: `s31_CHI200_INTRASAZONAL_FORECAST/<MODELO>/<N>_DAY/`. Os **dois Hovmöllers** (CHI200 e CHI200+u850) passaram para a subpasta **`<MODELO>/HOVMOLLER/`**; o mapa do período fica na raiz do modelo. Conjunto de janelas configurável via `FORECAST_MAP_WINDOWS`. Reanálise inalterada (segue pêntadas flat). `script_version='2.12'`
- **s31 forecast — modelo CFSv2 (pseudo-ensemble lagged, à la NCICS/Schreck) + os dois modelos sempre rodam** — `app/src/uteis/downloaders_cfs_ensemble.py`. Segundo modelo subsazonal do s31, replicando o método do NCICS/Carl Schreck: o **pseudo-ensemble defasado de 16 membros** = **4 ciclos (00/06/12/18Z) × 4 membros (`time_grib_01..04`)** do dia D, mediado **alinhado por data válida** (os ciclos são defasados 6h). Fonte: CFSv2 operacional no NOMADS (`.../cfs/prod/cfs.{D}/{HH}/time_grib_{0M}/wnd200|wnd850...daily.grb2`), via **byte-range pelo `.idx`** (corta os ~12 MB/membro dos primeiros **45 dias** = piso dos membros = janela do Schreck). Computa a **média dos 16 membros** de u/v 200 e 850 hPa e entrega NetCDF diário no formato que o s31 já consome — então segue o **Path A** (alimenta o pipeline existente; como o χ é linear no vento, χ(média do vento) = média dos χ200). Resiliente: membro ausente (404/403) é descartado, exige ≥ 8/16. **Dia D = ONTEM por padrão** (garante os 16 ciclos publicados; hoje os ciclos 12/18Z ainda não saíram). O `main()` do s31 virou um **laço por modelo** (`_run_once(mode, model, logger)`): em forecast **GEFS (35d) e CFS (45d) SEMPRE rodam**, cada um em `s31_CHI200_INTRASAZONAL_FORECAST/<GEFS|CFS>/`. `CFS_LEAD_DAYS`/`CFS_WORKERS` configuráveis. `script_version='2.12'`. Validado end-to-end (16/16 membros, 46 dias, Hovmöller/pêntadas/período).
- **s31 — modo previsão (CHI200 intrasazonal/MJO via GEFS), à la NCICS/Schreck**. Novo `MODE='forecast'` no s31 (espelha o s34: `RODADA`, `FORECAST_INIT`, `FORECAST_LEAD_DAYS`). Emenda **reanálise ERA5/GDAS `[init−~266d, init]` + GEFS `geavg` `[init, init+lead]`** numa série diária única e roda a MESMA cadeia intrasazonal. Funda-se no fato de `remove_media_movel` ser **CAUSAL** (só usa os 120 dias anteriores) → o sinal intrasazonal é válido em toda a previsão. **Não usa Lanczos** no forecast (filtro centrado `n=60 > lead=35` degradaria a previsão inteira): seguindo o método do NCICS/Carl Schreck, usa o **sinal causal (trailing-mean) + Wheeler-Weickmann com a previsão servindo de padding de borda**. Pêntadas **para frente** a partir de `init` (P1=init+1..+5…). Nos **Hovmöllers** (forecast e reanálise) o **logo foi para o canto superior direito**; no forecast, o início da previsão é marcado por **linha tracejada grossa** com o rótulo **"Previsão"** centralizado (branco/negrito, contorno preto). Saída em `s31_CHI200_INTRASAZONAL_FORECAST/<MODELO>/` (reanálise inalterada). Por enquanto só GEFS (único subsazonal com `geavg` pronto); CFSv2 (pseudo-ensemble lagged, ~45d) virá depois. `script_version='2.11'`
- **s31 (forecast) — downloader GEFS u/v 850 hPa** — `app/src/uteis/downloaders_gefs_uv850.py`. Dependência do modo previsão do s31 (Hovmöller de vento zonal 850 intrasazonal): baixa u/v (UGRD/VGRD) em 850 hPa do mesmo arquivo pgrb2a 0.5° da média do ensemble (`geavg`), via NOMADS grib filter. Espelha o `downloaders_gefs_tmp850` e reusa o parser agnóstico de nível `_open_gfs_grb2` (filtra `isobaricInhPa`, devolve `u`/`v`) + o `_download_grb2` resiliente. Um NetCDF por dia válido com `u`/`v`. API: `ensure_gefs_uv850_fcst_for_period(init, lead_hours, ...)`. Validado end-to-end (NetCDF com `u`/`v` em 850, 0.5°).
- **s36 (frentes frias) — downloader ERA5 de superfície** — `app/src/uteis/downloaders_era5_superficie.py`. Primeiro tijolo do produto `s36_cold_front_track` (frequência de passagens de frente fria por localidade). Baixa ERA5 single-levels **6-horário** (00/06/12/18 UTC) com `10m_u/v` (`u10`/`v10`), `2m_temperature` (`t2m`) e `mean_sea_level_pressure` (`msl`, diagnóstico). Reaproveita a lógica robusta do `downloaders_wind100m_ERA5.py` (arquivo `.part`, validação de cobertura sinótica real, regra conservadora alinhada à UI do CDS no mês corrente). API: `download_era5_superficie_hourly(year, month, ...)` e `ensure_era5_superficie_for_period(start, end, ...)`. A detecção usará o método de mudança de vento (Simmonds et al., HS): giro de `v` para sul + `|Δv| > 2 m/s` em 6 h, confirmado por `ΔT2m < 0`. Validado end-to-end (NetCDF sai com `u10/v10/t2m/msl`).
- **Modelos de IA do NCEP (AIGFS e AIGEFS) no s34** — `app/src/uteis/downloaders_ai_nomads.py`. Fonte: NOMADS (`com/aigfs` e `com/aigefs`), que **não tem grib filter** → download por **byte-range usando o `.idx`** (novo helper que lê o índice `registro:offset:PARAM:nível:` e baixa só os campos por `Range` HTTP). Alcance **16 dias** (f384), 6-horário. **AIGEFS usa a média do ensemble PRONTA** (`ensstat/products/...avg...`, análogo ao `geavg` do GEFS) → 1 download por campo, leve (não baixa os 31 membros). Ambos têm `u/v/HGT@200` e `TMP@850` mas **NÃO têm OLR** → geram **4 dos 5 mapas** (reusa o suporte a "modelo sem OLR" do AIFS). Flags `RUN_AIGFS`/`RUN_AIGEFS`. Validado end-to-end (4 mapas, `waf_olr` vazio). Total de 8 modelos no s34.
- **Modelos de IA do ECMWF (AIFS) no s34**: `downloaders_aifs_fcst200.py`, `downloaders_aifs_tmp850.py` (AIFS-single determinístico) e `downloaders_aifs_ens.py` (AIFS-ENS, média dos 50 membros). Fonte: ECMWF Open Data (`aifs-single/0p25/oper` e `aifs-ens/0p25/enfo`), mesmo byte-range do IFS — reusam os helpers do ECMWF, generalizados com um parâmetro **`model`** (`ecmwf_grib_url`/`ecmwf_index_url`/`fetch_index`/`_open_uvhgt200`/`_open_t850`/`_ens_mean_2d`). Alcance 6-horário até **15 dias**. Flags `RUN_AIFS`/`RUN_AIFS_ENS`. **O AIFS NÃO emite OLR** (sem `ttr`/`mtnlwrf`): o s34 ganhou suporte a **modelos sem OLR** (`has_olr`/`olr_fn=None` → pula download, anomalia e mapa de OLR), então o AIFS gera **4 dos 5 mapas** (Ks, RWS, WAF/Z200, T850). O AIFS-ENS usa `z` (geopotencial) convertido p/ altura (`hgt=z/g`), pois só o single traz `gh`. Validado end-to-end (AIFS gera os 4 mapas; `waf_olr` corretamente vazio).
- `app/src/uteis/downloaders_gefs_fcst200.py` + `scripts/s34_wave_guide_rossby_wave.py`: **horizonte estendido do GEFS (35 dias, subsazonal) + nº de pêntadas configurável**. O GEFS v12 **00Z** roda até **840h (35 dias)**; os demais ciclos (06/12/18Z) só até 384h (16 dias). O cap passou a ser **dependente do ciclo** (`_gefs_max_fhr`: 840 no 00Z, 384 senão) — antes era fixo em 384, ignorando os dias 17–35. Confirmado na fonte (NOMADS `geavg` pgrb2a 0.5°: arquivos até `f840`, 6-horários no trecho estendido; `u/v/HGT@200`, `TMP@850`, `ULWRF` todos presentes em f840). O nº de pêntadas fixas virou configurável com precedência **`N_PENTADAS_FIXAS` → `N_PENTADAS` (a mesma do s31/s32) → 3**: assim o `N_PENTADAS` já existente vale também no s34 (defina `N_PENTADAS_FIXAS` só para um valor exclusivo do s34). Use **7** com GEFS 00Z 35d. Para subsazonal: `RUN_GEFS=true`, `RODADA="00"`, `FORECAST_LEAD_DAYS=35`, `N_PENTADAS_FIXAS=7`. GFS (16d) e ECMWF (15d) inalterados — pêntadas além do alcance de cada modelo são puladas com aviso.
- `app/common/forecast_download.py` + downloaders GFS/GEFS/ECMWF: **download paralelo dos dias de previsão** (s34). Novo helper `download_days_parallel` roda o laço de dias num `ThreadPoolExecutor` limitado por **`DOWNLOAD_WORKERS`** (default 6) — download é I/O-bound, então o ganho é quase linear (**medido ~3,2× em 4 dias do ECMWF HRES 200 hPa**, 93,9s → 29,2s). Aplicado em GFS (200/OLR/T850), GEFS (200/OLR/T850) e ECMWF HRES (200/T850). Modelos e variáveis seguem em série (concorrência total ≤ `DOWNLOAD_WORKERS`, amigável ao NOMADS que limita conexões). Diferente do `parallel_process` genérico, o helper **propaga exceções** (um dia que falhe estoura o erro, não gera dados incompletos). Temporários do ECMWF passaram a ser **únicos por dia** (evita colisão entre threads). Não-paralelizados de propósito: ECMWF-ENS (já paraleliza os 50 membros) e ECMWF OLR (cache de `ttr` entre passos).
- `app/src/uteis/downloaders_ecmwf_ens.py` + `scripts/s34_wave_guide_rossby_wave.py`: **suporte ao ECMWF-ENS (média dos 50 membros)**. Novo modelo `ecmwf_ens` (flag `RUN_ECMWF_ENS`, default `false`), salvando em `Saida/ECMWF_ENS/`. O open data do ECMWF não expõe o controle nem a média prontas do ENS — só os **50 membros perturbados** (`enfo`/`ef`) — então a **média é computada por nós**, baixando cada membro por byte-range e mediando (análogo ao `geavg` do GEFS). **Pesado** (~50 downloads/campo/passo): membros baixados em paralelo (`ECMWF_ENS_WORKERS`, default 8, arquivos temporários únicos por thread); nº de membros configurável (`ECMWF_ENS_MEMBERS`, default 50). OLR usa a **linearidade da desacumulação**: `média(OLR) = desacumular(média(ttr))` (media-se ttr por passo, com cache, e desacumula-se). Helpers base do ECMWF (`ecmwf_grib_url`/`ecmwf_index_url`/`fetch_index`/`match_record`) generalizados para `stream`/`ftype`/`number`. Validado: média global de OLR ~225 W/m² (mais suave que o HRES, como esperado)
- `scripts/s34_wave_guide_rossby_wave.py`: **suporte ao modelo ECMWF (HRES determinístico)**. Terceiro modelo de previsão, habilitado por `RUN_ECMWF` (default `false`), salvando em `Saida/ECMWF/`. Fonte: **ECMWF Open Data** (`https://data.ecmwf.int/forecasts`, stream `oper`/HRES, IFS 0.25°, até 360h = 15 dias) — cobre as 3 pêntadas. Traz **todas** as variáveis do s34: `u/v/gh@200`, `t@850` e OLR. Observação importante: o **membro de controle do ENS não é exposto** no open data (apenas HRES determinístico e os 50 membros perturbados), por isso usa-se o HRES. `script_version='3.8'`
- `app/src/uteis/downloaders_ecmwf_fcst200.py`, `downloaders_ecmwf_olr.py`, `downloaders_ecmwf_tmp850.py`: novos downloaders **ECMWF HRES via Open Data por byte-range** (sem dependência extra, só `httpx`): lê o `.index` (uma linha JSON por campo, com `_offset`/`_length`) e baixa cada campo por `Range` no GRIB. O **OLR é derivado de `ttr`** (top net thermal radiation, acumulado J/m²) por **desacumulação entre passos sinóticos**: `OLR(S) = -(ttr(S) - ttr(S-6)) / 21600` (W/m²) — validado (média global ~228 W/m²). Mesma estrutura de saída (um NetCDF por dia válido) dos downloaders GFS/GEFS
- `scripts/s34_wave_guide_rossby_wave.py`: **suporte ao modelo GEFS + seleção de modelos por flags**. Em `MODE="forecast"`, os modelos passam a ser habilitados por **flags `RUN_GFS` / `RUN_GEFS`** (substituem o antigo `FORECAST_MODEL`): pode habilitar um ou ambos, e o s34 roda o pipeline completo para **cada modelo habilitado**, salvando em `Saida/<MODELO>/`. **GEFS = média do ensemble (`geavg`) 0.5°** — análogo direto ao GFS determinístico. A `main()` virou um laço sobre os modelos habilitados (`_enabled_models`), chamando o núcleo `_run_once(mode, forecast_model, logger)` (antiga `main`); despacho de downloaders via `_FCST_DOWNLOADERS`/`_MODEL_FLAGS`. `cache_params` ganhou `forecast_model`. `script_version='3.8'`
- `app/src/uteis/downloaders_gefs_fcst200.py`, `downloaders_gefs_olr.py`, `downloaders_gefs_tmp850.py`: novos downloaders **GEFS (previsão) via NOMADS Grib Filter** (`filter_gefs_atmos_0p50a.pl`, membro `geavg`, produto `pgrb2a` 0.5°), espelhando os do GFS — um NetCDF por dia válido com as horas sinóticas. As três variáveis (u/v/HGT@200, TMP@850, ULWRF topo) vêm do **mesmo arquivo pgrb2a**; reusam os parsers de GRIB dos downloaders GFS (`_open_gfs_*`) para não duplicar
- `scripts/s34_wave_guide_rossby_wave.py`: **nova modalidade de mapas por PÊNTADA fixa** — além da janela móvel, cada tipo de mapa (Ks, RWS, WAF/Z200, WAF/OLR, WAF/T850) ganha **3 pêntadas** (média de 5 dias em janelas **fixas e não móveis**) a partir do **dia seguinte à data da rodada**: P1 = dia+1..dia+5, P2 = dia+6..dia+10, P3 = dia+11..dia+15 (ex.: rodada dia 15 → P1 16–20, P2 21–25, P3 26–30). Arquivos salvos com **sufixo `pentadaX`** (`*_YYYYMMDD_YYYYMMDD_pentada1.png` etc.) nas mesmas subpastas por tipo. Referência de "data da rodada": `init0` em forecast, `DATA_INICIAL` em reanálise. Pêntada sem dados no período é pulada com aviso (forecast precisa de `FORECAST_LEAD_DAYS >= 15`). Novos helpers `_pentad_windows`, `_render_spatial_window` (corpo do loop fatorado, reutilizado pela janela móvel e pelas pêntadas); constantes `N_PENTADAS_FIXAS=3`/`PENTADA_DIAS=5` (nome distinto da setting `N_PENTADAS` do s31/s32, que conta pêntadas para trás de `DATA_FINAL`). `script_version='3.6'`
- `scripts/s34_wave_guide_rossby_wave.py`: **`FORECAST_INIT` aceita data de rodada antiga em formato ISO + vazio = data atual**. Deixar a variável **vazia (`""`)** ou `"latest"` usa a **data atual** (ciclo mais recente); para uma rodada antiga, basta digitar a **data no formato `YYYY-MM-DD`** (ex.: `2026-06-10`) no `settings.local.toml` — a hora do ciclo vem da `RODADA`. Mantida a compatibilidade com o timestamp `YYYYMMDDHH`. Novo helper `_parse_forecast_init`; default do `settings.toml` mudou de `"latest"` para `""`
- `scripts/s34_wave_guide_rossby_wave.py`: **mapa de T850 (estilo s10) por janela** — 5º mapa por janela/área (`waf_tmp850/waf_tmp850_anom_*.png`): **anomalia de T850 sombreada (paleta do projeto) + contornos pretos de Z200 + WAF**, em °C (GFS em Kelvin é normalizado p/ °C; `_to_celsius`). Forecast = GFS `TMP@850`; reanálise = ERA5/GDAS T850; climatologia diária NCEP `air.day.ltm` 850 (`clim_t850_daily`). `script_version='3.5'`
- `app/src/uteis/downloaders_gfs_tmp850.py`: downloader GFS de temperatura 850 hPa (`TMP@850`) via NOMADS — `ensure_gfs_tmp850_fcst_for_period()`
- `app/src/uteis/clim_diaria_uv200_ltm.py`: nova `clim_t850_daily(dates)` (LTM diária NCEP `air.day.ltm` em 850)
- `scripts/s34_wave_guide_rossby_wave.py`: **mapa de OLR (estilo s08) + saída organizada por modelo/tipo + título de previsão**. (1) Novo 4º mapa por janela/área (`waf_olr/waf_olr_anom_*.png`): **anomalia de OLR sombreada (BrBG_r) + contornos pretos de Z200 + WAF**. (2) Saída reorganizada em **`Saida/<MODELO>/s34_WAVEGUIDE_ROSSBY/<tipo>/`** (MODELO = `GFS` em previsão, `REANALISE` em reanálise; subpastas `ks_waveguide/`, `fontes_rws/`, `waf_z200/`, `waf_olr/`, `hovmoller/`). (3) Em previsão, **linha extra no título** com modelo/rodada/data (e, no ensemble, as N inicializações). `script_version='3.4'`
- `app/src/uteis/clim_diaria_olr.py`: OLR diário (CPC Blended OLR 2.5°, `olr.day.mean.nc`) — `clim_olr_daily()` (LTM por dia-do-ano calculada do **mean**, base 1991-2020) e `olr_obs_daily()` (observado). Anomalia = OLR(GFS/obs) − clim do mean, em vez do arquivo de anomalia pronto do PSL
- `app/src/uteis/downloaders_gfs_olr.py`: downloader GFS de OLR (`ULWRF` no topo da atmosfera) via NOMADS, um NetCDF/dia válido — `ensure_gfs_olr_fcst_for_period()`
- `scripts/s34_wave_guide_rossby_wave.py`: **modo PREVISÃO + média móvel + lagged ensemble**. Setting `MODE = "reanalysis" | "forecast"`. Em `forecast`: usa **GFS** via `RODADA` (00/06/12/18 UTC) + `NUM_RODADA` (últimas N rodadas diárias dessa hora) + `FORECAST_LEAD_DAYS`; as N rodadas viram um **lagged ensemble** (alinhadas por tempo válido e mediadas — `_lagged_ensemble_mean`). **Mapas espaciais (Ks/RWS/WAF/Z200) agora por JANELA MÓVEL deslizante** de `MOV_AVG_DAYS` dias (default 5) → sequência D1-5, D2-6, … por área (filenames com a janela), mostrando o padrão evoluir; o **Hovmöller fica fora da média móvel** (rodada mais recente, série diária completa). Climatologia **unificada na LTM diária NCEP** para u/v **e hgt** (nova `clim_hgt200_daily`) — por dia-do-ano, deslizável e consistente entre todos os campos (substitui a composite PSL por intervalo no WAF/Z200; removido `_compute_waf200`/PSL-scrape do s34). Novos helpers `_windows`, `_resolve_run_inits`, `_waf_from_means`, `_window_spatial_fields`, `_plot_spatial_window`, `_daily_hgt200_on_grid`. Adicionado **3º mapa por janela/área no estilo s07** (`waf_z200_anom_{area}_{janela}.png`): **anomalia de Z200 sombreada (paleta do projeto) + vetores de WAF + contornos brancos**, padrão s07 de features/cbar/ticks. `RODADA` é string de 2 dígitos (`"00"/"06"/"12"/"18"`, validada). `script_version='3.3'`
- `app/src/uteis/clim_diaria_uv200_ltm.py`: nova `clim_hgt200_daily(dates)` — LTM diária NCEP de altura geopotencial 200 hPa (mesma fonte/base/grade das LTMs de u/v200), para anomalia de hgt deslizável por janela e consistente com u/v
- `app/src/uteis/downloaders_gfs_fcst200.py`: novo downloader **GFS 0.25° (previsão)** via NOMADS Grib Filter (`filter_gfs_0p25.pl`) — baixa `UGRD/VGRD/HGT` em 200 mb numa única chamada por passo de previsão e monta **um NetCDF por dia válido** com as horas sinóticas (00/06/12/18), mesma estrutura dos arquivos GDAS (HGT já em metros). Função `ensure_gfs_fcst200_for_period(init, lead_hours)`
- `scripts/s34_wave_guide_rossby_wave.py`: novo script — **guia de onda de Rossby (waveguide)** seguindo Hoskins & Ambrizzi (1993). Calcula o **número de onda estacionário Ks** em 200 hPa a partir do vento zonal médio do período (formulação de Mercator: `U_M = u/cosφ`, `β_M = 2Ω·cos²φ/a − ∂²U_M/∂y²`, `Ks = a·√(β_M/U_M)`) e sobrepõe a **anomalia de vento meridional v'200** (a onda real, seguindo a literatura de circumglobal waveguide — Branstator 2002; Ding & Wang 2005). Gera (1) **mapas por área** (padronização idêntica ao s07 — `LST_AREAS_S34`, projeções/`central_longitude`, pilha de features, `_configure_gridlines`, posição das barras de cor e tamanho dos ticks; inclui o polar `globo_3d`): Ks sombreado + isolinhas + máscara/hachura das regiões evanescentes (leste/`β_M<0`, sem onda estacionária possível) + linha crítica `U=0` + isolinhas de v'200 médio; (2) **dois Hovmöllers** de v'200, um por corrente de jato do HS — **subtropical** (`-45..-20`, guia de trens forçados pelos trópicos: PSA/ENSO/MJO) e **polar/lat. médias** (`-60..-40`, variabilidade extratropical: SAM/bloqueios) — cada um em faixa estreita (~20°, padrão da literatura), com eixo de longitude **0–360 centrado no Pacífico** (data line ao centro) e rótulos W/E; bandas verticais = onda estacionária, inclinadas = propagante. No mapa, as regiões evanescentes (Ks imaginário) ficam **em branco** (sem máscara/hachura, rendering canônico) com a linha `U=0` delimitando o duto. **A onda real no mapa é mostrada por anomalia de Z200 (isolinhas) + vetores de WAF (Takaya & Nakamura 2001)** — pareamento clássico waveguide+fluxo de atividade de onda; o WAF reusa a maquinaria do s07 (`tnflux.tnf2d` + helpers de `plot_rossby_waf`) em 200 hPa, com climatologia PSL (`clim_PSL_geop200`/`clim_PSL_wnd200`). Pipeline ERA5/GDAS híbrido; settings `LST_AREAS_S34` (áreas dos mapas) e `WGUIDE_HOV_SUBTROPICAL_*`/`WGUIDE_HOV_POLAR_*`/`WGUIDE_SMOOTH_DEG`; registrado no CLI como `RUN_S34`; `script_version='2.0'`
- `scripts/s34_wave_guide_rossby_wave.py`: **mapa companheiro de Fonte de Onda de Rossby (RWS)** por área (`fontes_rws_200hpa_{area}.png`), seguindo Sardeshmukh & Hoskins (1988): `RWS = -ζ_a·D − v_χ·∇ζ_a`. RWS **anômala** (período − climatologia) sombreada na paleta `BrBG` do s05 — **verde (RWS>0) = fonte anticiclônica no HS** (lança o trem de ondas), **marrom (RWS<0) = fonte ciclônica** — com vetores de **vento divergente anômalo** e máscara `|lat|<10°`. Reusa a maquinaria de divergência/potencial de velocidade do projeto (sem `windspharm`); o estado básico é suavizado (gaussiana, padrão 6°) antes das 2as derivadas. NetCDF ganhou `rws_anom_200`; `script_version='3.0'`
- `app/src/uteis/rossby_wave_source.py`: novo utilitário NumPy — `rossby_wave_source(u, v, lat, lon)` calcula a RWS (Sardeshmukh-Hoskins 1988) e o vento divergente associado, reusando `_compute_divergence` (`plot_chi200`) e `chi_from_wind`/`div_wind_from_chi` (`chi200_intrasazonal`); inclui `relative_vorticity` esférica
- `app/src/uteis/downloaders_hgt200_ERA5.py`, `app/src/uteis/downloaders_gdas_hgt200.py`: novos downloaders de altura geopotencial 200 hPa (ERA5 via CDS mensal NetCDF, convertendo `z`→`hgt` em metros; GDAS via NOMADS Grib Filter), espelhando os equivalentes de 250 hPa — usados pelo s34 para o cálculo do WAF
- `app/src/uteis/clim_PSL_geop200.py`, `app/src/uteis/clim_PSL_wnd200.py`: climatologias PSL de geopotencial e u/v em 200 hPa (composites diários, cache por MM-DD), análogas às de 250 hPa
- `app/src/uteis/stationary_wavenumber.py`: novo utilitário puro NumPy — função `stationary_wavenumber(u_mean, lat, lon, smooth_deg, mask_tropics_deg)` que calcula Ks (Hoskins & Ambrizzi 1993) a partir do vento zonal médio básico, retornando `ks`, `ks2` (com sinal, base da máscara evanescente), `β_M` e `U_M`. Independente da fonte do vento (reanálise **ou previsão**) — projetado para reuso futuro em prognóstico; suaviza o escoamento básico (boxcar separável) antes da 2ª derivada meridional e mascara `|lat| < mask_tropics_deg`
- `scripts/s31_chi200_intrasazonal.py`: terceiro Hovmöller — CHI200 intrasazonal filtrado (shading) + **vento zonal 850 hPa intrasazonal** em isolinhas (azul=negativo/leste anômalo, vermelho=positivo/oeste anômalo); vento 850 baixado via ERA5+GDAS (mesmo pipeline do u200) na grade 2.5°; **anomalia verdadeira** `u850 − LTM_diária(dia-do-ano)` usando a mesma climatologia NCEP 1991-2020 do u200 (consistência total com o chi200), seguida de running mean `JANELA_MEDIA_MOVEL` + Lanczos `LANCZOS_PERIOD_MIN`–`LANCZOS_PERIOD_MAX`; datas de chi e u850 alinhadas por interseção; novo arquivo `chi200_u850_hovmoller_com_filtro.png`; `LEVELS_U850=[-6,-4,-2,2,4,6]`; `script_version='2.10'`
- `app/src/uteis/clim_diaria_uv200_ltm.py`: generalizado para qualquer nível de pressão (núcleo `_ensure_local_ltm`/`_select_by_doy` compartilhado); nova função `clim_u850_daily()` — LTM diária de u zonal em 850 hPa da mesma fonte/base/grade da LTM de 200 hPa; `clim_uv200_daily()` mantém a interface anterior
- `scripts/s33_homoller_free_ssta_wnd_zonal850.py`: novo script — Hovmöller de **caixa livre** (versão parametrizável do s27): a faixa de latitude e o intervalo de longitude da caixa são lidos do `settings.local.toml` (`HOV_FREE_LAT_MIN/MAX`, `HOV_FREE_LON_MIN/MAX`), permitindo selecionar qualquer caixa do mundo (inclusive cruzando o antimeridiano via `LON_MIN > LON_MAX`); gera o Hovmöller (TSM OISSTv2 shaded + isolinhas de anomalia de U850) — latitudes no **título**, longitudes no **eixo X** — e o mapa SSTA + vetores de vento 850 hPa recortado na caixa (extensão = caixa + `HOV_FREE_MAP_PAD`, com a caixa desenhada como retângulo); defaults reproduzem o cinturão ENSO do s27 (5S–5N, 160E–80W); settings `HOV_FREE_NOME` (rótulo em título/arquivo) e `HOV_FREE_MAP_PAD` (padding do mapa); registrado no CLI como `RUN_S33`; `script_version='1.0'`
- `app/src/uteis/downloaders_wind850.py`, `app/src/uteis/downloaders_gdas_uv850.py`: novos downloaders ERA5+GDAS para u/v 850 hPa — mesma estrutura dos equivalentes de 200 hPa (s31)
- `scripts/s32_olr_intrasazonal.py`: terceira versão de produtos — Hovmöller de OLR filtrada (shading) + u850 intrasazonal (isolinhas azuis/vermelhas para negativo/positivo); mapas de pêntada e mapa do período com OLR shaded + vetores de vento 850 hPa; vento baixado via ERA5+GDAS (mesmo pipeline do s31), filtrado com running mean + Lanczos 20–90d; `script_version='2.0'`
- `app/src/uteis/chi200_intrasazonal.py`: nova função `lanczos_bandpass(series, period_min, period_max, n)` — aplica filtro passa-banda de Lanczos no eixo temporal de qualquer array (T, lat, lon); reutilizada pelo s31 e exportável para outros scripts
- `scripts/s31_chi200_intrasazonal.py`: filtro Lanczos 20–90d adicionado após o running mean — produz **duas versões** de todos os produtos (mapas de pêntada, Hovmöller, período): `com_filtro` (banda MJO 20–90d) e `sem_filtro` (apenas running mean removido); WW isolines calculadas sobre o campo filtrado para `com_filtro` e sobre o original para `sem_filtro`; `start_dl` estendido por `lanczos_n` dias extras; `script_version='2.7'`
- `scripts/s32_olr_intrasazonal.py`: novo script — OLR intrasazonal (CPC Blended OLR 2.5°, PSL/NOAA); filtragem em dois passos: (1) remove interanual via `olr_intra = olr_anom - média_móvel(120d)`, (2) aplica filtro passa-banda de Lanczos 20–90 dias (`_lanczos_weights` + `scipy.ndimage.convolve1d`, modo `nearest`) para isolar a banda MJO; gera mapas de pêntada, Hovmöller lon×tempo e mapa do período em **duas versões** — `com_filtro` (banda 20–90d) e `sem_filtro` (apenas running mean removido); nomes de arquivo e títulos distinguem as versões; settings `LANCZOS_N` (padrão 60), `LANCZOS_PERIOD_MIN` (20), `LANCZOS_PERIOD_MAX` (90); `script_version='1.3'`
- `docs/metodologia_s31_chi200_intrasazonal.md`: documentação completa da metodologia do s31 — equivalência com o método operacional CPC/NOAA, passo a passo das 6 etapas e comparação com o método Wheeler-Weickmann
- `scripts/s31_chi200_intrasazonal.py`: isolinhas Wheeler-Weickmann sobrepostas a todos os mapas espaciais: **MJO** (preto sólido/tracejado, k=1–9, 30–90 dias) e **onda de Kelvin** (azul sólido/tracejado, k=1–14, 2.5–30 dias); linestyles automáticos — sólido para chi<0 (favorecimento), tracejado para chi>0 (supressão); filtro via `ww_filter_chi_modes` sobre chi_intra; `WW_LEVELS` recalibrados em `[-40, -20, 20, 40]` (×10⁵) após correção do sinal do filtro; `script_version='1.7'`
- `scripts/s31_chi200_intrasazonal.py`: vetores de vento divergente intrasazonal (∂χ/∂x, ∂χ/∂y) sobrepostos em todos os mapas espaciais (pentadas e período); divisas de países (`BORDERS 50m`) e estados/províncias (`STATES 50m`, inclui estados brasileiros) adicionados; `script_version='1.3'`
- `scripts/s31_chi200_intrasazonal.py`, `app/settings/settings.toml`, `settings.local.example.toml`: **nova setting `WW_EXTRA_JANELA` (padrão 0)** — estende a janela de download especificamente para o filtro Wheeler-Weickmann; ao configurar `WW_EXTRA_JANELA = 270` e baixar ERA5 de Jan–Ago/2025 (~272 MB), chi_intra aumenta de 122d para ~420d, dando 28 modos espectrais Kelvin k=2 (vs 8 modos atualmente) e separação MJO/Kelvin confiável; documentado em `settings.local.example.toml`; `script_version='2.6'`
- `app/src/uteis/chi200_intrasazonal.py`: **banda Kelvin corrigida para f=1/30–0.10 CPD (10–30 dias, k=1–2)** — a propagação Kelvin visível no NCICS é ~10 m/s; para k=2 isso implica f≈0.043 CPD (período 23d); com T=122d, f_min=1/30 captura efetivamente a partir do bin 5 = f_real=0.041 CPD, cobrindo exatamente essa propagação; banda anterior (f=0.05–0.15) começava no bin 7 (f=0.057), perdendo o sinal dominante; `script_version='2.5'`
- `app/src/uteis/chi200_intrasazonal.py`: **banda Kelvin restrita a k=1–2 (removido k=3)** — k=3 criava círculos de ~60° de longitude por interferência construtiva de 3 componentes; NCICS mostra padrão dominante k=2 (~90° de longitude); com k=1–2 e f=0.05–0.15 CPD a escala espacial dos contornos passa a k=2 (~90°), compatível com o produto de referência; `WW_LEVELS` ajustados para `[-30,-15,15,30]` após nova calibração (Kelvin max esperado ~30–50×10⁵); `script_version='2.4'`
- `app/src/uteis/chi200_intrasazonal.py`: **banda Kelvin estreitada para f=0.05–0.15 CPD (7–20 dias, 36 modos = igual MJO com T=122d)** — banda anterior (f=0.033–0.40, 132 modos) capturava ~80% da energia total de chi200, dando Kelvin max=104×10⁵ > total chi (104>76), fisicamente impossível e gerando blobs globais sem relação com ondas de Kelvin; nova faixa alinhada à velocidade de fase da onda de Kelvin convectivamente acoplada (c≈15–25 m/s → f≈0.05–0.10 CPD para k=1–3); `script_version='2.3'` no s31
- `app/src/uteis/chi200_intrasazonal.py`: **Kelvin agora calculado via média tropical ±10° + extensão Gaussiana (escala 20°)** — `ww_filter_chi_modes` recebe `lat` como parâmetro obrigatório; para MJO, o filtro latitude-a-latitude é mantido (funciona bem); para Kelvin, a média chi200 da faixa ±10° é filtrada em k=1–3 / f=1/30–2/5 CPD e então extendida meridionalmente via `exp(-0.5*(lat/20)²)`; motivação: com T~160d o filtro latitude-a-latitude captura ciclones extratropicais e ondas de Rossby com k=1–3 que contaminavam as isolinas de Kelvin com "blobs" em 30–40°S; a abordagem de média tropical garante que o sinal seja domindado pela dinâmica equatorial (Kelvin é modo confinado ao equador), e a Gaussiana reproduz o confinamento meridional esperado; `script_version='2.2'` no s31
- `app/src/uteis/chi200_intrasazonal.py`: nova função `ww_filter_chi_modes`; **bug corrigido em `_ww_filter_2d`** — convenção FFT numpy/scipy (IFFT usa exp(+2πi(ft+kl))) faz ondas LESTE terem energia em (freq<0, k>0), não (freq>0, k>0); filtro anterior capturava propagação OESTE; corrigido para manter (freq<0, k>0) + conjugado (freq>0, k<0), recuperando amplitude ~4× maior e propagação correta; `ww_filter_modes`, `chi_from_wind`, `div_wind_from_chi` mantidas

### Corrigido

- `app/src/uteis/downloaders_wind200.py`: `NetCDF: HDF error` / `Can't open HDF5 attribute` ao baixar ERA5 U/V 200 hPa em paralelo (`ensure_era5_uv200_for_period`, `max_parallel=2`) — a lib HDF5 não é thread-safe e a validação/merge concorrentes de meses corrompiam o I/O e abortavam o s29. Adicionado lock de módulo (`_HDF5_IO_LOCK`) serializando todo I/O netCDF (leitura em `_extract_time_index_from_file` e merge `to_netcdf`); downloads de rede seguem paralelos. Corrigido também bug latente: `merged.load()` antes de fechar os datasets-fonte (o merge lazy referenciava arquivos já fechados)
- `scripts/s27_hovmoller_enso.py` e `scripts/s28_hovmoller_iod.py`: `AlignmentError: cannot align objects with join='override'` ao concatenar ERA5 (181 lat, 1°) com GDAS (721 lat, 0.25°) — grades com tamanhos diferentes; agora interpola arquivos GDAS para a grade de referência ERA5 antes do `xr.concat`
- `scripts/s29_walker_cell_mundo.py`: colorbar SSTA agora usa colorscale idêntico ao piso (`sst_colors` mapeado linearmente de 0→1 com `cmin=-5/cmax=+5`), eliminando a dessincronização de cores causada pelo range estendido da versão anterior; `script_version='5.43'`
- `scripts/s16_wnd250_zonal_anom_div.py`: remove contornos (`ax.contour`) sobrepostos ao shaded de magnitude de vento 250 hPa em todas as modalidades de plotagem; `script_version='1.2'`
- `scripts/s16_wnd250_zonal_anom_div.py`: paleta do modo `_pos` (anomalia positiva) alterada de amarelos/laranjas/vermelhos para nova paleta azul-rosa-cinza (`CMAP_POS_COLORS`); `script_version='1.3'`
- `scripts/s16_wnd250_zonal_anom_div.py`: continentes pintados com `#d4d4d4` via `cfeature.LAND` (zorder=1, abaixo do shaded para não cobrir a anomalia de vento sobre terra); `script_version='1.5'`
- `scripts/s16_wnd250_zonal_anom_div.py`: adicionado modo `_mag` (4ª figura por área): magnitude vento 250 hPa (sqrt(u²+v²)) + quiver divergente onde chi200<0 + anomalia OLR negativa em verde; nova utilidade `app/src/uteis/plot_wnd_speed_250.py`; `script_version='1.6'`
- `scripts/s16_wnd250_zonal_anom_div.py`: modo `_mag` refinado — `CMAP_MAG_COLORS` alterado para paleta azul-rosa-cinza idêntica ao modo `_pos`; OLR translúcido (`alpha=0.5`); adicionadas isolinhas de altura geopotencial 250 hPa (Z250) com rótulos em "placa branca" — 9960/10200 m em azul (lw=2.0) e 10440/10680 m em vermelho (lw=2.2); novos utilitários `app/src/uteis/downloaders_z250_era5.py` e `app/src/uteis/plot_z250_mean.py`; `script_version='1.7'`
- `scripts/s16_wnd250_zonal_anom_div.py`: modo `_mag` — `LEVELS_MAG` agora começa em 25 m/s (era 10); velocidades ≤24 m/s ficam transparentes (sem preenchimento via `extend='max'`); paleta de cor aplicada somente a partir de 25 m/s; `script_version='1.8'`
- `scripts/s16_wnd250_zonal_anom_div.py`: logo da Ampere com `zorder=1100` (era 500) para ficar acima das isolinhas de altura geopotencial (`zorder=900`) e do retângulo da borda (`zorder=1000`), que antes cortavam o logo

### Modificado

- **Anomalia de TSM calculada localmente** nos scripts `s11`, `s12`, `s14`, `s24` e `s29`: deixam de baixar o `sst.day.anom.{ano}.nc` pronto e passam a baixar a SST absoluta (`sst.day.mean.{ano}.nc`, igual ao s13), subtraindo a climatologia diária OISST (LTM 1991-2020, `Entrada/sst.day.mean.ltm.1991-2020.nc`) **recortada no mesmo período** dia-a-dia (mapeamento mês/dia → dia-do-ano, média ponderada por ocorrência, 29/fev usa 28/fev). Lógica centralizada no novo módulo `app/src/uteis/ssta_climatologia.py` (`clim_mean_array`), validada como matematicamente idêntica à anomalia dia-a-dia ingênua. Novo setting `FILE_CLIMATOLOGIA_SST`/`REMOTE_CLIMATOLOGIA_SST`; climatologia registrada como `required_file` de s11/s12/s14/s24 (s29 degrada sem SSTA). `script_version`: s11→`2.0`, s12→`2.2`, s14→`2.0`, s24→`2.0`, s29→`5.44`. **Atenção:** os mapas de anomalia agora usam a base 1991-2020 e diferem dos antigos `sst.day.anom` da NOAA (base climatológica distinta)
- `scripts/s13_sst_todas_areas_vento_medio_1000.py`: OLR agora é obtido via **OPeNDAP** (THREDDS/PSL) baixando só a fatia de tempo do período (~4 s), em vez de re-baixar o arquivo monolítico `olr.day.anom.nc` (~450 MB, série diária completa desde 1991) toda vez que `DATA_FINAL` era recente. Nova função `_load_olr_dataset` com fallback automático para o download completo via aria2 caso o OPeNDAP esteja indisponível; erro de cobertura/gap não aciona fallback (mesma fonte)
- `scripts/s26_chi200_anom_div_fluxo_rossby_wave.py`: cor das setas do vento divergente alterada de `paleturquoise` para `white`
- `app/src/uteis/downloaders_gdas_omega.py`: fix "the new name 'time' conflicts" — arquivos GDAS recentes têm `time` (referência) e `valid_time` simultaneamente; agora descarta `time` antes de renomear `valid_time` → `time`

### Adicionado

- `scripts/s31_chi200_intrasazonal.py`: novo script — **CHI200 intrasazonal (MJO)** pelo método operacional do CPC (remoção de média móvel). Anomalia diária = u/v200 − LTM diária (remove ciclo sazonal); intrasazonal(t) = anom(t) − média dos 120 dias anteriores (remove interanual); χ200 por dia via inversão de Poisson (reusa o solver do `plot_chi200`). Gera **mapas de pêntada** + **Hovmöller** (faixa equatorial) + **mapa do período** (`DATA_INICIAL`/`DATA_FINAL` = janela que se quer ver). Novos módulos: `app/src/uteis/clim_diaria_uv200_ltm.py` (LTM diária NCEP u/v 200 via OPeNDAP, nível 200, ~15 MB, carregada em chunks p/ contornar instabilidade do THREDDS) e `app/src/uteis/chi200_intrasazonal.py` (filtro + χ por dia). Settings `JANELA_MEDIA_MOVEL`, `N_PENTADAS`, `HOVMOLLER_DIAS`, `FAIXA_HOVMOLLER`; CLI `RUN_S31`
- `app/src/uteis/indices_climaticos_tsm.py`: novo módulo com a fonte única dos boxes e índices climáticos de TSM (IOD, Nino 1+2/3/3.4/4, AMO, TNA, TSA, SAD, PDO) — `calcula_indice_pdo` + `desenha_boxes_indices`. O s24 foi refatorado para usá-lo (remoção do bloco inline e das funções locais de PDO); os rótulos passaram a usar coordenadas reais (`transform=PlateCarree`) para funcionar em qualquer `central_longitude`. A área `globo` do s12 agora replica todos os boxes e índices do s24 (índice PDO calculado em `main` só quando `globo` é plotado; `EOF1.csv` vira `required_file` do s12) e usa a **mesma projeção do s24** (`central_longitude=220`, override local no worker sem alterar o settings.json) para acomodar os rótulos do Atlântico (TSA/SAD) sem corte. Boxes em `zorder` alto (`_ZORDER_BOX=1100`, Nino 3.4=1110, rótulos=1200) para nunca ficarem sob as linhas de continente; `script_version` s12→`2.4`
- `scripts/s12_ssta_todas_areas_vento850_anom.py`: na área `enso`, os nomes dos boxes agora ficam centralizados no eixo x sobre o centro de cada box (derivado de `lst_boxes` do settings.json) e exibem a **anomalia média de TSM do próprio box** ao lado do nome (ex: `Nino 4 = 1.2°C`). Média calculada por box na sua região real lon/lat (regiões canônicas NOAA/CPC via `ENSO_BOXES`/`_box_mean`, com wrap na linha de data para o Nino 4); cor do texto mantida igual à cor do box; texto com `zorder=400` (antes ficava escondido sob o shading da TSM, zorder=5); `script_version='1.2'`
- `scripts/s30_sst_todas_areas.py`: novo script — média de TSM (OISSTv2/NOAA) por área geográfica, **sem vento e sem OLR** (cópia enxuta do s13). Para a área `enso`, desenha os boxes do ENSO (Nino 1+2, Nino 3, Nino 3.4, Nino 4) e escreve ao lado de cada um a TSM absoluta média do box (`Nino 3.4 = 27.3°C`), com as regiões canônicas NOAA/CPC iguais às do s24 (`ENSO_BOXES` + `_box_mean`, com tratamento de wrap na linha de data para o Nino 4); registrado no CLI (`RUN_S30`); saída em `Saida/s30_SST_TODAS_AREAS/`
- `scripts/s29_walker_cell_mundo.py`: v5.37 — `WIND_VEC_LAT_MAX`: 30° → 80° (teste — vetores U/V 850 hPa cobrem ±80°); `script_version='5.37'`
- `scripts/s29_walker_cell_mundo.py`: v5.36 — `_build_land_mesh3d`: reescrita com Delaunay + grade interior 1.5° por polígono (pontos dentro do polígono a cada 1.5°); elimina slivers (aspect ratio ~5:1 vs 300:1 anterior); pula Antártida (span >350°); `simplify(0.3)` na borda; filtro antimeridiano 90°; borda do fill agora coincide com as linhas de costa 50m; `script_version='5.36'`
- `scripts/s29_walker_cell_mundo.py`: v5.35 — `_build_cartopy_land_mask`: muda resolução 110m→50m (mesma fonte das linhas Scatter3d) para alinhar borda do preenchimento LAND_SENTINEL com as linhas pretas; `script_version='5.35'`
- `scripts/s29_walker_cell_mundo.py`: v5.34 — remove `go.Mesh3d` dos continentes (triângulos fan com aspect ratio ~300:1 geravam arestas visíveis como linhas retas no WebGL); fill retorna ao LAND_SENTINEL no ocean Surface; `_extract_lines_360`: insere None onde longitures consecutivas saltam >180° em 0–360 (fix para cruzamento do meridiano 0° gerando linha reta atravessando o mapa); `script_version='5.34'`
- `scripts/s29_walker_cell_mundo.py`: v5.33 — `_build_land_mesh3d`: substitui `scipy.spatial.Delaunay` por **fan triangulation** (centroide→aresta polígono); elimina slivers de alta razão de aspecto que cruzavam o mapa como linhas retas; usa `representative_point()` como centroide interior garantido; remove dependência de `scipy.spatial`; `script_version='5.33'`
- `scripts/s29_walker_cell_mundo.py`: v5.32 — `_build_land_mesh3d`: adiciona teste 4-pontos (centróide + 3 pontos médios de aresta via `MplPath.contains_points`) para sliver triangles em concavidades (ex: Golfo da Guiné); `script_version='5.32'`
- `scripts/s29_walker_cell_mundo.py`: v5.31 — `_build_land_mesh3d`: adiciona filtro antimeridiano (`lons_360.max()-lons_360.min() > 45`); corrige import `plotly.graph_objects as go` dentro da função (erro `go is not defined`); `script_version='5.31'`
- `scripts/s29_walker_cell_mundo.py`: v5.30 — refatora `_build_land_mesh3d`: substitui `shapely.ops.triangulate` (falhava silenciosamente) por `scipy.spatial.Delaunay` (mais robusto); adiciona `part.buffer(0)` para corrigir auto-interseções antes de simplificar; usa `poly_buf.contains(Point)` com buffer de 0.05° para não excluir triângulos limítrofes; volta ao LAND_SENTINEL no ocean Surface (sem z=NaN que gerava artefato ciano escalonado); Mesh3d em z=FLOOR_Z-10 (separação de profundidade maior)
- `scripts/s29_walker_cell_mundo.py`: v5.29 — resolve z-fighting definitivo entre ocean Surface e land Mesh3d: ocean Surface agora usa `z=NaN` para células de terra (buracos geométricos — Plotly não renderiza), Mesh3d 50m preenche esses buracos em z=FLOOR_Z-5 sem competição de profundidade; colorscale do oceano simplificado para apenas cores SST (sem LAND_SENTINEL); `_build_floor_arrays` passa a retornar `land_mask_bool`; log do número de triângulos gerados no Mesh3d
- `scripts/s29_walker_cell_mundo.py`: v5.28 — unifica fonte do fill e das linhas: `_build_land_mesh3d` migrado de 110m para 50m Natural Earth com `simplify(0.1)`; agora fill (Mesh3d) e linhas de costa (Scatter3d) partem do mesmo shapefile e coincidem; todas as linhas (costa, fronteiras, estados) alteradas para preto `rgba(0,0,0,1.0)`
- `scripts/s29_walker_cell_mundo.py`: v5.27 — substitui shading rasterizado dos continentes por `go.Mesh3d` com triangulação Delaunay dos polígonos Natural Earth 110m (`_build_land_mesh3d`); bordas do continente whitesmoke agora seguem exatamente os polígonos vetoriais (sem efeito "degrau" de grade); z-stack: oceano z=FLOOR_Z(1050) → continente mesh z=FLOOR_Z-5(1045) → linhas z=FLOOR_Z-20(1030)
- `scripts/s29_walker_cell_mundo.py`: v5.26 — grade do piso refinada de 0.5° para 0.25° (resolução nativa OISSTv2), reduzindo o efeito "degrau" na borda da máscara de terra rasterizada; HTML mais pesado (~4× mais vértices na superfície do piso)
- `scripts/s29_walker_cell_mundo.py`: v5.25 — linhas de costa/fronteiras/estados movidas para `LINE_Z = FLOOR_Z - 20` (reduz z-fighting) e coloridas de branco (`rgba(255,255,255,1)` costa, tons de cinza claro para fronteiras/estados) para máxima visibilidade tanto sobre oceano colorido quanto sobre continentes whitesmoke; largura da costa aumentada para 2
- `scripts/s29_walker_cell_mundo.py`: v5.24 — corrige definitivamente continentes azuis: (1) substitui máscara baseada em NaN do OISSTv2 por máscara vetorial com polígonos Natural Earth 110m via `_build_cartopy_land_mask()` (mesma fonte que `cfeature.LAND` do cartopy) — cobre células costeiras com SST extrapolado que OISSTv2 não marca como NaN; (2) aumenta zona sentinela de 0.17% para 50% do colorscale (`LAND_SENTINEL = sst_cmin - sst_range`), eliminando falha de quantização do Plotly onde a banda anterior era menor que 1 passo de 256 cores
- `scripts/s29_walker_cell_mundo.py`: v5.23 — cópia automática do HTML gerado para `C:\Users\Pichau\Desktop\walker_cell_s29.html` (via `/mnt/c/...`) ao final da renderização em ambiente WSL; elimina a necessidade de copiar manualmente após cada execução
- `scripts/s29_walker_cell_mundo.py`: v5.22 — abordagem definitiva: substitui duas superfícies (continente+oceano) por UMA superfície única sem NaN; terra recebe valor sentinela `sst_cmin-0.01` e colorscale customizado mapeia [LAND_SENTINEL, sst_cmin) → `#f5f5f5` (whitesmoke) e [sst_cmin, sst_cmax] → cores SST normais; elimina completamente o problema de NaN rendering do Plotly 6; remove trace separado de continentes
- `scripts/s29_walker_cell_mundo.py`: v5.21 — solução definitiva para continentes azuis: usa `z=NaN` onde há terra na superfície do oceano (`z_ocean = np.where(np.isnan(ocean_sst), np.nan, FLOOR_Z)`); em Plotly, NaN no z cria **buracos geométricos** na malha da superfície — o oceano não renderiza sobre a terra, e o trace dos continentes (whitesmoke, z=FLOOR_Z) fica visível nesses buracos; continent trace adicionado primeiro, ocean trace adicionado depois com z_ocean; linhas de costa/fronteiras/estados mantidas em FLOOR_Z-1
- `scripts/s29_walker_cell_mundo.py`: v5.20 — corrige causa raiz dos continentes azuis: dataset OISSTv2 usa fill values (-9999, 1e20) nas áreas terrestres em vez de NaN; `np.isnan()` não os detectava, `land_mask` ficava todo NaN e o trace do oceano pintava a terra com cor azul (mínimo da escala SST); fix: mascarar `|sst_2d| > 50` após interpolação + log de diagnóstico `n_land`; trace dos continentes mantido em z=FLOOR_Z-0.5 (após oceano) para garantia adicional
- `scripts/s29_walker_cell_mundo.py`: v5.19 — corrige continentes azuis em Plotly 6: em Plotly 6, NaN no `surfacecolor` do trace do oceano pinta áreas terrestres com a cor mínima da escala SST (azul escuro); solução: (1) trace do oceano adicionado primeiro em z=FLOOR_Z=1050 hPa, (2) trace dos continentes adicionado depois em z=FLOOR_Z-0.5=1049.5 hPa para vencer depth buffer independente de rendering order, (3) linhas de costa/fronteiras/estados em z=FLOOR_Z-1.0=1049 hPa para ficarem acima dos continentes; `#f5f5f5` + `cmin=0.0, cmax=1.0` mantidos; fix `ADM0_A3` para estados do Brasil (61 registros); resolução das linhas de costa elevada para 50m Natural Earth; adicionadas fronteiras de países (50m) e divisas estaduais do Brasil (10m) como traces `go.Scatter3d` separados; linhas de costa com cor cinza escuro `rgba(60,60,60,0.9)` (antes branco); refatoração interna com helper genérico `_extract_lines_360()` + funções dedicadas `_extract_borders_360()` e `_extract_brazil_states_360()`; `script_version='5.17'`
- `scripts/s29_walker_cell_mundo.py`: v5.3 — substitui `go.Volume` por `go.Isosurface` (mesmo traço do script de referência, mais robusto para grades esparsas); muda coordenada z do volume e do piso de pressão negada (-hPa) para pressão positiva (+hPa); eixo z agora usa `type='log'` + `autorange='reversed'` (1000 hPa embaixo, 100 hPa em cima); `opacity=0.5`, `surface_count=7`; `script_version='5.3'`
- `scripts/s29_walker_cell_mundo.py`: novo script — Célula de Walker v5.0; visualização 3D com **Plotly** (`go.Volume` + `go.Surface` + `go.Scatter3d`): piso com SSTA OISSTv2 (oceano) + continentes verdes (`go.Surface`), linhas de costa em coordenadas 0-360 (`go.Scatter3d`), volume 3D de anomalia de omega com `go.Volume` (isosuperfícies semi-transparentes empilhadas, opacity=0.12, surface_count=21, NaN mask para valores near-zero) e taper gaussiano latitudinal criando efeito de "colunas luminosas"; Gaussian blur + interpolação para 25 níveis antes da renderização; colormap azul-âmbar; câmera e aspecto 2:1:0.55 para perspectiva sul-elevada; saída HTML interativo (`include_plotlyjs='cdn'`) + PNG via kaleido; `script_version='5.0'`; saída em `Saida/s29_WALKER_CELL_MUNDO/`
- `pyproject.toml`: `plotly>=6.0.0,<7` e `kaleido>=1.0.0` adicionados às dependências
- `app/src/uteis/downloaders_omega_era5.py`: novo downloader ERA5 `vertical_velocity` em múltiplos níveis de pressão; cache mensal; validação de período; protegido com `_HDF5_LOCK`
- `app/src/uteis/downloaders_gdas_omega.py`: novo downloader GDAS omega multi-nível via NOMADS filter (`var_VVEL`, múltiplos `lev_XXX_mb`); padrão idêntico ao `downloaders_gdas_uv850.py`; salva NetCDF diário com dim `pressure_level`
- `app/src/uteis/clim_PSL_omega_multilevel.py`: novo downloader PSL omega multi-nível; abre uma sessão Playwright única e itera os níveis; mescla os arquivos individuais em um único netCDF com dim `level` (`clim_omega_ML_*`); cache em dois níveis (por nível + arquivo mesclado)
- `app/cli/run_script.py`: s29 registrado no SCRIPTS dict
- `app/settings/settings.toml`: `RUN_S29 = true` adicionado
- `settings.local.example.toml`: `# RUN_S29 = false` adicionado

- `scripts/s28_hovmoller_iod.py`: novo script — Hovmöller IOD; cópia do s27 com domínio 5S-5N, 50E-103E (Oceano Índico, sem cruzamento do antimeridiano); nomes de arquivo `hovmoller_sst_u850_INDICO_*`; saída em `Saida/s28_MONITORAMENTO_IOD_HOVMOLLER/`
- `app/cli/run_script.py`: s28 registrado no SCRIPTS dict
- `app/settings/settings.toml`: `RUN_S28 = true` adicionado
- `settings.local.example.toml`: `# RUN_S28 = false` adicionado

- `scripts/s27_hovmoller_enso.py`: novo script — Hovmöller de Anomalia de TSM (OISSTv2, 0.25°, idêntico ao s11) e Vento Zonal 850 hPa (ERA5/GDAS híbrido + climatologia PSL u-zonal via `clim_PSL_wnd_zonal_850.get_clim_wnd_zonal_850_path`); SST shaded com `LST_ANOM_CORRETA`/`LST_SSTA_NEW_GREC`; isolinhas U850 anom (azul=negativo, vermelho=positivo, limiar 3 m/s); domínio 5S-5N, 160E-80W (cruza antimeridiano); salva PNG + NetCDF em `Saida/s27_MONITORAMENTO_ENSO_HOVMOLLER/`
- `app/cli/run_script.py`: s27 registrado no SCRIPTS dict
- `app/settings/settings.toml`: `RUN_S27 = true` adicionado
- `settings.local.example.toml`: `# RUN_S27 = false` adicionado

- `scripts/s26_chi200_anom_div_fluxo_rossby_wave.py`: novo script — chi200 shaded (verde→bege→marrom, `LinearSegmentedColormap`, ±60×10⁵ m²/s) + contornos pretos de anomalia de geopotencial 250 hPa + vetores WAF (Takaya & Nakamura 2001) + vento divergente 200 hPa onde chi200<0 entre -20° e 20° (paleturquoise com contorno preto); suporte a defasagem interativa entre chi200+vento divergente e geopotencial+WAF; cópia estrutural do s25 sem o download de OLR
- `app/cli/run_script.py`: s26 registrado no SCRIPTS dict
- `app/settings/settings.toml`: `RUN_S26 = true` adicionado
- `settings.local.example.toml`: `# RUN_S26 = false` adicionado

- `scripts/s23_sst_todas_areas_correntes_marinhas.py`: novo script — TSM shaded (OISSTv2) + streamlines de correntes marinhas de superfície (CMEMS GLOBAL_ANALYSISFORECAST_PHY_001_024, dataset `cmems_mod_glo_phy_cur_anfc_0.083deg_P1D-m`); OLR e vento 1000 hPa removidos; `copernicusmarine` adicionado ao `pyproject.toml`; credenciais `CMEMS_USERNAME`/`CMEMS_PASSWORD` adicionadas ao `.secrets_example.toml`
- `app/cli/run_script.py`: s23 registrado no SCRIPTS dict
- `app/settings/settings.toml`: `RUN_S23 = true` e `LST_AREAS_S23` adicionados

### Modificado

- `scripts/s13_sst_todas_areas.py` → renomeado para `s13_sst_todas_areas_vento_medio_1000.py`; `app/cli/run_script.py`: module e description atualizados

### Adicionado

- `scripts/s21_wnd850_anom_chi200_anom.py`: novo script — CHI200 shaded (sem contorno) + streamlines do vento anômalo 850 hPa (dimgray) + vento divergente 200 hPa apenas onde chi<0 entre -20° e 20°; áreas iguais ao s20; registrado no SCRIPTS dict e em `LST_AREAS_S21`
- `app/cli/run_script.py`: s21 registrado no SCRIPTS dict
- `app/settings/settings.toml`: `RUN_S21 = true` e `LST_AREAS_S21` adicionados
- `scripts/s07_ssta_vento850_anom.py`: novo script — anomalia de TSM (OISSTv2) sobreposta com vetores de vento anômalo 850 hPa (pipeline ERA5/GDAS + climatologia PSL); 26 áreas, sem blue marble, geração sequencial
- `app/cli/run_script.py`: s07 registrado no SCRIPTS dict e em `list_scripts()`
- `app/settings/settings.toml`: flag `RUN_S07 = true`

### Corrigido

- `scripts/s06_ssta_todas_areas.py`: substituída geração paralela (`ProcessPoolExecutor`) por sequencial — `ProcessPoolExecutor` com fork copiava o array blue_marble (96 MB) para cada worker, estourando a RAM do WSL ao rodar `--all`; qualidade da imagem restaurada para resolução original (8192×4096)

### Modificado

- `scripts/s21_wnd850_anom_chi200_anom.py` e `s22_wnd850_anom_olr_anom_div.py`: boxes oceanográficos (atlantico_tropical, iod, plot_box genérico) elevados de `zorder=100` para `zorder=300` — ficam acima do vento divergente (zorder=200) e não são cortados pelos vetores; `script_version` s21→1.5, s22→1.1
- `scripts/s22_wnd850_anom_olr_anom_div.py`: novo script — OLR shaded (BrBG_r, ±40 W/m²) + streamlines vento anômalo 850 hPa (black) + vento divergente 200 hPa onde chi200 < 0 e ±20° (white/black stroke); mesmas áreas e configurações de streamplot do s21; registrado no SCRIPTS dict e em `LST_AREAS_S22`
- `scripts/s21_wnd850_anom_chi200_anom.py`: adicionado `add_cyclic_point` ao vento 850 hPa — corrige descontinuidade das streamlines em 180° em áreas com extent cruzando o antimeridiano (ex: MJO); `script_version` → `1.4`
- `scripts/s21_wnd850_anom_chi200_anom.py`: vento divergente com preenchimento branco e contorno preto (`path_effects.withStroke`); `zorder` elevado para 200 (acima dos contornos de costa/fronteiras em 100); `script_version` → `1.3`
- `scripts/s21_wnd850_anom_chi200_anom.py`: `linewidth` das streamlines agora configurável por área via `_STREAMPLOT_LINEWIDTH` (padrão 0.5 em todas); cor das streamlines alterada para `'black'`; `script_version` → `1.2`
- `scripts/s21_wnd850_anom_chi200_anom.py`: densidade das streamlines aumentada para 2.5 em tropico/psa/pacifico_leste_america_sul/mjo/hemisferio_sul/globo/globo_3d/enso/america_sul_zom_out e para 3.0 em atlantico_tropical/amo; streamlines habilitadas em `globo_3d` (remoção do guard `if not is_polar`); `script_version` → `1.1`
- `scripts/s20_olr_wnd850_geop500_anom.py`: cor dos vetores de vento alterada de `'k'` para `'dimgray'`; `script_version` → `1.1`
- `scripts/s15_chi200_psi200_anom.py`: adicionado quiver do vento divergente restrito a chi200 < 0 na faixa -20° a 20° (`QUIVER_STEP=4`, `QUIVER_SCALE=80`); removidos contornos brancos do shaded de CHI200; `script_version` → `1.2`
- `scripts/s16_wnd250_zonal_anom_div.py`: quiver do vento divergente restrito às regiões com chi200 < 0 — carregado campo escalar `chi_anom` do `chi200.nc`, amostrado no mesmo step do quiver e aplicada máscara `chi >= 0`; `script_version` incrementado para `1.1`
- `scripts/s15_chi200_psi200_anom.py`: removida plotagem do vento divergente (quiver) — constantes `QUIVER_DEFAULTS`, `QUIVER_POR_AREA`, `UCHI_CANDIDATES`, `VCHI_CANDIDATES` e funções `_add_cyclic_uv`, `_prepare_quiver_masked`, `_get_quiver_config` deletadas; `script_version` incrementado para `1.1`
- `app/src/uteis/plot_geop250.py`: substituída concatenação em memória por acumulador streaming (`_compute_period_mean_streaming`) — processa um arquivo mensal por vez, evitando estouro de RAM no WSL para períodos longos (3–4 meses de dados globais ERA5/GDAS)
- `app/src/uteis/clim_PSL_psi200.py`: reescrito com cache por período MM-DD, logger, path via settings e bug de tupla morta corrigido (não utilizado no pipeline — s03 reutiliza `clim_PSL_wnd200.py`)
- `app/src/uteis/clim_PSL_wnd850.py`: criado pelo usuário; corrigido erro na mensagem de exceção (200→850)
- `app/src/uteis/plot_olr_wind250_anom.py`: migrado para pipeline híbrido ERA5/GDAS (250 hPa) + PSL clim + streaming; removidas importações quebradas de plot_chi200.py
- `app/src/uteis/plot_olr_wind850_anom.py`: criado seguindo mesmo padrão do 250 hPa, para anomalia de vento 850 hPa
- `scripts/s06_olr_wind_250_850_anom.py`: renomeado de s06_olr_wind250_anom.py; adicionado pipeline 850 hPa com figuras com sufixo `_250hPa.png` e `_850hPa.png`; OLR download com aria2c; try/except removido
- `app/cli/run_script.py`: s06 atualizado para novo módulo e support_files limpos

### Adicionado (s06/850hPa)

- `app/src/uteis/downloaders_wind850.py`: downloader ERA5 u/v em 850 hPa
- `app/src/uteis/downloaders_gdas_uv250.py`: downloader GDAS u/v 250mb via NOMADS
- `app/src/uteis/downloaders_gdas_uv850.py`: downloader GDAS u/v 850mb via NOMADS
- `app/src/uteis/clim_PSL_wnd250.py`: criado pelo usuário; corrigido erro na mensagem de exceção (200→250)
- `app/src/uteis/plot_rossby_waf.py`: migrado para pipeline híbrido ERA5/GDAS (hgt 250 hPa) + PSL hgt+uv 250mb + streaming accumulator; removida dependência de funções do plot_psi200.py
- `scripts/s04_fluxo_rossby_wave.py`: `script_version` incrementado para 2.0
- `app/cli/run_script.py`: removidas as 3 climatologias locais dos `support_files` do s04
- `app/src/uteis/plot_psi200.py`: migrado para pipeline híbrido ERA5 (200 hPa)/GDAS + climatologia PSL u/v 200mb (via `clim_PSL_wnd200.py`) + streaming accumulator; anomalia calculada de u/v primeiro, depois psi — mesmo padrão do s02
- `scripts/s03_psi200_anom.py`: `script_version` incrementado para 2.0
- `app/cli/run_script.py`: removidas climatologias uwnd/vwnd250 dos `support_files` do s03
- `app/src/uteis/clim_PSL_wnd200.py`: reescrito com cache por período MM-DD, logger, path via settings e uma única sessão Playwright para u e v
- `app/src/uteis/plot_chi200.py`: migrado para pipeline híbrido ERA5 (200 hPa)/GDAS + climatologia PSL + streaming accumulator (mesmo padrão do s01)
- `scripts/s02_chi200_anom.py`: removido try/except genérico em torno de `plot_chi200()`; `script_version` incrementado para 2.0

### Adicionado (s02)

- `app/src/uteis/downloaders_wind200.py`: downloader ERA5 u/v em **200 hPa** (separado do wind250 que continua em uso por s03/s04/s06)
- `app/src/uteis/downloaders_gdas_uv200.py`: downloader GDAS u/v 200mb via NOMADS Grib Filter (padrão do downloaders_gdas_hgt250)

### Removido

- Scripts de teste legados da raiz: `teste.py`, `teste00_dados_vento_SEMOP.py`, `teste01_dados_anom_geop.py`, `teste02_dados_anom_chi200.py`
- Módulos legados da API Ampere: `app/common/ampere_api_client.py`, `app/config.py`, `app/src/reanalise.py`, `app/src/prompt_data.py`, `app/src/gridlines.py`
- `AMPERE_API_BASE_URL` do `settings.toml` e bloco `AMPERE_API_USERNAME/PASSWORD` do `settings.local.example.toml`

### Adicionado

- `--force-rerun` no `run_script.py` — invalida o cache de um script especifico antes de executar (ex: `uv run python run_script.py s05 --force-rerun`), forcando o reprocessamento sem mexer no cache dos demais. Portado do `main.py` legado
- Script s06: Anomalia de OLR + linhas de corrente da anomalia do vento em 250 hPa (OLR do PSL/NOAA + vento ERA5/CDS)
- `app/src/uteis/plot_olr_wind250_anom.py` — modulo de processamento que reutiliza downloader de vento 250 hPa e climatologias uwnd/vwnd para calcular anomalia
- `RUN_S06` — flag de execucao do s06 no settings.toml
- Script s05: Anomalia de OLR (Outgoing Longwave Radiation) a partir de dados CPC Blended OLR do PSL/NOAA
- Download integrado no s05 (HTTP simples via `download_with_progress`, sem necessidade de downloader separado)
- `RUN_S05` — flag de execucao do s05 no settings.toml
- Script s04: Rossby Wave Activity Flux (Takaya & Nakamura 2001) com vetores WAF sobre anomalia de hgt 250 hPa
- `app/src/uteis/plot_rossby_waf.py` — modulo de processamento que reutiliza downloaders existentes (hgt250 + climatologias uwnd/vwnd) e calcula WAF via pacote `tnflux`
- Dependencia `tnflux` adicionada ao projeto
- `RUN_S04` — flag de execucao do s04 no settings.toml
- Script s02: anomalia de CHI200 (velocity potential) com vetores de vento divergente
- `app/src/uteis/plot_chi200.py` — modulo de download, processamento e calculo de anomalia CHI200 via scipy (Helmholtz decomposition)
- `app/src/uteis/downloaders_wind250.py` — downloader de u/v 250 hPa (ERA5) em formato NetCDF
- `FILE_CLIMATOLOGIA_UWND250` e `FILE_CLIMATOLOGIA_VWND250` — settings para climatologias de vento 250 hPa
- Climatologias uwnd250/vwnd250 adicionadas como `support_files` do s02 (download automatico via SFTP)
- `RUN_S02` — flag de execucao do s02 no settings.toml
- Interpolacao automatica da climatologia de vento para o grid do ERA5 (mesmo padrao do s01/geop250)
- Configuracao de quiver por area via `CHI200_QUIVER_POR_AREA` no settings (scale, width, step, min_mag, headwidth, headlength, color)
- Boxes ENSO (Nino 1+2, 3, 3.4, 4), IOD e TSA/TNA no s02

### Alterado

- Mensagem de interrupcao por Ctrl+C (`KeyboardInterrupt` no `@friendly_errors`) agora exibe um bloco mais visivel informando que o script foi cancelado
- `downloaders_wind250.py`: download de u/v 250 hPa migrado de GRIB para **NetCDF** — elimina dependencia de ecCodes C library (`cfgrib`)
- `downloaders_wind250.py`: removidos parametros `area` e `grid` — download global sem filtro (padrao hgt250)
- `downloaders_wind250.py`: download de u e v agora em **requisicoes CDS separadas em paralelo** — cada variavel baixa independentemente via ThreadPoolExecutor, depois mescla com `xr.merge()` (melhora significativa de velocidade)

### Removido

- `main.py` — entry point legado removido. So conhecia o s00 e usava imports proibidos pelo CLAUDE.md (`app.config`, `app.common.logger`). O entry point unico passa a ser `run_script.py` → `app/cli/run_script.py`, que ja registra s00–s06 no dict `SCRIPTS`. As funcionalidades uteis do `main.py` foram portadas (`--force-rerun`); o tratamento de Ctrl+C ja existia no `@friendly_errors`

### Corrigido

- s05/s06: arquivo `olr.day.anom.nc` (PSL/NOAA, atualizado diariamente) nao era re-baixado quando ja existia localmente, gerando mapas em branco ou com dados incorretos quando o periodo solicitado nao estava coberto pelo arquivo local cacheado. Agora o script consulta o periodo solicitado (DATA_INICIAL/DATA_FINAL) contra o arquivo local e so re-baixa se necessario; se mesmo apos download o periodo nao estiver disponivel, aborta com mensagem clara mostrando a primeira/ultima data disponivel no arquivo
- `dataset_utils.arquivo_cobre_periodo`: novo helper que verifica se um arquivo NetCDF existe e cobre completamente um intervalo `[start_date, end_date]`. Util para evitar re-downloads desnecessarios de datasets atualizados periodicamente (PSL/NOAA OLR diario, etc.)
- `dataset_utils.validar_cobertura_temporal`: novo helper que valida cobertura temporal de um dataset e levanta `RuntimeError` com mensagem clara contendo as datas disponiveis se o periodo solicitado nao estiver coberto ou tiver gaps
- `download_helper.download_with_progress`: novo parametro `max_age_hours` (Optional[float]) — checa mtime do arquivo local e re-baixa se exceder o limite. Util para datasets atualizados diariamente
- Logo do s02 deslocado para cima — substituido `fig.add_axes` + `get_position` por `AnchoredOffsetbox` (ancorado ao axes, sobrevive a `bbox_inches='tight'`)
- Faixa branca na plotagem do s01 na divisa dos hemisferios — removida conversao 0-360 e sort manual de lon, usando `sortby` + `add_cyclic_point` direto no DataArray (mesmo padrao do s00)

### Adicionado

- `app/src/uteis/plot_geop250.py` — modulo de download, processamento e calculo de anomalia geopotencial 250 hPa (ERA5)
- `FILE_CLIMATOLOGIA_GEOP250` e `REMOTE_CLIMATOLOGIA_GEOP250` — settings para climatologia geopotencial 250 hPa
- Climatologia geop250 adicionada como `support_files` do s01 (download automatico via SFTP)
- Docstrings descritivas nos scripts (`s00`, `s01`) com finalidade, dados de entrada/saida, datas de criacao e atualizacao
- `SCRIPT_ID`, `SCRIPT_NAME`, `SCRIPT_DESC` derivados de `__file__` e `__doc__` — logger, cache e header dinamicos
- `LST_AREAS_S01` no `settings.local.toml` — lista de areas de plotagem do s01 configuravel sem mexer no git
- `.hooks/` — diretorio para shell scripts de desenvolvimento (changelog-reminder.sh)
- **Refatoracao completa de infraestrutura** seguindo padroes dos projetos `usando_api_ampere` e `windx-automatico`
- Migracao de Poetry para **UV** como package manager
- `setup.sh` — setup automatizado com menu de ambiente (development/production/qa), copia configs, cria diretorios
- `setup.sh` — copia automatica de `.vscode/settings_exemplo.json` para `.vscode/settings.json`
- `.python-version` — Python 3.12.9 fixo
- `.env.example` — template para `ENV_FOR_DYNACONF`
- `.vscode/settings_exemplo.json` — template VSCode com icones e cores do projeto
- Modulo `app/shared/` com infraestrutura reutilizavel:
  - `singleton.py` — Metaclass Singleton
  - `settings_factory.py` — Dynaconf factory com Singleton
  - `logger.py` — LoggerService com Loguru (Singleton, silencia cdsapi/paramiko)
  - `sftp_client.py` — Cliente SFTP enxuto com context manager
- Modulo `app/cli/run_script.py` — CLI com argparse e dicionario SCRIPTS
- `run_script.py` — entry point wrapper na raiz
- Download automatico de arquivos de suporte (`support_files`) via SFTP antes da execucao de cada script
- Verificacao de arquivos obrigatorios (`required_files`) com `FileNotFoundError` claro antes da execucao
- Saida colorida no `--list` com status de cada script e seus requisitos (ANSI colors)
- Suporte a 3 ambientes: development (SFTP + DEBUG), qa, production
- `app/settings/.secrets_example.toml` — template para credenciais (KEY_CDS + SSH)
- `settings.local.example.toml` — template simplificado para datas e flags
- Secao `[qa]` no `settings.toml`
- Configuracoes de logging por ambiente (LEVEL_LOGGING, LOGGER_BACKTRACE, LOGGER_DIAGNOSE)
- README reescrito com diagramas Mermaid (fluxo geral, s00, s01, prioridade de settings)
- QUICKSTART atualizado para UV e novo CLI
- `GUIA-NOVOS-SCRIPTS.md` — guia completo para adicionar novos scripts ao projeto
- Skill Claude Code `.claude/skills/adicionar-novo-script.md` para assistencia ao adicionar scripts
- `app/shared/error_handler.py` — Handler global de excecoes com `@friendly_errors` e mapa `_ERROR_HINTS`
- Skill Claude Code `.claude/skills/tratamento-de-erros.md` para guia de tratamento de erros
- Secao "Tratamento de Erros" no README e GUIA-NOVOS-SCRIPTS.md
- `dados/` — diretorio dedicado para dados baixados do CDS (separado de `Entrada/` que mantem arquivos fixos)
- `DIR_DADOS` — nova setting para configurar diretorio de dados baixados por ambiente
- `.claude/rules/` — regras adaptadas do windx-automatico (code-style, security, architecture, gotchas, review, testing)
- Diagrama mermaid de separacao de diretorios (Entrada/ vs dados/ vs Saida/) no README
- `pyproject.toml` com ruff, isort, taskipy e pytest configurados (alinhado com windx-automatico)
- `.pre-commit-config.yaml` com hooks de lint, format, sort, fix e changelog-reminder
- `scripts/changelog-reminder.sh` — lembrete de atualizar CHANGELOG em cada commit
- Detalhes de conexao SFTP (IP, porta, chave) nas mensagens de log e erro
- Validacao de integridade de arquivos .nc/.grb com retry automatico (apaga + re-download 1x)
- `FILE_CLIMATOLOGIA_VENTO100M` e `REMOTE_CLIMATOLOGIA_VENTO100M` configuraveis via settings

### Alterado

- `downloaders_hgt250_ERA5.py`: download de geopotencial 250 hPa migrado de GRIB para **NetCDF** — elimina dependencia de ecCodes C library (`cfgrib`)
- `pyproject.toml` migrado de Poetry para UV (hatchling)
- Imports em todos os modulos atualizados: `app.config` → `app.shared.settings_factory`, `app.common.logger` → `app.shared.logger`
- `settings.toml` refatorado com ambientes (development/qa/production) e configuracoes de logging
- CLI simplificado: `uv run python run_script.py s00` em vez de `poetry run python main.py --script s00`
- Credenciais movidas de `settings.local.toml` para `app/settings/.secrets.toml`
- `.gitignore` atualizado para UV (uv.lock, .secrets.toml, poetry.lock)
- Downloaders e processadores agora salvam em `dados/` (DIR_DADOS) em vez de `Entrada/arquivos_nc/`
- Climatologia vento 100m agora lida do settings (`FILE_CLIMATOLOGIA_VENTO100M`) em vez de path hardcoded
- Scripts `s00` e `s01` refatorados: paths via `Path(settings.DIR_OUTPUT)`, `Path(settings.DIR_INPUT)`, `Path(settings.DIR_DADOS)`
- `s01`: diretorio de saida renomeado de `s04_GEOP250` para `s01_GEOP250`
- `s01`: `os.path` substituido por `pathlib.Path`, `raise Exception` por `raise RuntimeError from err`
- Logging nos scripts: f-strings substituidas por `%s` (lazy formatting do loguru)
- `settings.json` reformatado (whitespace consistente) e limpo (`lst_emails` removido — vinha de outro projeto)
- `s01`: `lst_areas` hardcoded movido para `settings.LST_AREAS_S01`
- `Scripts/` renomeado para `scripts/` (convencao Python lowercase)
- `s01`: referencias `s04_GEOP250` corrigidas para `s01_GEOP250` no README, QUICKSTART e list_scripts
- `s01`: descricao de requisitos no CLI atualizada (climatologia + legenda + dados em `dados/`)
- `scripts/changelog-reminder.sh` movido para `.hooks/changelog-reminder.sh`
- `REMOTE_CLIMATOLOGIA_VENTO100M`: path corrigido para `.../climatologia/` em vez de `.../Entrada/arquivos_nc/`

### Corrigido

- `s01`: logger usava `'s04'`, cache key `'s04'` e mensagens `'S04'` — corrigido para `'s01'`
- `s01`: path da legenda Atlantico usava `Path.cwd()/Entrada/` hardcoded — agora usa `settings.DIR_INPUT`

### Removido

- `database.md` das rules (nao ha banco de dados neste projeto)

- `app/logging_config.json` (substituido por LoggerService)
- Dependencia de Poetry (substituido por UV)

### Corrigido

- Logging excessivo do `cdsapi` (debug=False nos downloaders + logging.WARNING)
- Logging excessivo do `paramiko` (logging.ERROR)

---

## [0.1.0] - 2026-03-13

Commit: `6bba154`

### Adicionado

- Estrutura inicial do projeto
- Script s00: download e plotagem de vento a 100m + MSLP do ERA5
- Script s01: download e plotagem de anomalia de geopotencial em 250hPa
- Modulo `app/common/` com infraestrutura reutilizavel:
  - `logger.py` - Sistema de logging com Loguru (rotacao, retencao, thread-safe)
  - `cache_manager.py` - Cache inteligente com hash MD5 de parametros
  - `connections.py` - Conexao SSH/SFTP via Paramiko
  - `download_helper.py` - Download multi-engine com retry e barra de progresso
  - `file_loader.py` - Carregamento transparente local/remoto de arquivos
  - `dataset_utils.py` - Utilitarios xarray para NetCDF
  - `parallel_helper.py` - Processamento paralelo com ThreadPoolExecutor
- Modulo `app/src/uteis/` com downloaders e processadores ERA5
- Configuracao via Dynaconf com suporte a ambientes (development/production)
- Suporte a SSH/SFTP para acesso remoto a dados (modo development)
- `main.py` com CLI para execucao de scripts (--script, --force-download, --force-rerun, --clear-cache)
- Configuracoes de 30+ regioes geograficas para plotagem (settings.json)
- Template de configuracao local (settings.local.example.toml)
- `.gitignore` configurado para excluir dados pesados (.nc, .grb), saidas e credenciais
